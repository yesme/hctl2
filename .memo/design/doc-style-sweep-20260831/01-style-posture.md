# 量尺落库记录与库内缺口种子

> 状态：已落地（量尺）· 待拍板（缺口处置与松紧）<br>
> 基线：main @ `b41ff6c`（草案 v0.15.4）<br>
> 去向：量尺已进 [`WRITING-GUIDE.md`](../../../WRITING-GUIDE.md)；缺口清单待裁决后进结构与语言两个 PR

## 已裁决：量尺直接进库

所有者裁定（2026-08-31）：文风指南就是一篇文档，直接复制进 code base，内容按本库需要改，不必与上游 1:1。

据此落地：`yesme/abacistopia` 的 `SPEC-WRITING-GUIDE.md` 改编为本库根目录的 `WRITING-GUIDE.md`，从 `AGENTS.md` 的 Repo 地图与设计地图的「文档纪律」链入，并加入 `materialize_repo_tree.sh` 的 lint 范围（链接与退役词检查照常覆盖）。

**放弃的方案**：原打算写一份「外部单源 + Follows/Profiles/Defers 姿态声明」，把上游留在另一个仓库。放弃理由：跨仓库无法相对链接，本库读者读不到；而文风指南不是合同，两个仓库各持一份并各自演化没有权威冲突问题——我先前把「合同才需要的单源纪律」错套到了工作辅助件上。

**改编要点**（相对上游）：

- 适用范围从「网络协议规格与白皮书」改为本库的设计文档与合同。
- 目标文件路由表改写为**四层路由表**（愿景 / 架构 / 机制 / 交付与验证），附读者停靠点与失败判据——这是所有者「横向分割」诉求的落点，原表的 abacistopia 文件名全部替换。
- 规范性关键词改为本库三档（必须·不得 / 应当·不应 / 可以），并把「只能」定为 MUST 的排他形态、「需要」逐出规范用词。
- 新增三条（`## HCTL2 增补`）：**S1 压缩下限**、**S2 自造词密度上限**、**S3 分层语域**。S1 是本轮的核心新规则——上游只有压缩的上限（M6 密度优先于覆盖），没有下限，而上一轮大修恰好把那股力用过了头。
- 删去与本库无关的条目（原语合约模板与 conformance、确定性编码、演进纪律、对手模型、wire format、测试向量、identifier 后缀分型），理由逐条记在指南文末的「未采纳的上游条目」表，避免反复讨论。
- 清理全部指向 abacistopia 自有文件的链接（原文有 6 处）。

**待所有者复核**：S1 / S2 / S3 三条是我起草的，未经裁决就随量尺一并入库了（理由：四家 harness 即将开工，量尺缺了这三条就打不中所有者点名的病）。评审时可整节删除，不影响其余部分。

## 库内已知缺口（findings 种子，非全量）

| 类 | 事实 |
| --- | --- |
| T1 关键词 | 两档、SHOULD 档全空：必须 138 / 不得 72 / 只能 51 / 可以 131；应当、不应各 0；另有 82 处「需要」强度不明——同时承担「描述性必要条件」与「规范性应当」两种意思 |
| L10 文档头 | `agent.md`、`delivery.md`、`project.md`、`run.md`、`task.md` 五个设计正文缺状态行；`architecture.md` / `vision.md` / `spec/*` / `glossary.md` 都有 |
| L5 超长块 | 单行最长 1857 字（`spec/project.md:61`）；前 12 名全部 >1150 字，集中在 `spec/agent.md`（4 处）、`spec/system.md`、`spec/run.md`、`spec/task.md` |
| L3 术语 | `harness`(29) / `Harness`(132) 大小写两形，而 `spec/README.md:24` 把**小写**定为系统角色名；「路标」(30) / 「机械位置」(5) / 「Engine 位置」(2) 三名一物；「工具箱」(52) / `hctl2-tool`(12) 并存——本身合法，但要按 L3 显式声明为一对一映射 |
| L2 主语 | `spec/system.md:189`「备份完成前验证快照边界……」谁验证？`spec/system.md:70`「动作必须禁用、保留为待处理请求或安全拒绝」谁禁用，客户端还是 control？ |
| L5 复合约束 | `spec/README.md:18` 一段七条规则；`spec/run.md:37` 正常完成谓词一句四个分号条件 |
| S1 压缩下限 | `spec/system.md:171`（SQLite 锁）、`architecture.md:87`（路标停更三裁决压十五字） |
| S2 概念串烧 | `spec/run.md:79` 一句五个代次概念 |

## 待裁决

1. **松紧基准**：见 [`04-calibration.md`](./04-calibration.md)，18 条样本里 5 条判「合格不改」，逐条给「改 / 不改」。
2. **两个边界案例**（`04-calibration.md` 的 A8 与 B4）：定了就有判例可循——架构层的标签可以不解释到什么程度；哪些流程描述属于合同本身、不必改写成约束。
3. **T1 归档方向**：51 处「只能」与 82 处「需要」怎么归档（指南已定规则，逐处归档由 K3 出清单、所有者定原则）。
4. **L4 混排术语**：「Engine 检查点」「binding 分歧」「直接 mutation」这类是逐个翻译，还是列白名单豁免。
5. **S1 / S2 / S3 三条是否保留**，措辞是否收紧。
6. **重叠去重**：设计地图「文档纪律」第一条（分层写作：设计正文只用核心产品词）与指南 0.4 路由表在「允许词汇」上重叠。建议文档纪律那条收缩为一句加链接，词汇归属留在合同层总则、语域归指南。这条属于纯表述归并，可进结构 PR。
