# 任务书（一家一节，整节投喂）

> 状态：可发包<br>
> 基线：main @ `b41ff6c`（草案 v0.15.4）

## 共同前提（每份任务书都已内嵌，此处只作备查）

- **先 `git pull` 到最新 main**，量尺已经进库：根目录 `WRITING-GUIDE.md`。四层路由表在它的 §0.4，本轮三条重点在 `## HCTL2 增补`（S1 压缩下限、S2 自造词密度、S3 分层语域）。
- **只报不改。** 不编辑任何 `docs/` 文件，不提交，不开 PR。本轮产出全部是 findings 文件。
- **findings 写回 `/Users/haibara/workspace/hctl2-claude/.memo/design/doc-style-sweep-20260831/`**（绝对路径，跨 worktree 汇总用；这些 findings 尚未提交，不会冲突）。
- 量尺的增补三条尚未经所有者裁决，与 findings 一并校准。发现量尺本身有问题就在文件末尾单开「对量尺的异议」一节，不要私自变通。

---

## Grok · 结构审计（`10-grok-structure.md`）

先读 `WRITING-GUIDE.md` §0.4（四层路由表与读者停靠点）与本目录的 `02-layer-routing.md`（审计口径与纵向聚类种子），再读全部 21 个在范围文件。产出三件：

**一、横向越层清单。** 逐文件、到**节**粒度核对「实际语域 vs 该层语域」，分两类报：

- **下沉**：高层文件里出现了低层细节（合同词、状态机口径、字段名、锁路径）。
- **上浮**：低层文件里出现了本该在高层的论证、口号或愿景表述。

上一轮大修只查过下沉，**上浮是本轮新增的方向，请重点扫**。格式：

```
| # | 文件:行(节名) | 下沉/上浮 | 越到哪层 | 原文摘要 | 建议：删 / 改引用 / 搬到哪个文件 |
```

**二、纵向聚类矩阵。** 补全 `02-layer-routing.md` 的八簇种子（可增删簇），对每簇回答三问：是同一机制吗？权威定义在哪？其余位置是引用还是复述？然后标出归并候选，并**逐条分两流**：

- **纯表述**：只改写法（抽一张共同语义表、其余改引用），合同含义不变。
- **触语义**：合并会改变含义、状态数或写入者。判据是「改完之后，`contract-tests.md` 里任何一条 CT 用例的判定结果是否可能变化」——可能变化就是触语义。

A 簇（六个代次）请优先做深：给出它们的层级关系图，以及「哪些必须逐项携带、哪些可以推导」。

**三、四个层归属疑点的裁决建议**（见 `02-layer-routing.md` 文末），每条给出建议 + 一句理由，不要只列选项。

---

## GPT · 语言首读（机制层 + 交付层，`11-gpt-language.md`）

**身份要求：以一个没参与过 HCTL2 设计讨论的资深工程师身份读**，遇到读不懂的地方**不要回头补设计上下文**——读不懂本身就是 finding。

范围（9 个文件，约 1190 行）：`docs/design/spec/README.md`、`spec/system.md`、`spec/connections.md`、`spec/project.md`、`spec/task.md`、`spec/run.md`、`spec/agent.md`、`docs/design/delivery.md`、`docs/design/contract-tests.md`。

先读量尺 `WRITING-GUIDE.md`，重点是 `## HCTL2 增补` 三条（S1 压缩下限、S2 自造词密度、S3 分层语域）与共用规则 L2 / L3 / L5 / L7。

产出：

```
| # | 文件:行 | 条号 | 原文（截 40 字） | 问题 | 建议改法 |
```

条号取 S1/S2/S3/L1/L2/L3/L5/L6/L7/L10/T1/T2/T3/T6 之一。**建议改法要写出改后的句子**，不要只写「建议改写」。

每个文件另附一段「首次读者判定」：读到哪里读不懂、哪里必须回读、哪个术语失联（出现时没有释义也没有链接）、哪几段像英译中。

`spec/system.md:36`（1356 字单段）、`spec/agent.md:34`（1730 字）、`spec/project.md:61`（1857 字）这三段请给出完整重排方案（拆成几段 / 哪些改编号列表）。

---

## GLM · 语言首读（愿景层 + 架构层 + 参考，`12-glm-language.md`）

身份要求同上：**没参与过设计讨论的资深工程师**。

范围（12 个文件，约 1255 行）：根 `README.md`、`docs/usage.md`、`docs/design/vision.md`、`architecture.md`、`design/README.md`、`project.md`、`task.md`、`run.md`、`agent.md`、`participant.md`、`context.md`、`references/glossary.md`。

先读量尺 `WRITING-GUIDE.md`。你这半边多一条重点：**愿景层（`vision.md` + 根 `README.md`）按宣言文风核**——问题定义是否精确且零修饰、现有方案是否以最强形态描述后再指出不足、设计选择是否从约束推导（而非功能枚举）、代价是否诚实写出、每节结论是否在第一段。架构层（其余）按「抓大放小」核：是否出现了只有合同层才需要的细节。

产出格式与 GPT 相同（表格 + 每文件「首次读者判定」）。

`docs/design/run.md:22–23`（「重试不灌票 / 换人不换裁判 / 掉线不丢身份」）是量尺认定的**正例基准**——请以它为尺子判断其他口号式表述是否达标，达标的明确标注「合格，不改」。宁可少报，不要把合格的口号也报成问题。

---

## K3 · 机械清单（`13-k3-mechanical.md`）

全部为机械扫描，范围是 21 个在范围文件。不需要判断，只要**全、准、可复算**，每项给 `文件:行`。

1. **规范性关键词全量表**：`必须` / `不得` / `禁止` / `只能` / `可以` / `需要` / `应当` / `不应` 每处的行号与整句。其中 **82 处「需要」逐处标注是「描述性必要条件」还是「规范性应当」**——这是本轮杠杆最大的一项。另需把「应」的命中里属于 `相应/对应/供应/应用/响应` 的词内命中剔除后重新计数。
2. **术语形态清单**：`harness` vs `Harness` 全部出现位置；`路标` / `机械位置` / `Engine 位置`；`工具箱` / `hctl2-tool`。然后**再扫一遍全库，找出其他同概念两形**（大小写不一、中英别名并存、缩写与全称混用）。
3. **超长块清单**：非表格行且 >300 字符的全部行，按文件分组，给字数并降序。
4. **文档头现状表**：21 个文件的「状态行 / 日期 / 版本戳」三项现状（已知 `agent.md`、`delivery.md`、`project.md`、`run.md`、`task.md` 缺状态行，请核实并补全其余）。
5. **中英混排术语清单**：形如「英文词 + 中文词」构成的复合术语全量（如 `Engine 检查点`、`binding 分歧`、`直接 mutation`），去重后给频次与首现位置。

完成后跑一次本地文档检查确认基线是绿的（`cd src && build/docs/materialize_repo_tree.sh && ./buck2 test root//build/docs/...`，应为 Pass 5），把结果写在文件末尾。
