# 设计文档四轴评审：方案与交接

> 状态：待拍板 · 执行方式待讨论（范围、分派、是否先抽成 Skill）<br>
> 基线：main @ `e4d0e55`（草案 v0.16.0）<br>
> 去向：评审产出分批进 `docs/design/`（约束变化配 CT 与版本）、`docs/research/`（调研）、`WRITING-GUIDE.md`（写作规则）；方法本身抽成 `src/agency/skills/` 下的评审 Skill

这份文件是给下一个接手的会话看的，不管它在哪台机器上。它回答三件事：现在的状态是什么、要做的评审是什么、我打算怎么做。所有者的四条评审轴是原则，记在 [`../../notes/review-four-axes-20260902.md`](../../notes/review-four-axes-20260902.md)；这里只写怎么执行。

## 一、接手时的状态

**基线**：main @ `e4d0e55`，设计草案 v0.16.0。2026-09-02 一天合入了六个我这边的 PR，顺序是：

| PR | 内容 | 对本轮评审的意义 |
| --- | --- | --- |
| #137 | `docs/research/methodology-mattpocock-skills-20260902.md`：对 mattpocock/skills 的逐源码审计 | 给出「方法归 Skill、产出归对象、管不住的归机制」的分界判据，本轮第三轴直接沿用 |
| #138 | `Participant` 立为第四模块，`Agent` 模块名退役；人不是 `Participant`；Skill 归 Agency 申报；Agency 是供给方不是工厂 | 模块表已改，第二轴的覆盖检查要以四模块（Project / Task / Run / Participant）为准 |
| #140、#142 | `src/agency/` 本地 Agency 参考实现，第一份 Skill `hctl2-shaping`，随发布包分发 | 评审方法将来抽成的 Skill 有家了 |
| #143 | Buck2 项目根挪到仓库根，`src/` 仍为 `root` cell；文档以 `repo//:docs_tree` 进图，`materialize` 桥与 USAGE 快照退役 | 文档检查全部是 Buck 目标，本轮的机械清点脚本可以照样做成目标 |

**刚收口的一轮**：文风与分层复审（`../doc-style-sweep-20260831/`），四家审计 → 十条裁决 → 结构批 #132 → 语言批 #134，基线 v0.15.6 收口。它的遗产是写作指南里的判例（S1 两关、L7 黑名单、L14 断句与连接）和文档纪律独立成篇。它**没做**的：「会改变含义」的归并候选一条未动，清单在该目录 README；写作指南里没有「避免驼峰名」和「同层中英一致」两条规则。这两样都归本轮。

**本机之外拿不到的东西**：我在这台 Ubuntu 上的会话记忆（用户偏好、PR 流程的坑）不在仓库里。与本轮有关的都已写进本文和四轴原则；PR 流程的坑写在 AGENTS.md 与 `.memo/notes/harness-discipline-20260829.md` 里，另有一条本机经验值得写明：Buck2 跑 `sh_test` 时 cwd 是测试所在 cell 的根（`src/`），不是项目根，写检查脚本时不要假设相对路径。

## 二、要评审什么

对象是全部设计文档：`docs/design/` 下愿景、架构、约束（含 `spec/` 七个文件）、交付与契约测试、参考文件，共 21 个文件；加上根 README、`WRITING-GUIDE.md`、`docs/design/doc-discipline.md`；`docs/research/README.md` 只审索引与借鉴决策，不重审各条目。

四条轴，每条轴的**判断单位**不同，这决定了怎么切工：

| 轴 | 判断单位 | 判什么 |
| --- | --- | --- |
| 方法论 | 场景之间的**边**：四个场景两两之间实际存在的交接（连接约束总表里的每一行） | 这条边该不该存在；过去的是什么、什么条件算过去、谁判；快好省三问 |
| 模块与架构 | **具名概念**与它所在的**层** | 概念过不过门槛；在不在该在的层；是核心概念还是实现方案；条款的目的是否被文本遮住 |
| 具体实现 | 约束层里每条「必须」背后的**强制手段**；每个**部件**的选型 | 强制手段是账本机制、适配器回读，还是只有文字；部件的业界最佳与借用等级 |
| 写作风格 | 句子与术语 | 沿用文风复审的尺子，加两条新规则：驼峰名、同层中英一致 |

## 三、我的方法（可移植）

五步。第一步和第三步尽量做成脚本，让 LLM 只做它非做不可的判断，这本身就是第三轴的要求。

**第一步，定标与切片。** 冻结基线 sha，列出文件清单，把四轴原则改写成四张检查表，每张表的每一行都能对一个位置说「过」或「不过」。判断单位按上表定死，评审员不许换单位。

**第二步，机械清点（脚本，不判断）。** 四张清点表，全部可复算：

1. 概念清点：从词汇表和正文抽出全部具名概念（代码体英文名、「」内的中文名），按文件、按层统计出现次数、定义位置、首次出现是否给了对照；同时标出驼峰拼接名，标出同一概念在同一层里中英两写的情况。这是第二轴和第四轴的输入。
2. 边界清点：从连接约束的总表抽出每一条交接，对每条列出交付物、写入者、准入条件、失败处理四格，空格就是空格。这是第一轴的输入。
3. 强制手段清点：约束层每一句含「必须／不得／只能」的句子，标出它依赖的机制词（比较并交换、摘要、租约、代次、归约器、回读、outbox），没有机制词的句子单列。这是第三轴（a）的输入，分类本身要人或模型看一眼，但候选集由脚本给出。
4. 部件清点：交付文档与研究索引里的每个外部产品和第一方部件，列成「目的、现选择、借用等级」三列的骨架，等第三轴（b）填。

清点脚本放 `src/build/docs/`，会长期用的做成 Buck 目标（比如驼峰名与同层中英一致，可以直接加进文档检查），一次性的把命令和输出一起留在评审目录里，保证可复算。

**第三步，业界调研（只为第一轴和第三轴 b）。** 第一轴：在 `methodology-landscape-20260824.md` 的十二族基础上换一个问题再扫一遍——不再问「谁拥有完成判定权」，而是问「每家把阶段边界画在哪、边上递什么、谁判可以过」；找三类东西：别家画了而我们没有的边（遗漏）、只有我们画的边（可能是过早投票）、别家定义得比我们干净的交付物。第三轴 b：对部件清点的每一行找业界最佳，按「跨平台二进制、SDK、复制代码、借鉴想法」四级标借用等级，写取舍。调研纪律照旧：一对象一文件进 `docs/research/`，钉 commit，宣传语不作数。

**第四步，判断（多路评审加对抗核验）。** 每条轴一份发现清单，列固定五栏：位置、发现、判据（引用原则条目）、建议、证据。建议只分两类：**只改写法**（含义不变，可以直接进 PR）和**改含义**（要所有者拍板，进裁决包）。清单出来后换一个评审员做对抗核验，默认怀疑，逐条标维持、修正或推翻——这一步在 mattpocock 那次审计里推翻了我三成的初判，值。第一轴另加一张小表：每条边写快、好、省三格，答「这条边是否多出一个来回、是否逼执行体重读上下文、换来了什么质量」。

**第五步，裁决包与分批落地。** 一份裁决包，按「方法论投票、概念增删、机制改造、部件替换、写作」五组排列，只放「改含义」的条目，让所有者一次拍板。落地顺序固定：先方法论与边界，再概念与分层，再机制与部件，最后写作——因为后面的改动建在前面的结论上。每批一个 PR；约束层的语义变化配契约测试用例并推进版本号；转折进来时路。

**沉淀。** 方法跑通一遍之后，把它抽成 `src/agency/skills/` 下的第二份 Skill（评审），四轴原则做它的判据表，本文第三节做它的流程。这正是参与者正文里「评审是第一个必须专业化的岗位」的第一个实例；abacistopia 装载同一份 Skill 就能用，只是第一轴的对象要从「四个场景」抽象成「这个仓库把边界画在哪」。

## 四、怎么分工（提议，待讨论）

沿用文风复审的分法：每轴一家 harness，Fable 做汇总与对抗核验，所有者裁决。

| 轴 | 需要什么能力 | 建议 |
| --- | --- | --- |
| 方法论调研 | 联网、clone 仓库、读源码 | Codex 或 Fable 子代理 |
| 概念与边界清点 | 机械、全、准 | Kimi（K3 那次做得好） |
| 强制手段与部件矩阵 | 读约束层、查业界 | Grok 或 GLM |
| 写作轴 | 已有尺子 | GPT（语言一支笔的延续） |
| 对抗核验与裁决包 | 全局视角 | Fable |

## 五、等讨论定的几件事

1. **范围**：按第二节的 21 加 3 个文件，还是把 `docs/research/` 各条目也纳入第四轴？
2. **调研深度**：第一轴在十二族的代表工具上抽边界就够，还是重新扫一轮生态（工具在快速涌现）？
3. **先手工还是先 Skill**：先按第三节手工跑一轮再抽 Skill，还是先把方法写成 Skill、用它派活（顺手验证塑形与评审两份 Skill）？我倾向前者，因为方法还没被这个对象检验过。
4. **「改含义」的门**：默认约束层任何语义变化都要过契约测试与版本号，是否有例外？
5. **快好省怎么判**：接受「定性推演加反例」，不做基准测试？我建议接受，基准测试要等有真实 Run 数据。
6. **与文风复审的遗留衔接**：「会改变含义」的归并候选清单直接并入第二轴，还是单独处理？我建议并入。

讨论完这六件，把结论回填到本文第四、五节，改状态为「已拍板」，然后按第三节开工。
