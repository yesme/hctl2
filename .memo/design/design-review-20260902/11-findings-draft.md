# 发现清单（草稿，未经对抗核验）

> 状态：第一轮 1.4 进行中<br>
> 基线：main @ `6850f18`（草案 v0.16.0）<br>
> 去向：经 `12-adversarial.md` 核验后合成 `13-findings.md`

按层逐文件写，读完一个文件立即落笔。位置写「文件 §节名」。判据引用 [`02-checklists.md`](./02-checklists.md) 的编号。类别只有两种：**写法**（含义不变，第三轮直接落）与**含义**（进裁决包）。A5 的「为什么在这」按节而不按句写，写不出的才成为发现。

## 愿景层

### `docs/design/vision.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-01 | §设计原则 第 5、6、10、11、12 条 | 五条原则只剩标题加「细则见设计地图共同规则」，没有原则本身。M3 要求每条原则是完整、自包含、可独立引用的句子；S1 要求断言带判据 | A5、S1 | 每条补成一句完整原则加一句为什么，链接保留。例：「机械动作必须由确定性规则执行：能由规则判定的事（幂等去重、代次比较、状态归约）不交给模型或人临场判断，否则同一输入在不同时刻得到不同结果，凭证就不可信」 | 写法 |
| A-02 | §产品原生核心与架构最小内核 表「持久账本与对账」 | 愿景层出现约束词 `outbox`／回读；路由表规定愿景层只用核心产品词加日常语言 | A2 | 改成日常语言：「先记账再执行，崩溃后能对账」 | 写法 |
| A-03 | §产品原生核心与架构最小内核 末段「外部平台的事件先按模块约束分类：……」 | 一句话里并列四类事件的处理规则，本质是约束层「客户端动作分类」的复述；愿景层只需说结论加链接 | A2、L5 | 缩成一句：「外部平台的事件不直接改变 HCTL 语义，只有过了同一道准入边界的才算；分类见系统边界」 | 写法 |
| A-04 | §四个阶段的心智模型 图与第 2 点 | 第四阶段标「运行」，模块标 `Participant`；梯度说「越靠后越是可替换的资源」。#138 把参与者身份并进第四模块后，第四模块同时持有最不可替换的东西（身份）和最可替换的东西（进程）。第 2 点已补了半句「参与者身份不随它们丢失」，但图与「运行」这个阶段名没有跟上：读者会把 `Participant` 读成「可替换的执行资源」 | A5、M1 | 两个改法供裁决：（甲）阶段名改「参与者与运行」，梯度句改「越靠后，可替换的部分越多」；（乙）保留「运行」，在图下加一句「第四模块的身份层属于上端事实，执行层属于下端资源」。我倾向（乙），改动小 | 含义 |
| A-05 | §一句话定位 与 §为什么需要 HCTL2 | 同一文件里 `Harness` 与 `Coding Harness` 两个名字指一件事；根 README 已定「下称 Harness」 | A7、W2 | 全文统一为 `Harness`，首现处一次对照 | 写法 |
| M-01 | §两种控制制度 | 塑形到施工的边要过两道门：批准施工图、启动 Run。文中给了理由（前者确认图，后者才消耗资源），且 r1 施工时 r2 可继续讨论，是快的正面证据。但两道门是否必须是两次人的动作，文中没说；如果多数场景下两者同时发生，就是 M5 里「过边多一个来回」 | M3、M5 | 留给第一轴对照别家：别家是否也分「批准计划」与「开工」两步。若别家普遍一步，建议约束层允许「批准并开工」一次提交、账本仍记两个事实 | 含义（待 R1 证据） |
| M-02 | §四个阶段的心智模型 | 四个模块对四个阶段是断言，不是推导：五种失败模式推出四个问题，四个问题各配一个模块。「承诺」是不是一个独立阶段（需要自己的场景与专职系统），还是规格里的一张清单，是这一票里最可疑的一格——spec 驱动一族普遍把任务当规格的附属 | M8、M1 | 等 R1 的边界对照：若多数方法论把任务列表当规格附属而非独立阶段，Kanban 作为独立场景要给出比「别家没有」更强的理由（现有理由是契约冻结与完成凭证，别家确实没有）。结论写进裁决包 A 档 | 含义（待 R1 证据） |
| M-03 | §要解决什么，不解决什么 | 「不用自然语言提示替代分支保护、Receipt、权限或规则」「不让每项工作都必须聊天，也不让每项工作都必须先画施工图」——这两句正是第三轴与第一轴的原则，已在愿景里 | — | 维持；第三轴的发现引用这一句当判据 | 维持 |
| A-06 | 全文 | 愿景没有把效率说成尺子。所有者这一轮提出的「快、好、省」三维度，文中分别散在注意力分配原则（快）、证据高于进度（好）、Context 必须可解释（省），但没有一句话把三者立为取舍原则 | A5、A6 | 在 §设计原则 加一条：「取舍看三个尺度：快——有进展、本地完成、不让人干等；好——方案对、实现对；省——不在上下文上浪费 token。三者冲突时先保好，再保快，最后保省」。顺序是我的建议，进裁决包 | 含义 |
| W-01 | §产品原生核心与架构最小内核 | 「project semantic control plane：控制面归用户级，语义范围以 Repo 为界」——英文短语后用冒号接中文解释，读者要猜冒号后是翻译还是补充 | W3、L4 | 写成「项目语义控制面（project semantic control plane）：它归用户级，语义范围以 Repo 为界」 | 写法 |

**每节「为什么在这」**：一句话定位（立场）、为什么需要（问题）、四阶段（心智模型）、目标体验（旅程）、两种控制制度（推进权）、原生核心与最小内核（换掉一切后什么必须留）、三类数据（数据分家）、设计原则（取舍）、解决与不解决（边界）——九节都能复述目的，结构成立。

### `README.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-07 | §产品姿态一句话 | 四个短语对应 Project、Room、Task、Run；第四模块 `Participant` 不在姿态里。姿态讲的是工作的旅程，参与者是干活的人，可以不进；但「施工交给 Run 治理」之后谁干活没说 | A6 | 两个选项供裁决：（甲）不改，姿态只讲工作不讲人；（乙）加半句「由 Participant 执行」。我倾向（甲）：姿态是给新读者的四个词，五个就记不住 | 含义 |
| W-02 | §目标架构 第二段 | 「执行面的聊天、任务、工作流三类系统由外部供应端承担，Agency 是参与者的供给方而不是第五个模块，第一阶段由发布包自带的本地参考实现承担（运行时为 Herdr）」——一句三件事，且「第一阶段」「Herdr」是交付层细节进了首页 | L5、A2 | 拆两句；Herdr 与阶段只留链接不留名字：「Agency 是参与者的供给方，不是第五个模块；第一阶段的实现见交付文档」 | 写法 |
| M-04 | 全文 | 首页按段混层、每段能指回权威，符合路由表对首页的要求 | — | 维持 | 维持 |

## 架构层

### `docs/design/architecture.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-08 | §三个面 表「执行面」、§场景与系统 表「Terminal」备注 | 架构层三处点名 `Herdr` 与「第一阶段」。同一文件的 §场景与系统 开头明说「具体采用哪个实现由交付文档的选型判据决定」，然后同一张表的备注列就把实现名写出来了，自己打自己。Matrix 是协议、算稳定边界，可以留；Herdr 是产品，归交付层 | A2、A5 | 三处改成「本地 Agency 参考实现（运行时选型见交付文档）」，Herdr 只在 delivery.md 出现。其中两处是我 #138 写的 | 写法 |
| A-09 | §模块交接 表 | 表说自己是连接约束总表的「产品语言投影」，但只投了 8 条边里的 4 条。漏掉的三条里有一条是本产品的差异化所在：「完成 Task」——有权的人或绑定契约的 Run 归约器提交同一个完成命令，Task 独立验收后才有完成凭证。架构层读者读完这张表，不知道完成是怎么判的 | M2、A6 | 加一行「完成：人或 Run 归约器 → Task」，交付物写「完成命令 + 验收证据 → 完成凭证」，一句话写「完成不能自述；谁提交都过同一道验收」。合入 ChangeSet 那条并进「执行回程」一句 | 写法 |
| A-10 | §三个面 表「控制面」与全库 | 控制面的现场执行组件在本文叫「现场执行者」，在参与者正文与多处约束里叫「工具箱」，词汇表另有 `hctl2-tool`。#33 把 hctl2-tool 定界为现场执行者时改了名，但没有 sweep | A7、L3 | 二选一全库统一（我建议「现场执行者」，因为它说的是职责而「工具箱」说的是形态），词汇表登记一对一映射 `hctl2-tool` ↔ 现场执行者；按写作指南 0.9 做一次改名 sweep | 写法 |
| M-05 | §避免供应商锁定 末段「可替换分三档承诺」 | 三档（新工作可换、活动执行不热切换、迁移内容另行校验）把「可替换」这条边界的转换条件说得很清楚，是 M3 的正面样本 | M3 | 维持；第一轴写别家对照时引用 | 维持 |
| M-06 | §数据丢了怎么办 | 不可用与永久丢失分开立约，四个场景逐条给降级与重建，目的（不伪造新鲜度）写在节首。快好省里的「快」在这里有直接体现：不依赖新鲜事实的施工继续跑，不整体停摆 | M3、M5 | 维持 | 维持 |
| W-03 | §4×3 归属矩阵 表头 | 三列写成「metadata（控制面账本）」「content（执行面系统）」「artifact（Git 结晶）」，正文却用「治理元数据」「场景内容」「结晶」；同一张表里英文小写词当列名，正文用中文 | W2 | 表头改中文加括注一次：「治理元数据（metadata）」，或全表用英文；同一层里统一 | 写法 |

**每节「为什么在这」**：三个面（部署视角）、场景与系统（谁承载 content）、避免供应商锁定（可替换的边界与承诺）、4×3 矩阵（数据住哪）、模块交接（递什么）、数据丢了（降级与重建）——六节都能复述。

### `docs/design/README.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-11 | 模块表「Participant」行 content 系统列 | 「第一阶段为本地参考实现，运行时 Herdr」——与 A-08 同病，设计地图是架构层索引 | A2 | 改「Agency（派出方）供给的执行体」，实现见交付文档 | 写法 |
| A-12 | 模块表「Project」行 模块拥有列 | 写着「参与者」。#138 之后参与者身份归 Participant 模块，Project 拥有的是名册与角色绑定；一个词让读者以为身份归 Project | A7、A6 | 改「参与者名册与角色绑定」 | 写法 |
| A-13 | §共同规则 末段 | 「CAS、outbox、单写者和适配器恢复等通用机制」——`CAS`、`outbox` 不在架构层允许的六个高频约束词里 | A2 | 改「比较并交换、发件箱、单写者与适配器恢复」或直接说「并发与恢复的通用机制」 | 写法 |
| A-14 | 开头「四个模块对应四个阶段」段 | 与 A-04 同源：梯度句「越靠后它的执行面越是可替换的资源」在这里又出现一次。两处要一起改 | A5 | 随 A-04 的裁决同改 | 含义（随 A-04） |
| M-07 | §对象关系 与 §共同规则 | 「场景与模块是一一对应的主视角，不是强制的调用链。Task 可以没有 Run；Project 可以发起一次 Harness 调用」——这是第一轴 M1 的正面证据：边是语义边，不是流程边 | M1 | 维持；第一轴引用 | 维持 |

### 机械清点里的架构层线索（来自 `10-inventory.md`，待通读逐条确认）

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| W-04 | 全库 | 驼峰拼接名只剩两个活的：`ChangeSet`（76 次，核心产品词）与 `ReviewSubjectRef`（14 次，约束层引用格式）；其余全在来时路里作历史记录。v0.11.1 把 25 个驼峰名改成带空格专名时留下了这两个 | W1 | `ChangeSet` 是核心产品词、Git 世界有 changeset 的通用说法，建议保留并在词汇表注明「唯一保留的驼峰专名」；`ReviewSubjectRef` 改成「评审对象引用」加字段名 `review_subject_ref`。两条都进裁决包 B 档 | 含义 |
| W-05 | 写作指南 L4 与 `docs/design/doc-discipline.md` 第四条 | 初看两条规则打架（指南说词汇表释义即可，旧原则说每文档首现给对照）。读到文档纪律才发现所有者 2026-09-02 已裁：有自然译名的十个词（仓库、看板、参与者、上下文、施工图、裁决、凭证、技能包、工件、备忘）每文档首现给对照，只有释义式说明的词汇表定义一次即可。机械清点第三节按前一类过滤后才是有效结果 | W3 | 维持规则；把清点脚本的「首现无对照」检查限定到那十个词，第三轮升为检查时照此 | 维持（清点口径修正） |
| W-06 | `spec/*.md` | `CAS` 与「比较并交换」在约束层各 8 次与 14 次并用；词汇表允许「字段名和实现机制名中保留 CAS」。要核对的是散文里有没有用 `CAS` | W2 | 通读约束层时逐处看；散文改中文，字段名保留 | 写法（待核） |

### `docs/design/project.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-15 | §为什么存在 末段、§模块拥有什么 | 两处仍把 `participant.md` 叫「横切正文」，并说 Project 拥有「Participant 与角色」。#138 之后参与者是第四模块，Project 拥有的是名册与角色绑定 | A6、A7 | 「横切正文」改「模块正文」；「Participant 与角色」改「参与者名册与角色绑定」。这是我 #138 漏改的 | 写法 |
| M-08 | §关键规则 「普通聊天形成提案；正式变化走带预览的类型化命令」；§Chat Room 场景 「重复且无需临场判断的协作进入 Workflow」 | Room 到 Task、Room 到 Workflow 两条边的跨越条件写得干净：前者是「采纳命令」，后者是「重复且无需临场判断」。M3 的正面样本 | M3 | 维持；第一轴对照时引用 | 维持 |
| M-09 | §关键规则 Request 条 | Request 的应答面升级阶梯（卡片 → 临时讨论空间 → 安全通道 → 终端）写成一条规则，读者要数着层级读。它同时回答了第一轴的「省」：不为一个问题开一间房 | M6、L5 | 拆成两句或一个四项清单，含义不变 | 写法 |
| A-16 | §Chat Room 场景 「Workbench 就位之前」段 | 「Matrix widget/AppService」——Matrix 是协议、算稳定边界，但 widget 与 AppService 是 Matrix 生态里的实现机制名，架构层读者不需要它们 | A2 | 改「将来若聊天客户端能提交显式结构化动作」 | 写法 |

### `docs/design/task.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| M-10 | §模块拥有什么 「Task 是承诺尺度，不是工作项尺度」段 | 这一段是对 M-02（承诺是不是独立阶段）的现成回答：看板上的卡可以随后端自由增删，只有值得冻结验收契约的卡才升格为承诺，契约惰性创建。也就是说 Kanban 这一票投的不是「任务列表是一个阶段」，而是「承诺需要一个有凭证的家」；卡片本身没被 HCTL 治理。这比我在 M-02 里写的担忧要弱得多 | M1、M8 | M-02 的裁决材料里引用本段；若 R1 的对照显示别家的任务列表同样不冻结验收，才构成过早投票 | 维持（M-02 证据） |
| M-11 | §无 Run 的轻量路径 「产品标尺：走完它必须比直接开一个终端干完更轻」 | 这是全库唯一一处把「快」立成可判的标尺的地方。第一轴应把它推广成每条边的通用问法 | M5 | 维持；在 A-06 建议的原则里引用它当例子 | 维持 |
| W-07 | §Kanban 场景 两处 「adapter（适配器）」 | 同一层里 `adapter` 与「适配器」并用（架构层 adapter×5、适配器×12） | W2 | 架构层统一用「适配器」，约束层字段名保留 adapter | 写法 |
| A-17 | §无 Run 的轻量路径 末段、§关键规则 | 「第一阶段一个 Run 绑定至多一个契约版本，同一 Task 最多一个活动 Run」——交付范围的限制写在架构层。这不是错，架构层多处都提「第一阶段」；但它让读者分不清哪些是设计、哪些是暂时没做 | A2 | 架构层保留结论，加「第一阶段的限制见交付文档」；或把「第一阶段」类限制集中到 delivery.md 一张表，架构层只链过去。进裁决包 B 档，因为它决定一类句子的去向 | 含义 |

### `docs/design/run.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| M-12 | §关键规则 「五种重试路径」条 | 每种混用的后果都写了（多建义务、沿用旧验收、复活已撤权执行），是 S1 判据齐全、用词也过关的样本 | M3 | 维持 | 维持 |
| A-18 | §Workflow 场景 与 §关键规则 | 「Dagu 控制台」「工作流引擎自带管理界面」——前者点了产品名，与 A-08 同病 | A2 | 改「工作流引擎的原生管理界面」，Dagu 只在交付文档 | 写法 |
| M-13 | §模块拥有什么 | Workflow 作为独立专职系统（外部引擎）是第二处可疑的投票：愿景说不重造通用工作流引擎，但也没论证为什么需要一个外部引擎而不是控制面自己的小状态机。Run 的语义（义务、席位、尝试、关卡）全在控制面，引擎只报告「令牌到哪了」。如果引擎只剩这点职责，它的复杂度（部署、对账、分歧处理）是否付得起，是第三轴的部件问题，也是第一轴的边界问题 | M8、I4 | 交给 R3 的部件矩阵回答「Dagu 之于 Run 的借用等级与取舍」；结论进裁决包 A 档 | 含义（待 R3 证据） |
| A-19 | §模块拥有什么 「结晶归属以事实为准绳，不随对象所有权走」 | 施工图归 Chat Room 的结晶、批准归 Run 的对象——一句话把目的说清了，A5 的正面样本 | A5 | 维持 | 维持 |

### 跨文件（架构层）

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-20 | 架构层七个文件 | 产品名进架构层的全部位置（grep）：见下方统计。协议名（Matrix）与「远端平台如 GitHub/Linear」作举例可留；Herdr、Dagu、Vikunja、Tuwunel、Cinny、Tauri、Electron 属交付层 | A2 | 一次 sweep：产品名只留 delivery.md、usage.md、research；架构层用系统角色名 | 写法 |
| A-21 | 架构层七个文件 | 约束词进架构层的全部位置（grep）：`outbox`、`CAS`、`digest` 等。路由表只放行六个高频约束词 | A2 | 逐处改中文日常词（发件箱、比较并交换、摘要）或改成引用 | 写法 |

### `docs/design/participant.md`（我 #138 写的，核验重点）

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-23 | §Agency 与执行体 「发布包自带一份本地 Agency 参考实现」段与 Herdr 三类接口表 | 架构层文件里 `Herdr` 出现 14 次，还有一张 Herdr API / 终端连接 / TUI 的接口表。这是交付层内容：哪个产品、它的接口长什么样。架构层只该说「Agency 的运行时由本地参考实现提供，选型见交付文档」。全库 A2 最重的一处，是我合并两份正文时原样搬过来的 | A2、S3 | 接口表搬到 delivery.md（或 spec/participant.md 的外部概念对齐），本文留一句话加链接；`Herdr` 在架构层归零 | 写法 |
| A-24 | §关键规则 | 十九条规则，是两份正文的并集。其中终局结果清单、观测截断、代次失效、三条底线这类是约束层口径，约束附录里都有原文。架构层要「抓大放小」，读者读完十九条记不住哪条是骨架 | A2、S3 | 收成八条左右的骨架（身份稳定、换人不换执行、单写者、恢复先隔离、只有结晶能过去、观测不是结果、窄能力只能提议、Skill 无权），其余改成一句「精确规则见约束附录」。含义不变 | 写法 |
| W-08 | §为什么存在 「人以 human actor 的身份出现在命令、名册和裁决里」 | `human actor` 是约束层沿用的英文短语，架构层散文里直接出现，词汇表没有词条 | W3、L4 | 二选一：词汇表登记「human actor｜有权的人」并在架构层用中文；或架构层写「有权的人」，约束层保留 `human actor`。进裁决包 B 档 | 含义 |
| W-09 | §Terminal 场景 「记作 `participant.tui`」 | 点号小写的接口名与全库命名方式都不同（其余是带空格的英文专名或中文）。它是所有者当场给的标签，含义清楚，但作为唯一的一个这种写法会被读者当成代码标识 | W1、L3 | 保留为代码体标签但在词汇表登记为「参与者终端接口」的标识符；或直接写「参与者的终端接口」。进裁决包 B 档 | 含义 |
| M-14 | §Agency 与执行体 | 「要人 → 报人 → 给端点」三段与「派工直达执行体、Agency 不在派工路径上」把 Run/Project → Participant 这条边的两个对话方分开说清了，是昨天辩论的落点。第一轴引用 | M2、M3 | 维持 | 维持 |
| A-25 | §七件事分层 「上三层归控制面账本…下四层的实物由 Agency 供给」 | 分层归属一句话说清，A5 的正面样本 | A5 | 维持 | 维持 |

### `docs/design/context.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-26 | §为什么存在 「检验标准就三个字：快、省、准」 | Context 有自己的三字尺子；所有者这一轮给全库的是「快、好、省」。两套尺子只差一个字（准 ↔ 好），并存会让读者以为是两回事 | A7、A5 | 随 A-06 一起裁：全库统一用「快、好、省」，Context 一节把「准」解释成「好」在上下文这件事上的具体含义（因果链完整、结论可回源） | 含义（随 A-06） |
| W-10 | §萃取与压缩 等五处 `small-brain` | 自造英文词当术语，正文用「专用小模型（small-brain）」引入后就一直用英文 | W3、W1 | 全文用「专用小模型」，首现括注一次英文；或进词汇表。进裁决包 B 档 | 含义 |
| M-15 | §投喂三档 「少一轮，而不是少一个指针」 | 这是第一轴「省」的最好判据：成本单位是往返轮次，不是 token 个数。第一轴每条边的快好省表都用它当尺子 | M6 | 维持；A-06 的原则里引用 | 维持 |
| I-01 | §萃取与压缩 「这一步必须快，缺省全本地、不花模型 token…只有用户配了专用小模型时才引入模型判定」 | 第三轴（a）的正面样本：能机械做的先机械做，模型判定是可选项且要记账 | I1、I2 | 维持；第三轴引用 | 维持 |
| A-27 | §萃取与压缩 「推理服务的计费与缓存也偏爱这么排，两个理由不打架」 | 实现层理由（推理服务缓存）进了架构层。按纪律它只能是脚注 | A2 | 改成「这个顺序也符合推理服务缓存的习惯」放括号里，或删 | 写法 |

### 跨层：概念数量（A 轴 b 问）

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-22 | `spec/README.md` §核心产品词 与词汇表 | 机械清点：词汇表带中英对照的概念 65 条；核心产品词 23 个（去掉 `Agent` 后），加六个系统角色名、六个高频约束词，一个架构层读者要认 35 个词。愿景第 2 条原则自己说「Repo、Project、Task、Room、Run 是用户核心对象；Obligation、Seat、Attempt 只在需要时展开」，说明词表内部已经有两档，但路由表和词汇分类法把 23 个一律叫核心产品词，愿景层可以全用 | A1、A3、A2 | 把核心产品词分两档写进词汇分类法与路由表：**用户核心词**（Repo、Project、Room、Task、Run、Participant、Request、Workflow，八个，愿景层与首页只用这一档加日常语言）与**治理词**（Memo、Artifact、Context、Skill、Obligation、Seat、Attempt、Gate、Verdict、Receipt、ChangeSet、Evidence、Kanban、Terminal、Workbench，架构层起可用）。分档不改任何对象的定义，只改哪层能用哪个词。进裁决包 A 档：它决定愿景层要不要去掉 Obligation/Seat/Attempt 那张表 | 含义 |

## 约束层

### `docs/design/spec/README.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-31 | §词汇分类法 「实现标识符另建『语义名 ↔ 标识符』对照表，以便回溯约束」 | 承诺的对照表只有词汇表里「系统组件与常用技术词」那张（owner、fence、CAS 等九个词），字段级的（`attempt_generation` ↔ 尝试代次、`state_version` ↔ 状态版本……）没有。约束层正文里代码体字段名有几十个 | A5、L12 | 二选一：在词汇表补一张字段名对照表（机械可生成：正文里全部反引号小写下划线标识符），或把这句承诺改成「字段名以代码体出现，含义随句可读，不另建表」。进裁决包 B 档 | 含义 |
| A-32 | §六族规则、§三类数据 | 族语义与三条法各定义一次、其余引用，是 A4 与 T4 的正面样本；三条法每条先说目的再说规则 | A4、A5 | 维持 | 维持 |
| M-21 | §三类数据 「能承载不等于能裁决」「命令走 HCTL，记录落平台」 | 这两条法就是四条边共同的跨越规则：content 侧的任何事实都要经命令归一才过边。第一轴的边界对照以此为基线 | M3 | 维持 | 维持 |

### `docs/design/spec/connections.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| M-16 | §连接约束总表 | 八条边每条都有交付物、准入与提交、恢复依据三格；第一轴问的「边界处交付物与转换条件定义得清不清楚」在这张表上是清楚的。缺的是「失败处理」一格，它被拆到 §失败与恢复 那张按失败点组织的表里，读者要在两张表之间对 | M2、M3 | 总表加一列「失败见」指向失败表的行号或失败点名；或在失败表每行标出影响的边。含义不变 | 写法 |
| M-17 | §Project / Task → Run 「批准 Workflow 只确认施工图；『启动 Run』命令才建立自动施工连接」 | 与 M-01 同一条边：两道门各有账本事实。这里没写两道门能否一次提交 | M5 | 随 M-01 裁决；若允许一次提交，这里加一句「可以在一次预览里同时批准并启动，账本仍记两个事实」 | 含义（随 M-01） |
| M-22 | §跨模块 Request 回路 「普通 Room 回复不能解决 Request，也不能直接完成引擎节点」 | 决定判定权的机械门。第三轴（a）的正面样本：不靠叮嘱模型，靠入口 | I1、M3 | 维持 | 维持 |
| A-28 | §连接模型 首段「连接不是一份可独立漂移的共享状态，也不需要 `Handoff` 聚合」 | 这是「为什么不那样设计」的论证，出现在约束层正文里；L11 要求理由段就近标注 | A2、L11 | 前面加 `> 理由：`，或搬到 decision-history 只留结论 | 写法 |
| W-12 | §连接约束总表 「human scene」「human Kanban」「task-bound Run reducer」「human provenance」 | 同一张中文表里夹着英文短语 | W2、L4 | 改「人在场景中的动作」「人在看板上」「绑定 Task 的 Run 归约器」「人的来源证明」 | 写法 |
| W-13 | §失败与恢复 「CAS 拒绝」与正文「比较并交换必须拒绝」 | 同一文件里同一机制两种写法 | W2、L3 | 表里也写「比较并交换拒绝」；`CAS` 只留字段名 | 写法 |
| I-02 | §失败与恢复 表 | 十一个失败点每个都对应一个机械结果（拒绝、回读、进入丢失、标分歧），没有一处靠人或模型「注意」。第三轴的正面样本 | I1 | 维持 | 维持 |
| M-23 | §版本、权限与替代 「权限只能逐级缩小」 | 四条边上权限单向收窄，是「好」的结构保证：下游拿不到上游没给的东西 | M7 | 维持 | 维持 |

### 八条边的快好省（第一轴，按连接约束总表）

| 边 | 交付物 | 谁判 | 快：多不多一个来回 | 省：要不要重读上下文 | 好：换来了什么 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| Project → Task | 契约版本 + 来源引用 | 人（采纳命令） | 一次动作 | 不：来源冻结为引用 | 承诺可核验、有版本 | 维持 |
| Project / Task → Run | 施工清单 | 人（批准图 + 启动） | **两次动作**，是否必须两次待定（M-01） | 不 | 图与开工分开，讨论不阻塞施工 | 待裁 |
| Project → Participant | 执行规格 | 人（Trigger Preview） | 一次动作，预览即提交 | 不：三档投喂，必用内联、可达指针 | 身份链冻结、可回溯 | 维持 |
| Run → Participant | 执行规格 | 代码（按清单派发） | 零来回 | 不：接力只经结晶 | 席位与尝试身份稳定 | 维持 |
| Participant → Project / Run | 结果提案 + 版本 / 证据 | 代码校验，Run 另有评审席位 | 零来回（提案即入） | 不 | 自述不能变事实 | 维持 |
| 人 / Run 归约器 → Participant（合入） | 合入命令 + 版本引用 | 代码（回读签凭证） | 零来回 | 不 | 模型说合了不算 | 维持 |
| 人 / Run 归约器 → Task（完成） | 完成命令 + 验收证据 | 代码（独立验收），人或归约器发起 | 无 Run 路径一次人的动作；有 Run 路径零来回 | 不 | 完成不能自述——差异化所在 | 维持 |
| Task / Run / Participant → Project | 事件 | 无需判 | 零 | 不：低噪声投影 | 一个 Project 看全局 | 维持 |

八条边里只有一条（施工的两道门）在「快」上有待裁的问题，其余七条的来回数已经是能做到的最少。第一轴剩下的问题不在边的定义，而在**边的数量**：Kanban（M-02）与 Workflow 引擎（M-13）这两个专职系统是否必要，等 R1、R3 的证据。

### 第三轴（a）对约束层的总判断

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| I-03 | `spec/*.md` 全部 225 句规范句（清点表：113 句含机制词、112 句不含） | 逐条看过 112 句无机制词的句子：它们几乎全是「类型化拒绝」「必须冻结 X」「不得静默 Y」这类由 control 代码强制的规则，机制词在同一节的别处已定义，句子本身只是不重复。真正可能落到「叮嘱模型」上的只有针对 Harness 与模型行为的几句，而它们的强制手段是三条底线（不给凭据、只有提案通道、隔离工作树），也是机械的。约束层没有一条规则的成立依赖模型听话 | I1、I2 | 维持。可选的写法改进：主语是外部方（Agency、适配器、Harness）的规范句，句末加「由 X 的契约测试判定」，让读者一眼看到强制点 | 维持 |
| I-04 | 写作指南与文档纪律（过程规则），`src/build/docs/` 现有五项检查 | 靠散文管 LLM 的规则不在约束层，在写作规则里：路由表的允许词汇（哪层能用哪些词）、驼峰名、同层同表中英夹杂、「需要」当规范词、产品名越层——这些全部可以机械检查，现在只有链接、版本戳、退役词、memo 基线、禁令密度五项是脚本。这一轮的机械清点脚本已经把前三项做出来了 | I2 | 第三轮把清点脚本升为检查：按路由表的词表做「越层词」检查（A-20、A-21 的机械版）；驼峰名检查（允许表：`ChangeSet`、外部产品名）；同层同表中英检查；约束层「需要」检查。每项一个 `sh_test` 进对应 profile。进裁决包 B 档，因为它决定写作规则的强制方式 | 含义 |

### `docs/design/spec/system.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-33 | §组件 表「Herdr」行；§端点与输入的信任边界 第 1、3、4 条；§全系统事实权威地图「Harness 进程」行；§启动与恢复 第 1、3、4 步 | 约束层把 `Herdr` 当约束的主语写了十余处（「Herdr 适配代码必须…」「Herdr 管理/API 端点只能绑定回环」）。路由表对约束层的验收是「读完能实现内核或写一个适配器，不需要知道选了哪个产品」；产品名该只出现在外部概念对齐表与交付文档。同病的还有 `spec/participant.md` 的运行时与观测一节 | A2、T6 | 主语改成角色：「Agency 适配代码」「Agency 端点」「Agency 运行时」；对齐表里保留「Agency 运行时 ↔ Herdr」一行；`Herdr` 在约束层正文归零。含义不变，因为约束本来就是对角色立的 | 写法 |
| A-34 | §安全边界 第 2 条 | 约束里写了 Tauri 2 的 capability/permission/scope 与 Electron 的 `nodeIntegration=false`、`contextIsolation=true` 等配置项。约束是「桌面壳只暴露具名类型化命令、最小权限面、禁远程脚本」；配置项是选型后的实现 | A2、A3 | 约束层只留三句约束；Tauri/Electron 的具体配置搬到 delivery.md 的桌面壳一节或研究条目 | 写法 |
| A-35 | §单写者 「锁路径与推进机制是实现细节，不构成对外约束」；§控制面自己的存储 「路径与布局是实现选择，三类存储的职责边界才是约束」 | 两处把「什么是约束、什么是实现」当场分开说了，A3 的正面样本。约束层里凡出现路径与字段名的地方都该这么标 | A3 | 维持；作为约束层写实现细节时的范式 | 维持 |
| I-05 | §代次家族 表与四条推导禁令；§命令与跨服务正确性 「同一个 SQLite 事务」「RFC 8785 JCS 的 SHA-256」 | 六个代次各有权威定义与推进时机，摘要算法钉到标准，事务边界唯一——机制全是代码能判的，没有一句靠人记住。第三轴（a）的正面样本，也是 I5「胶水还是轮子」要问的地方：这些机制（outbox、租约、代次、JCS 摘要、SQLite 单写者）是我们自己写的通用机制，业界有没有现成的库 | I1、I5 | 维持；I5 的问题交 R3 部件矩阵的「自研胶水」一节回答 | 维持（I5 待 R3） |
| W-14 | §单写者 「以 CAS 推进」、§控制面自己的存储 「expected-version CAS」 | 散文里的 `CAS`，与本文件别处「比较并交换」并用 | W2 | 散文统一「比较并交换」，字段名保留 | 写法 |
| W-15 | 全库约十处 「ground truth（事实源头）」 | 每次出现都带括注，L4 要求首现释义一次、之后用选定形式 | L4、W2 | 全库选一个：约束层用「事实源头」，首现括注一次英文 | 写法 |

### `docs/design/spec/run.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-36 | §写入约束 「适配器才推动 Dagu」「Dagu 原生 UI/API」；§Workflow 与 Run 授权 「生成并验证 Dagu YAML」「Dagu `human.task` 仅作被动检查点」；§从节点到结果 第 5 步；`spec/task.md` §契约与来源 「Vikunja webhook 没有独立投递 ID 时」 | 与 A-33 同病：产品名当约束主语。其中「HCTL Profile 第一阶段规则三组」（允许的图结构、编译器拒绝的副作用、dynamic fork 限制）是编译器对某个引擎的契约，属于适配器规格，不属于 Run 模块约束 | A2、T6 | 主语改「workflow engine」「引擎适配器」「任务后端 webhook」；Profile 三组规则搬到交付文档的引擎适配一节或对齐表下的「第一阶段编译器契约」小节；Dagu、Vikunja 在约束层正文归零、对齐表保留 | 写法 |
| A-37 | §写入约束 Run 状态表；§从节点到结果 Attempt 状态表 | 两张状态表没有超时行。写作指南 T3 要求每个非终结状态定义超时语义。正文说「启动中、暂停中和取消中都必须能通过取消、失败或替代进入终态」，也就是靠人取消或替代，没有自动超时；Attempt 的超时靠 Obligation 截止时间与 Request 截止策略，表里没写 | T3、A5 | 二选一：在两张表各加一列「超时」，写明「无自动超时，由 X 结束」或引用截止时间来源；或在表下加一句总纲。这是把隐含设计写明，进裁决包 B 档 | 含义 |
| A-38 | §Request、重试与 Gate 「`known/unknown`」「`accepted/rejected/changes_requested`」「quorum-unreachable」「unsupported」；§Run → Task 「`completion_pending(run_ref)`」；`spec/task.md` §契约与来源 表「backend_authoritative / hctl_authoritative / linked_readonly」 | 词汇分类法规定「状态值使用中文语义名」，实现标识符另建对照表。这些状态值与模式名全是英文标识符直接进正文 | A3、W2 | 正文用中文语义名（已知 / 未知、接受 / 驳回 / 要求修改、票数不可达、不支持、待完成、后端权威 / 控制面权威 / 只读关联），首现括注标识符；标识符进 A-31 建议的对照表。属于一次 sweep | 写法 |
| M-24 | §Request、重试与 Gate 五种重试路径表 | 每条路径写清「产生的新身份」与「不变的身份」，是第一轴 M2/M3 在 Run 内部的最好样本 | M2、M3 | 维持 | 维持 |
| I-06 | §写入约束 完成谓词四条 「任何一项未知都不得完成 Run」 | 完成的判定是四个可核对的账本谓词，模型自述与引擎进度都不能补足 | I1 | 维持 | 维持 |

### `docs/design/spec/task.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-39 | §启动 Run 的前置与排序令牌 第三段 | 「Start、Complete、Adopt 与跨来源冲突判断若要求 task backend 的当前 placement、remote revision、source head 或完整 cursor……cursor 有 gap 或 readback 超出冻结 freshness 上限」——一段里英文名词十来个，中文只剩连接词。这一段是文风复审漏掉的 | W2、L4、L7 | 重写成中文：「启动、完成、采纳与跨来源冲突判断若依赖任务后端的当前位置、远端版本、来源头或完整游标，必须先完成当前回读；后端不可用、游标有缺口或回读超出冻结的新鲜度上限时，类型化拒绝」 | 写法 |
| M-25 | §写入约束 「Task 终结只有两个获准 actor 来源」段与 Receipt 逐项绑定 | 完成这条边的交付物（命令 + 逐项证据）与判据（独立验收）写得最完整，「不能用一个总括的『测试通过』替代逐项绑定」是第三轴（a）的直接体现 | M2、I1 | 维持 | 维持 |
| A-40 | §契约与来源 「Task Revision 契约按需创建（契约惰性）」 | 契约惰性是 M-02 的关键证据：Kanban 上的卡不是治理对象，只有升格的才是。这句话在架构层 task.md 也有，两层一致 | A5 | 维持 | 维持 |

### `docs/design/spec/project.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| W-16 | §对象 表「Chat 端口绑定」及全库约 20 处 | 术语内中英混排（写作指南 L4 明令禁止的形态），而且它是一个对象名 | L4、W1 | 改「聊天端口绑定」（词汇表 Binding 族已有此中文对照）；按 0.9 协议 sweep | 写法 |
| A-41 | §Context、Memo 与 Artifact 「small-brain」四处 | 与 W-10 同一个词；约束层用它当模型引用的名字 | W3 | 随 W-10 裁决 | 含义（随 W-10） |
| M-26 | §Room 与消息 Scoped Room 「创建时必须冻结 parent Room、讨论目标、完成条件和回填动作」；§Request 「普通 Room 回复不能解决 Request」 | 讨论空间的进出条件与决定的落锤都是机械门。第一轴引用 | M3 | 维持 | 维持 |
| A-42 | §对象 表 | 十一个对象里 Context Manifest / Context Bundle 是「票据与规格」（词汇分类法自己的分类），却与领域对象同列一表 | A1、A3 | 表里分两段或加一列「类别」，让读者看出哪些是对象、哪些是票据。含义不变 | 写法 |

## 交付与参考层

### `docs/design/delivery.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| A-43 | §选型判据 首句「实现名只出现在本文与实现证据，设计层正文只用系统角色名」 | 仓库自己立的规则，正是 A-08、A-20、A-23、A-33、A-36 所违反的那条。规则在，执行没跟上 | A2 | 维持规则；上述发现按它清零。第三轮把它做成机械检查（I-04） | 维持 |
| I-07 | §选型判据 与 `docs/research/README.md` 五种复用决策用语 | 所有者这一轮给的借用偏好是四级：跨平台二进制、SDK、复制代码、借鉴想法。仓库现有的五种复用决策（采用为依赖、适配协议、移植有边界的组件、仅参考行为、暂缓）里没有「拿 SDK 自己开发」这一级，也没有写出偏好顺序 | I4 | 在选型判据加一条「借用偏好顺序」，并把五种复用决策与四级偏好做一张对照（采用为依赖 ≈ 二进制；适配协议与 SDK 分别对应协议形状与开发库；移植组件 ≈ 复制代码；仅参考行为 ≈ 借鉴想法）。进裁决包 B 档 | 含义 |
| A-44 | §明确不做 | 「第一阶段不做」的清单集中在这里，是 A-17 建议的落点：架构层散落的「第一阶段」限制可以都指到这一节 | A2 | 随 A-17 裁决 | 含义（随 A-17） |
| M-27 | §纵向切片 A、B | 两条切片把八条边逐一走了一遍，每步写清交付物与判据，是第一轴检查表 M2/M3 的验收样例 | M2 | 维持 | 维持 |

### `docs/design/contract-tests.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| W-17 | 全文 | 全库中英夹杂最重的文件：`fail closed`、`drift`、`stage or health`、`lane`、`terminal intent`、`doer`、`provenance`、`recipient`、`placement`、`regate`、`reviewer Seat`、`Start/Stop/Retry/Reschedule/Approve/Reject/Edit/Rename/Delete`。交付层允许约束词，但术语内混排与整句英文名词仍受 L4 约束 | W2、L4 | 单独一次语言 sweep：有中文对照的（失败即关闭、漂移、阶段或健康、泳道、终态命令、操作者、来源、接收者、位置、重新评审、评审席位）改中文，产品动作名保留代码体 | 写法 |
| A-45 | `CT-PROJECT` | 约束层写了「普通 Room 回复不能解决 Request」（决定不能自述的机械门），CT 里有「普通消息不能成为命令」与「模型建议不能创建 Invocation」，但没有针对 Request 解决路径的失败用例 | A6、I1 | 加一条：「普通 Room 回复、模型消息或 Result Proposal 提交『解决 Request』时拒绝；只有获准动作解决」。约束已存在，只补用例 | 写法 |
| I-08 | 全文 | 十族全部是失败用例，每条都能写成机械测试；第三轴（a）在验证层的正面样本 | I1 | 维持 | 维持 |

### `docs/design/doc-discipline.md`、`WRITING-GUIDE.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| W-20 | `WRITING-GUIDE.md` L3、L4 | 所有者这一轮的两条写作规则——避免驼峰拼接的自造名、同一层同一表内中英不混——指南里没有明文（v0.11.1 做过一次驼峰名收敛，没立规则；L4 只禁术语内混排） | W1、W2 | L3 加一句「不造驼峰拼接名；唯一保留的是 `ChangeSet`（随 W-04 裁决）」；L4 加一句「同一层、同一张表或清单内同一概念只用一种写法」。两条同时进文档检查（I-04）。进裁决包 B 档 | 含义 |
| A-46 | `doc-discipline.md` 第四条 | 首现对照的两类词由所有者裁定并写明，解决了 W-05；这一条是机械检查的白名单来源 | W3 | 维持 | 维持 |

### `docs/design/references/decision-history.md`、`docs/usage.md`

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| W-18 | 来时路 §36 「四个对仗 Scene」 | 「Scene」是英文，全库的产品词是「场景」 | W2 | 改「四个对仗的场景」 | 写法 |
| W-19 | `docs/usage.md` 多处「Chatroom」 | 场景名是 `Chat Room`（两个词），使用说明写成 `Chatroom` | L3 | 统一为 `Chat Room`；`hctl2-services` 的子命令名若是 `chatroom` 保留代码体 | 写法 |
| A-47 | 来时路全文 | 驼峰名与历史概念名全部集中在这里，是允许的（历史记录）；退役词检查已把它排除 | W1 | 维持 | 维持 |

---

**1.4 通读完成。** 24 个文件全部读过（词汇表按行、来时路按当前设计一节与标题）。待 R1/R3 证据的条目：M-01、M-02、M-13、I-05（I5 部分）、I-07。

## R3 部件矩阵回填（第三轴 b）

来源：[`docs/research/component-matrix-20260902.md`](../../../docs/research/component-matrix-20260902.md)（40 行，全部钉版本）。

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| I-05（回填） | `spec/system.md` 表 D 十三项通用机制 | R3 逐项对照 durable execution 引擎（Restate、DBOS、Temporal）后结论：没有一项能整块换成二进制或 SDK——Restate 服务端是 BSL 1.1、DBOS 无 Rust、Temporal 要独立服务加数据库。语义部分维持自研，但六项应由「可能手写」改为「拿 SDK 自己开发」：JCS 摘要（`serde_jcs`）、备份快照（SQLite Online Backup API）、全文索引（FTS5）、现场锁（`fd-lock`）、密钥（`keyring`）、provider 客户端从 schema 生成 | I4、I5 | 交付文档「技术基线」加一句：这六项的 SDK 选定；契约测试为 JCS 钉 RFC 8785 附录向量。进裁决包 B 档（部件替换） | 含义 |
| I-09 | `src/build/tools/reindeer`（构建工具） | Reindeer 启动器用 Cargo 从源码编译，上游每个 release 带八个官方二进制；这是「业界有二进制、我们在自己编」 | I4 | 换成 DotSlash 官方二进制，删启动器，去掉对宿主 Cargo 的隐含依赖。进裁决包 B 档 | 含义 |
| I-10 | `src/packaging/dependencies/hctl2-services` 与 `runtime.sh` 系列（约 675 行 shell） | 用 PID 文件、`kill -0`、`sleep` 轮询自己写了一个进程监督器，而开发侧已经拍板用 Process Compose 管本机 cache。R3 标为方向问题：control 经 Process Compose API 托管四个执行面服务，代价是用户包多 15–16 MB | I4、I5 | 候选：P2 首次消费前做限时验证再定。**R3 建议所有者拍板**，故放 B 档但请务必看这一行 | 含义 |
| I-11 | `hctl2-control` 的六个 provider 适配客户端 | 预防性发现：Dagu 钉定 commit 带 OpenAPI 3.0.0、Herdr 可导出 schema、GitHub 有 `octocrab`、Linear 有 `graphql_client`、Matrix 有 `ruma`；Vikunja 是 Swagger 2.0 要先转 3.0。不该手写 HTTP 客户端 | I4 | 交付文档技术基线写明「provider 客户端从 schema 生成或用类型库，不手写」。进裁决包 B 档 | 含义 |
| M-13（回填） | `run.md` §模块拥有什么 | 部件侧：R3 把 Dagu 与另两个候选对照后维持，且认为控制面自己写状态机不如借引擎。边界侧（Workflow 作为独立阶段是否别家都有）仍待 R1 | M8、I4 | 部件侧维持；边界侧待 R1 后合并结论 | 含义（待 R1） |
| I-07（回填） | 五种复用决策用语 | R3 按四级尺子给 40 行分类：二进制 17、SDK 12、借鉴想法 4、自研 7——SDK 是第二大类，而现有用语没有这一级 | I4 | 维持 I-07 的建议，证据更强 | 含义 |
| I-12 | `hctl2-control` 十八条、`hctl2-tool` 五项（R3 第二节） | 「自研是不是胶水」逐条问过：hctl2-tool 五项全是 git 命令的胶水；hctl2-control 十八条里九条胶水、两条薄自研、一条可改借用、一条待定、五条非胶水——五条非胶水正好落在愿景层「最小内核」的五行上。也就是说自研的部分与愿景说「换掉一切后必须留下的」一致 | I5、A5 | 维持；这是第三轴给愿景层「最小内核」表的一次证明 | 维持 |

## R2 下半年扫尾回填（第一轴与第三轴）

来源：[`docs/research/methodology-sweep-2026h2-20260902.md`](../../../docs/research/methodology-sweep-2026h2-20260902.md)。没有第十三族；两处地图变化：软件工厂成为「多 agent 拓扑家族」之外的新亚种（代码持有控制面，agent 是无状态有界节点，验收靠代码事后核对 agent 的声明）；第十二族由「TDD/eval 驱动（弱）」改名扩容为「机械守卫与验收账本」（unlazy、stop-that-shit、old-coder、procoder），两个月四个同机制样本。

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| M-28 | 四场景边界 vs 新工具的四处不同画法 | （一）**分诊是独立边界**：Foreman 的 classifier 给外来事项三态出口（可执行 / 需澄清 / 不做）。HCTL2 没有分诊这条边：外来 issue 进 Room 后由人塑形成 Task，或直接是无契约卡。（二）**评审是流水线工位、独立性靠换厂商模型**而不是确定性归约——与 HCTL2「独立性按 known/unknown 记录、作者不占席位」的信任来源不同。（三）**权限天花板由制品能力定义**（草稿 PR 合并不了，所以无人值守可放行），而不是状态机。（四）**边界可以画在一次工具调用上**（PreToolUse 钩子按声明约束返回 deny） | M4、M8 | （一）不加边：分诊在 HCTL2 是 Room 里的塑形动作，Request 的三态（解决 / 过期 / 取消）与 Scoped Room 的显式结案已覆盖「需澄清 / 不做」；但值得在 project.md 写一句「外来事项的分诊发生在 Room，不另立阶段」。（二）写成显式否决理由进 run.md 的评审独立性一段：换厂商不等于独立，独立要能机械认证。（三）（四）是第三轴的借鉴想法，见 I-13。进裁决包 A 档（因为它回答「边界有没有遗漏」这一问） | 含义 |
| I-13 | 本地 Agency 参考实现（`src/agency/`）与 `spec/participant.md` 三条底线 | 第十二族的机制是「harness 侧机械拒绝 + 可执行验收账本」：stop-that-shit 用五家 harness 的 PreToolUse 钩子按声明约束 deny，unlazy 用机器校验的 CHECK/EXPECT/EVIDENCE 账本判「勾了框但证据 pending 仍算未完成」。HCTL2 的三条底线靠不给凭据与只有提案通道，是账本侧的门；harness 侧的钩子是另一层机械门，两层不冲突 | I2、I4 | 本地 Agency 参考实现除技能目录外，可以带一份 harness 钩子（PreToolUse deny 越界写、Stop 钩子核对终局结果清单），作为执行加固的一种可声明项。适配协议候选：`ControlEvent v1`；有边界移植候选：unlazy `gate-check.mjs`。进裁决包 B 档（部件与机制） | 含义 |
| M-29 | 第十二族改名 | 「机械守卫与验收账本」正是 HCTL2 完成判定权的同路人，且全部在 harness 侧或提交侧，没有一家有不可变 Task。方法论生态审计的第十二族一行与横评应更新 | M4 | 第三轮把 `methodology-landscape-20260824.md` 文末追加复核记录：第十二族改名、补四样本、软件工厂亚种。研究层只追加 | 写法（研究层追加） |
| A-48 | 相邻元层：better-harness 的证据状态词表 | Observed / Missing / Unobserved / Not applicable 四态，加「配置了不等于用过了」。HCTL2 的 known/unknown 是两态；四态区分了「没观测到」与「不适用」 | A3、I1 | 评估是否把 known/unknown 扩成四态：至少把「不适用」从 unknown 里分出来（例如没有 Terminal 的执行不该记 unknown）。进裁决包 B 档 | 含义 |

## R1 边界抽取回填（第一轴）

来源：[`docs/research/methodology-boundaries-20260902.md`](../../../docs/research/methodology-boundaries-20260902.md)（19 家逐边抽表，对照 HCTL2 八条连接，三类归纳逐条裁决）。R1 的总判断：**别家的边共同的形状是「门后没有冻结」**——阶段产物里不可变的只有 git commit 与 issue 号，其余全是可就地改写的文件；HCTL2 八条边的交付物全部带摘要或代次。这不是「我们更严」，是递的东西不同：别家递当前状态的副本，我们递对精确版本的引用。第二个总判断：别家的散文门集中在计划段、机械门集中在集成段；HCTL2 反过来——计划段只有一道门（采纳契约）但它是机械准入，集成段两道（合入、完成）机械且逐项绑定证据。19 家里没有一家这么投。

| 编号 | 位置 | 发现 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| M-01（回填） | Project / Task → Run 两道门 | R1：批准与启动分两步不是我们独有（GSD、Kiro、spec-kit 都分），四家记了 baseline sha 但没长出预算、权限、Gate。「多一个来回」的担忧不成立：别家也是两次动作 | M4、M5 | **维持两道门**；不改约束。可选写法：连接约束加一句「同一次预览可同时批准并启动，账本仍记两个事实」——只是产品交互，不动边 | 维持（原「含义」降为可选写法） |
| M-02（回填） | 承诺是不是独立阶段 | R1：19 家的任务列表全是可就地改写的文件，没有一家有不可变 Task Revision；用户在替我们要这条边——Kiro #5019「Treat Completed Task as Immutable」被以无跟进关闭，#6826 说 `tasks.md`「drifting from actual implementation state」。加上 task.md 的契约惰性（卡片自由、只有升格的才治理），Kanban 这一票不是过早，是别家缺的 | M8、M1 | **维持**；把 Kiro 两个 issue 写进 task.md 的「为什么存在」当反例证据（写法） | 维持 |
| M-13（回填） | Workflow 作为独立专职系统 | R1：需要持久状态机被别家验证（spec-kit、Taskmaster 都长出了），需要外部引擎没有——是交付选型问题不是边界问题。R3：Dagu 与两个候选对照后维持。合起来：边（Run/Workflow 阶段）成立，引擎是借用等级的问题 | M8、I4 | **维持**边；引擎选型维持 Dagu。愿景「不重新实现通用工作流引擎」与之一致 | 维持 |
| M-30 | Workflow Profile 的节点类型 | 别家有、我们没有的六条边里唯一要改写的：**等待外部机械事实的节点**。beads 把 gate 做成一等 issue 五型（human / timer / gh:run / gh:pr / bead），vibe-kanban 用 PR 事实驱动看板，Gas Town 用 merge proof 关单。HCTL2 的 Profile 只有 timer wait 与被动检查点；要在 Run 里「等 CI 绿」只能派一个 Participant 去查，把机械事实伪装成执行体提案 | M4、A6、I1 | 补一种 `executor = tool` 的 Obligation：由工具箱或适配器回读外部事实（CI 状态、PR 状态、外部系统状态）并产出 Evidence，**不占席位、不投票**；外部不可读时类型化拒绝（对照 beads 在 gh 不可用时放行的反例）。不新增对象，只扩 Obligation 的执行者种类。进裁决包 A 档：它回答「边界有没有遗漏」 | 含义 |
| M-31 | `spec/run.md` Verdict `changes_requested` 的去向 | BMAD 用 `intent_gap` / `bad_spec` 让评审者按根因把回环分流到冻结块内外；HCTL2 有语义返工与替代执行两条路，但**谁判走哪条**没写 | M3 | Verdict 的 `changes_requested` 加一个「分歧落点」字段：施工侧（返工）还是契约侧（要改验收约束 → 替代执行或新 Task Revision）；由评审席位填、归约器据此走边。不新增对象。进裁决包 B 档 | 含义 |
| M-32 | `spec/task.md` 验收约束的形状 | GSD `must_haves` 把验收项分成真值 / 工件 / 连线三类，核验分 exists → substantive → wired 三级，前两级代码判、后一级模型判；HCTL2 说了 Receipt 绑什么，没说一条验收项长什么样 | M2、I1 | Task Revision 的验收约束采用三类，每项声明校验等级；Receipt 逐项绑定时按等级记「谁判的」。不新增对象。进裁决包 B 档 | 含义 |
| M-33 | Evidence 的生产者 | tdd-guard 的 `test.json` 由 reporter 在测试进程内写、不经模型转述；HCTL2 的 Evidence 没有生产者字段区分「工具箱直接产生」与「harness 事件转述」 | I1、M3 | Evidence 加生产者字段（工具箱 / 适配器 / harness 转述），验收策略可要求某类证据必须是前两者。不新增对象。进裁决包 B 档 | 含义 |
| M-34 | `methodology-landscape-20260824.md` 横评两处校准 | R1：Gas Town 的「机械关单」只覆盖关单那一步，合并决策是 Claude 按清单手打 `git merge`，Go 合并管道在 HEAD 无调用方；Witness 是 restart-first 不是 reset-first | — | 研究层文末追加复核记录，两处校准；与 M-29 一并 | 写法（研究层追加） |
| M-35 | 九条只有 HCTL2 有的边 | R1 逐条问「市场未收敛、我们先钉死？」：九条全部能在别家找到 prompt 写的同向碎片、自己长出的半成品或用户要它的事故记录；没有一条过早 | M8 | 维持；第一轴对「过早投票」的总结论：**没有** | 维持 |
| M-36 | 「谁判完成」总账 | 15 家有任务完成这条边，14 家让模型在主路径或侧门自标；全场由代码判且输入不由施工模型写的只有四处，且都是「合并即完成」或「文件齐即完成」，没有一处按验收约束逐项校验 | M3 | 维持；这是 HCTL2 差异化的最硬证据，愿景层「为什么需要」可引用 | 维持 |
