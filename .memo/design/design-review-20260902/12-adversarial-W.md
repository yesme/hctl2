# 对抗核验 · W 轴（写作风格）

> 状态：已核验<br>
> 基线：分支 claude/review-r1 @ f8dcdf8<br>
> 去向：合成 13-findings.md

核验方法：默认怀疑，逐条去推翻。判据只用四样东西——四轴原则第四条、写作指南（L3、L4、L7、S1、S2、0.4 路由表）、`doc-discipline.md` 第四条、约束层总则的词汇规定；证据只用两样——文档原文行号，和我自己重跑的 grep／awk 扫描（排除来时路与代码块）。凡是主评审员给了数字的，我都重数了一遍。

草稿编号从 W-10 直接跳到 W-12，W-11 是空号，不是漏核；合成 13 时重排即可。

## 逐条结论

| 编号 | 结论 | 理由与原文引用 | 修正后的表述（若修正） |
| --- | --- | --- | --- |
| W-01 | 修正 | `vision.md:106` 原文是「……是一个**随用户走、按仓库划分语义范围的项目语义控制面**（project semantic control plane：控制面归用户级，语义范围以 Repo 为界）」。主评审员的引文把前面的加粗中文短语截掉了——首现对照其实已按「中文（英文）」形态给出，读者不必猜冒号后面是不是翻译。真正的毛病是括注里冒号后那半句在复述加粗短语（「随用户走」＝「控制面归用户级」，「按仓库划分语义范围」＝「语义范围以 Repo 为界」），属 L5 表里「也就是说」那一类填充。 | 删掉复述半句：「……项目语义控制面**（project semantic control plane）。这才是产品原生核心……」。类别仍是写法。 |
| W-02 | 修正 | 一句三件事成立（分号连三个分句），L5 拆句维持。但「Herdr 是交付层细节进了首页」不成立：`architecture.md` §三个面 表执行面一格与 §场景与系统 表 Terminal 一行都写「运行时为 Herdr」，词汇表 Agency 行也写「运行时用 Herdr」——Herdr 已是架构层与参考层在用的名字。路由表对首页的要求是「按段混层……每段必须能指回本层权威」，这一段末尾指回了交付文档与三面架构，合规。Herdr 该不该在架构层露名是 A2 问题，不是首页越层。 | 只拆句、不删名：「图里带 `hctl2-` 前缀的是本仓库交付的第一方组件。执行面的聊天、任务、工作流三类系统由外部供应端承担；Agency 是参与者的供给方，不是第五个模块，第一阶段由发布包自带的本地参考实现承担（运行时为 Herdr）。选型与阶段见交付文档。」判据只留 L5。 |
| W-03 | 修正 | 表头三列「metadata（控制面账本）／content（执行面系统）／artifact（Git 结晶）」写法一致，全是英文类别词，不构成 W2 的同表夹杂；约束层总则 §三类数据 明文「三个类别词可在设计正文携中文对照使用」。`architecture.md` 正文也以英文为主：metadata ×5、content ×11，中文「治理元数据」×2、「场景内容」×4（各含一次首现对照）——「正文却用中文」不准确。真正的毛病是括注内容：首现处括的是对照「metadata（治理元数据）」，表头同一形态里括的却是「住在哪」，读者会把「控制面账本」当成 metadata 的译名。 | 表头去括注，写「metadata／content／artifact」——「住在哪」由表前那句「每格回答什么数据、存在哪」和每格内容承担；或写「metadata · 控制面账本」用间隔号与对照括注区分。§避免供应商锁定 末段「治理元数据」「场景内容」两处中文裸用顺手统一为英文类别词。写法。 |
| W-04 | 修正 | 数字核对无误：`ChangeSet` 76 ＝ 来时路 9 ＋ 活文 67，`ReviewSubjectRef` 14 ＝ 来时路 2 ＋ 活文 12，与我排除来时路和代码块后的自扫一致。自扫加了小驼峰：另见 GitHub、OpenCode、AppService、WebView、WezTerm、macOS、sortOrder、nodeIntegration、ipcRenderer、contextIsolation，全是外部产品名或外部 API 原名，总则允许「外部标准、产品或源码中的原名……保留原形」，无漏。另：`src/build/docs/dead_names.txt` 注释已把 ChangeSet、ReviewSubjectRef 记为「现行保留原形」，改任一个都要同批改它。**对「保留 ChangeSet」的判断**：倾向保留，但理由要换——「Git 世界有 changeset 的通用说法」不准，Git 自己叫 commit，changeset 是 Mercurial／Perforce／TFS 的词。站得住的理由是：W1 的病是「两个词粘成一个新概念」造成概念爆炸，ChangeSet 是 Change＋Set 合起来才成一个概念，是总则点名的核心产品词，ChangeSet Revision、Write Lease 的定义都挂在它上面，改名的收益只是让规则零例外。若所有者要零例外，最便宜的改法是 `Changeset`（业界单词形、不驼峰），按 0.9 协议扫 76 处。**ReviewSubjectRef**：维持改法，依据更硬——总则 §词汇分类法 把它归「引用格式：指认他物的结构化引用，不是对象」，并规定格式标识「保留原形并用代码格式标示」；它的同族 `review_subject_digest`、`revision_digest` 都是蛇形代码体，只有它是驼峰散文体。 | 「`ChangeSet` 保留（或改 `Changeset` 求零例外，所有者拍板），词汇表注明保留理由是『合成才成一个概念、核心产品词』而不是 Git 通用说法；`ReviewSubjectRef` 散文写『评审对象引用』（词汇表 L168 已给此译），字段位写 `review_subject_ref`，同批改总则 §词汇分类法 表例、§词汇索引 与 `dead_names.txt` 注释」。仍进 B 档。 |
| W-05 | 维持 | `doc-discipline.md` 第四条原文：「专有名词保留英文，中文对照分两种：有自然译名的（仓库、看板、参与者、上下文、施工图、裁决、凭证、技能包、工件、备忘），每个文档第一次出现时给对照；只有释义式说明的（Task、Run、Workbench、Seat、Obligation 这类），在术语对照表定义一次即可，正文不要求括注。（所有者裁定，2026-09-02）」。L4「新术语在词汇表**或**首次出现处给一次释义」是选择关系，第四条是对其中十个词的收窄，两条不冲突；写作指南开头也说「两者冲突时，内容正确性优先」。反转正确。但这个反转把一个实质发现藏掉了：按十个词过滤后残余不是空的，见漏掉 #1。 | — |
| W-06 | 修正 | 约束层 `CAS` 8 处逐处看。机制名 4 处：`system.md:162`「expected-version CAS」；`:198` 两处（「单写者、CAS 与 current pointer 是三件事」「expected-version CAS 回答一条命令对哪个版本生效」，句子主语就是机制名）；`task.md:119`「不放宽 Project group、drift 或 CAS 前置」（前置条件的类名）。散文动词用法 4 处：`system.md:188`「以 CAS 推进其 `control_writer_generation`」、`:208` 表格「取得账本写权时 CAS 推进」、`:227`「并 CAS 推进 control writer」、`connections.md:175` 表格「CAS 拒绝」。词汇表 L57 允许「字段名和实现机制名中保留 CAS」，前 4 处合规，后 4 处不在豁免内。 | 「待核」改为定论：「system.md:188／208／227 与 connections.md:175 四处动词用法改『比较并交换』；`expected-version CAS` 与作主语的机制名保留」。写法。 |
| W-07 | 维持 | `task.md:52`「adapter（适配器）可以把它转成同一个完成请求」与 `:62`「不能由 adapter 代替人猜」——同文件一处带对照、一处裸用。架构层九文件实数：adapter ×4（README.md 1 是「provider adapter（供应端适配器）」对照、task.md 2、participant.md 1），适配器 ×15（architecture 6、participant 4、docs/design/README 2、project 1、task 1、README 1）；主评审员的 5／12 略偏，方向不变。`participant.md` 更明显：`:53`「每个 harness 适配器」与 `:56`「集成凭据由工具箱或 adapter 代用」隔三行两写。L4 第二行「已有通用中文译名的技术词用中文」。 | 位置补 `participant.md:56`；数字改 4／15。 |
| W-08 | 修正 | 约束层总则 §核心产品词 原文：「人不是 Participant，写 human actor。」——总则对写法有明文规定且未限定层，`participant.md:17` 与 `project.md:25` 是照规定写，不是「约束层短语漏进架构层」。「词汇表没有词条」为真。因此二选一里「架构层写『有权的人』、约束层保留 human actor」会与总则相抵，除非同批改总则。真正的同层两写在别处：架构层大量裸用「human」当形容词——`docs/design/README.md:55`「只来自有权 human」、`:56`「由 human 提交」、`project.md:24／61／83`「结构化 human 动作」「归属到 human 的动作」、`task.md:28`「有权 human」；同时 `participant.md:17`、`task.md`、`project.md` 又写「有权的人」；愿景层 `vision.md:122`「human 动作」。三种形态（human actor／human／有权的人）并存，正是 L4「中文句子里嵌了英文词……有且不歧义，换掉」的情形。 | 「词汇表补一行『human actor｜有权的人』；架构层与愿景层裸用的『human』先清零——统一为 human actor（从总则）或改『人／有权的人』（从 L4）二者取一，所有者拍板」。仍进 B 档，但裁决对象换成「human 裸用」而不是 human actor 本身。 |
| W-09 | 推翻 | 词汇表 L42 Terminal 行已写「也是执行体暴露给 Workbench 的 TUI 接口（participant.tui）」，来时路 `:337` 同。L3 要求的「产品散文名与组件标识符必须在词汇表显式声明一对一映射」已经满足（Terminal ↔ participant.tui 同一行）。它不是驼峰，W1 不适用；它本来就是接口标识符，读者「当成代码标识」是读对了。主评审员给的两个选项之一（「保留为代码体标签但在词汇表登记」）就是现状。剩下的只是格式：`participant.md:96` 用了代码体，词汇表 L42、`docs/design/README.md:13`「Workbench Terminal（participant.tui）」没用。 | 从含义／B 档降为格式小修：全库标识符统一代码体 `participant.tui`，不进裁决包。 |
| W-10 | 维持 | 判据要修：它不是驼峰，是连字符自造复合词，W1 不适用、W3 适用；业界现成词是 small language model（SLM）。`context.md:58` 首现「专用小模型（small-brain）」之后 `:74／75／76` 三处只写 small-brain，违反 L4「之后全文统一使用选定形式」；`spec/project.md:112`「专用的小模型（small-brain）」、`:114`「专用压缩模型……small-brain 引用」、`:118` 两处 small-brain——同文件三个名字（专用小模型／专用压缩模型／small-brain）指一物，L3。`contract-tests.md:27`、来时路 `:230／231` 同。`context.md:58` 自己写明「small-brain 只是执行者配置里的一个模型引用，不是新对象」，所以它不该有专名待遇。建议不变；`spec/project.md:114`「专用压缩模型」一并归入。含义无争议，B 档可降为写法。 | — |
| W-12 | 修正 | `connections.md:44`「human scene / Run reducer → Participant」、`:45`「human Kanban / Run reducer → Task」，耐久输入列「human provenance」，准入列「human actor 或 task-bound Run reducer」。逐词看：「human actor」是总则规定形态；「human provenance」是总则 §三类数据 三条法原文（「回写本身不得再取得 human provenance」），约束层沿用合规。真夹杂只有「human scene」「human Kanban」——不是任何东西的名字，是临时短语；「task-bound」是形容词。但主评审员的改法会制造新问题：方向列其余行全是英文模块名（Project → Task……），改成「人在场景中的动作／Run 归约器 → Participant」后同一列中英夹杂，正撞 W2。另外「Run reducer」在约束层与「归约器」两写（见漏掉 #3），改法里的「归约器」等这一条先裁。 | 方向列写「human actor / Run reducer → Participant」「human actor / Run reducer → Task」（scene／Kanban 限定词删去——耐久输入列已说清带的是什么）；「task-bound Run reducer」→「绑定 Task 的 Run reducer」；human provenance 保留；reducer／归约器随 #3 一起定。 |
| W-13 | 维持 | `connections.md:175` 表格「目标事务提交前来源已变化｜CAS 拒绝，不创建下游事实」与同文件 `:165`「提交前发生分歧时，比较并交换必须拒绝」同一机制两写；表格里的「CAS 拒绝」是动词用法不是字段名，不在词汇表豁免内。L3。 | — |
| W-14 | 修正 | 两处性质不同。§单写者 `:188`「以 CAS 推进其 `control_writer_generation`」是动词散文，改「以比较并交换推进」成立。§控制面自己的存储 `:162`「更新 current pointer 必须经唯一 control writer 做 expected-version CAS」——「expected-version CAS」是带限定词的机制名，同文件 `:198` 把它当专名解释，落在词汇表「实现机制名中保留 CAS」的豁免里，不该改。主评审员漏了同文件另两处动词用法：`:208` 表格「CAS 推进」、`:227`「CAS 推进 control writer、site 与 Agency binding generation」。 | 「`system.md` 三处『CAS 推进』（:188、:208、:227）改『比较并交换推进』；`expected-version CAS` 保留」。 |
| W-15 | 修正 | 全库 `ground truth` 9 处（含来时路 1），带对照的只有 3 处：`task.md:60`、`project.md:71`「ground truth（事实源头）」、`spec/README.md:50`「第三方 ground truth，事实源头」；其余 6 处裸用：`spec/README.md:57`、`spec/project.md:13`、`:64`、`spec/system.md:15`、`:16`、来时路 `:221`。「每次出现都带括注」不成立，事实相反——全库从未单独用过「事实源头」。问题仍是 L4（首现之后未统一），只是方向反了：架构层两处都是各自文件的首现且带对照，合规；约束层 `spec/README.md` 首现带对照、同文件 `:57` 就裸用了，`spec/project.md`、`spec/system.md` 全裸。 | 「架构层维持；约束层 `spec/README.md:50` 之后 5 处裸用改『事实源头』，或整层选定英文——二者取一，按 0.9 协议扫」。写法。 |
| W-16 | 修正 | L4 原文判据：「禁止在同一术语内混用中英文——『直接 mutation』『binding 分歧』既不是中文也不是英文。」形态上「Chat 端口绑定」正是这类。但 L4 表第一行「具名对象与角色名｜按约束层总则的规定形态，全库统一」把具名对象的形态交给了总则，而 `spec/project.md:14` 对象表登记的形态就是「Chat 端口绑定」——所以「L4 明令禁止」不准确，L4 对已登记对象名让路。它真正的病是在自己两个家族里都是孤例：Binding 族其余四个（Resolved Port Binding、Task Binding、Project Role Binding、Engine Execution Binding）全是英文带空格专名；端口名其余全中文（`system.md:16`「任务源端口」）只有它写「Chat 端口」（`system.md:15`、`docs/design/README.md:10`）。词汇表 L110 已把中文对照定为「聊天端口绑定」。处数：全库 16（来时路 4），活文 12，不是「约 20」。顺带发现：`spec/README.md` §词汇索引 Binding 族只列四个、不含它，词汇表 Binding 族却含它；来时路 `:111` 记它在 v0.11.1 从 ChatSurfaceBindingRevision 降为「Room 的 Chat 端口绑定字段组」，`spec/project.md:29` 写入约束表又给了它 lifecycle——它是字段组还是对象未定，交 A 轴（A1／A4）。 | 改名方向维持「聊天端口绑定」（与「任务源端口」同形），理由改为「族内孤例＋词汇表已定对照」；若 A 轴裁它是 Binding 族对象，则应是「Chat Port Binding（聊天端口绑定）」与兄弟同形。处数改 16／12；判据去掉 W1（不是驼峰）。 |
| W-17 | 修正 | 交付层按路由表④「可引用约束词以指认被测约束」，总则又允许「外部标准、产品或源码中的原名……保留原形并用代码格式标示」。把主评审员列的词按这两条分三堆。**（甲）外部原名，合法但应加代码体**：Dagu 的 Start/Stop/Retry/Reschedule/Approve/Reject/Edit/Rename/Delete（`:53`）、Vikunja Done 与 webhook 字段 doer（`:32`、`:35`）、Herdr workspace/pane（`:80`）；`m.room.encryption` 已加。**（乙）约束层自己就用的英文约束词，交付层沿用合法，单改交付层反而造成跨层两写**：drift（spec 3 处：`task.md:111／119`、`connections.md:182`）、placement（spec 5 处）、provenance（总则三条法原文）、regate（`run.md:139`「完整 regate」）、quorum（`run.md:154`）、attach/replay（恢复等级名）、known/unknown、intent、Run reducer——这些要先改约束层（0.9 第二步「定义先行」），不能在契约测试单独 sweep。**（丙）真夹杂——约束层从不这么写、或同文件已有中文**：fail closed／fail-closed ×5（约束层 0 处，约束层写「类型化拒绝」）；adoption ×2 与同文件「采纳」×2 并存；divergence ×1 与同文件「分歧」×4 并存（约束层 分歧 26 : divergence 1）；credential ×1 与同一行「凭据」并存（`:78`）；readback ×2 与同文件「回读」×11 并存；lane（约束层写「泳道」）；terminal intent（约束层写「终态」27 处，且与场景名 Terminal 撞词，读者会读成「终端意图」）；reviewer Seat（约束层 `run.md:148` 写「必需评审 Seat」）；recipient、stage or health、human action、mutation、executor（约束层 0 处）。**比例感**：116 条用例里 108 条含拉丁字母，但大头是核心产品词、约束层对象名与外部原名；（丙）类约 15 个词、落在约 25 条用例里，占全文用例四分之一上下。「全库最重」成立，但可直接改的只有（丙）。 | 建议改为三段：「（丙）类直接改中文——fail closed→类型化拒绝，adoption→采纳，divergence→分歧，credential→凭据，readback→回读，lane→泳道，terminal intent→终态命令，reviewer Seat→必需评审 Seat，recipient→接收者，stage or health→阶段或健康，human action→human actor 的动作，executor→执行者；（甲）类加代码体不译；（乙）类不动，随约束层裁决」。主评审员原表里 drift、provenance、placement、regate 四词从『改中文』移到（乙）。 |
| W-18 | 维持 | 来时路 `:357` §36「四个对仗 Scene」——§36 标题是「当前设计」，写的是现行结论不是史料，全库现行产品词是「场景」（总则、词汇表 §场景与系统、architecture.md）。§4「四个 Scene 与模块对仗」是 v0.x 时期史料，路由表给参考层「全部」词汇，可不动；只改 §36。 | — |
| W-19 | 修正 | `usage.md` 六处 Chatroom 分两类。`:3`「现在可以运行 Chatroom、Kanban、Workflow、Terminal 四类打包依赖」是四个场景并列，该写 Chat Room。`:9`「Chatroom（Tuwunel + Cinny）」、`:86`「Tuwunel 与 Cinny 共同构成 Chatroom」、`:139`「Chatroom 随包浏览器客户端」、`:181`「Chatroom 页面」、`:211`「共同组成 Chatroom」指的是聊天服务器加浏览器客户端这组服务，是 content 系统不是场景——改成 Chat Room 会把场景名当服务名，撞 `architecture.md` §场景与系统「场景是用户视角的名字，系统是 content 承载者的角色名」的区分。`src/` 里没有 chatroom 子命令（grep 0 处；`usage.md:86` 说组件名固定为 tuwunel、cinny、vikunja、dagu、herdr），「若是 chatroom 保留代码体」那半句作废。 | 「`:3` 改 Chat Room；其余五处改『聊天服务（Tuwunel + Cinny）』或『chat server 与客户端』；不涉及代码体」。 |
| W-20 | 维持 | `WRITING-GUIDE.md` 全文 grep「驼峰」「CamelCase」0 处；L3 只管「同一概念同一术语」，L4 只禁「同一术语内混排」，都没有「同层同表同一写法」；来时路 §35 台账 v0.11.1「25 个驼峰名改带空格专名」是一次性词形收敛，没有立规则；四轴原则末段自述「前两条写作规则尚未进写作指南」。补一句：驼峰一条已有现成机械检查——清点脚本 §四 就是驼峰扫描，加一个白名单（ChangeSet 加外部产品名）即可升为检查，与 I-04 合并。 | — |

## 主评审员漏掉的 W 轴问题

按 W1–W3 与 S1 两关，愿景层（`vision.md`、根 `README.md`）与架构层（`architecture.md`、`docs/design/README.md`、`project.md`、`task.md`、`run.md`、`participant.md`、`context.md`）各通读一遍，重点看了 `participant.md` 与 `context.md`。

1. **十个自然译名词的首现对照，愿景层与架构层几乎无一文件合规**（W3、doc-discipline 第四条）。grep「X（对照）」形态：`architecture.md` 0／5（Repo `:32`、Kanban `:32`、Participant `:9`、Workflow `:33`、Memo `:57` 全裸）；`docs/design/README.md` 0／7；`context.md` 0／7（Repo、Participant、Context、Verdict、Skill、Artifact、Memo）；`participant.md` 1／6（只有 Skill（技能包）`:21`；Participant、Context `:125`、Verdict `:133`、Receipt `:133`、Memo `:51` 裸）；`project.md` 3／8（Participant、Artifact、Memo 有；Repo `:33`、Kanban `:12`、Context `:14`、Workflow `:12`、Skill `:51` 无）；`task.md` 1／6；`run.md` 2／4（Participant `:41`、Workflow `:1` 无）；`vision.md` 4／8（Repo `:71`、Context `:142`、Skill `:142` 无；Artifact `:71` 括的是「登记过的交付物」不是「工件」，对照词也不一致）。根 README 不在 doc-discipline 管辖范围（它管 `docs/design/`），但 Kanban、Participant、Workflow、Context 同样全裸。裁定是当天的、文档尚未跟上，性质是一次 sweep（写法），但应作为独立发现列入，而不是只改清点口径；清点脚本 §三 限十词后直接产出这张表。
2. **「Execution Spec」的中文对照两写**（W3、L3）。总则 §核心产品词「Execution Spec（执行规格）」；词汇表 §票据与规格「Execution Spec｜派发规格」；`architecture.md` §模块交接「派发规格（Execution Spec）」×2；`participant.md`「执行规格」×7、`context.md` ×3、`run.md` ×1、`connections.md` ×1。六个高频约束词是路由表点名允许架构层「携中文对照」用的，对照本身不一致就是直接违反。建议以总则为准统一「执行规格」，改词汇表与 `architecture.md` 两处。
3. **「Run reducer」与「归约器」同层两写**（W2、L3）。约束层 `connections.md` 4× Run reducer；`run.md:29` Run reducer 与 `:158` 归约器同文件；`system.md:112` 归约器；`project.md:31／32` reducer；`task.md` 3× 归约器。架构层统一用「归约器」（`docs/design/README.md` 1、`run.md` 2、`task.md` 2），交付层用 reducer。「归约器」是通用中文译名（L4 第二行），建议约束层统一「Run 归约器」；与 W-12 联动。
4. **状态值「known／unknown」违反总则「状态值使用中文语义名」**（W3）。`participant.md`（架构层）known ×4、unknown ×5（「标 known」「账本标 unknown」）；`spec/participant.md` 2／2；`spec/run.md` 2／2；`contract-tests.md` 1／1。它是可核验性标注的枚举值，总则 §词汇分类法「状态值使用中文语义名」直接适用；草稿 A-38 只点了 `spec/run.md` 的 `known/unknown`，架构层九处没点。建议「已核验／仅申报」（工具箱能回读＝已核验；只有 Agency 声明＝仅申报）。命名是 for agent 的接口细节，可直接落。
5. **`context.md` §与相邻概念的分工 表内对照混排，背后是两条规则冲突**（W2）。同一列里「Memo」（英文核心产品词）与「根上下文清单」「消费上下文包」（Context Manifest／Context Bundle 的中文对照）并列，按四轴原则记号约定「同一张表……要么都写英文名，要么都写中文名」不过。但这是路由表的必然结果：架构层只放行核心产品词与六个高频约束词，Context Manifest／Bundle 不在六词里只能写中文，Memo 必须写英文。`context.md` 里根上下文清单 ×4、消费上下文包 ×4、Context Manifest 0——在这份横切正文里它们就是高频词。要所有者裁：把 Context Manifest／Context Bundle 加进高频约束词表（架构层可写「Context Manifest（根上下文清单）」），或接受表内对照混排。进 B 档。
6. **`vision.md` 三处可直接用中文、或与首页主次相反**（L4 第二行、S3）。`:126`「这个内核在结构上是 client-server 的」「**架构是 client-server 的，local-first 是默认部署形态……**」——`architecture.md:19` 同一意思已写「客户端—服务端结构」，愿景层反而用英文；`:122`「显式且可归属的 human 动作」→「人的动作」（并入 W-08 的 human 裸用清零）；`:7` 产品姿态「**Project-scoped、Room-mediated shaping、Task-tracked、Run-executed**」英文加粗为主、中文为释，根 `README.md:7` 同一句是中文为主、英文括注——同一口号两种主次，建议愿景层改成首页形态。写法。
7. **词汇表 L43、L44 链接文本残留旧模块名**（L3、0.9 第五步）。ChangeSet、Evidence 两行「权威定义」列写「spec/agent」，指向 `../spec/participant.md`；Agent 被 Participant 取代（来时路 `:337`）后漏改的文本，同表其他行写「spec/participant」。写法。
8. **「Share to Room」动作名不合总则「命令使用动宾语义名」**（W3，低优先）。`participant.md:114`「只有显式的 Share to Room 动作经 Project 准入后才能发布」、`spec/participant.md:156`、`contract-tests.md:76`「无 provenance Share」。它是改变 Room 事实的动作，同类命令都写「完成 Task」「合入 ChangeSet」，应写「分享到 Room」。Trigger Preview（预览步骤）与 Execution Chat（总则明说的场景投影）不在此列。

查过、不报的：`participant.md` 恢复等级表五个英文等级名（exact attach 等）——一列全英、「含义」列给了中文释义，合 L4 第三行，spec 与 CT 沿用同名；「BYOA（bring your own agent）」首现有释义，「告别 BYOA」作节标题是同文件定义过的缩写，S1 第二关过；`context.md`「prompt」×7、「token」——业界通行未译词，四轴原则自己也写 token；「近详远略」「代取」「萃取」「受限召回」——中文自造但就地定义，S1 第二关过；`participant.md` 恢复等级、`spec/run.md` 图结构词（fork/join、switch、loop、dynamic fork、timer wait、noop）——外部工作流引擎的原名，总则允许。

## 统计

19 条实核（W-01–W-20，W-11 空号）：维持 6（W-05、W-07、W-10、W-13、W-18、W-20），修正 12（W-01、W-02、W-03、W-04、W-06、W-08、W-12、W-14、W-15、W-16、W-17、W-19），推翻 1（W-09）；漏掉的问题 8 条（其中 1 条需所有者裁决规则冲突，7 条为写法 sweep）。
