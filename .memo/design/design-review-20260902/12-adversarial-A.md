# A 轴对抗核验（模块与架构）

> 状态：已核验<br>
> 基线：main 分支 claude/review-r1 @ f8dcdf8<br>
> 去向：合成 13-findings.md

核验对象是 [`11-findings-draft.md`](./11-findings-draft.md) 里编号以 `A-` 开头的全部 45 条（A-29、A-30 在清单里不存在，编号跳过）。方法：先开原文核对「发现」栏的事实（位置、原句、数字），再用 [`02-checklists.md`](./02-checklists.md) 的 A1–A7 反问是不是真违反，并查写作指南 0.4 路由表、[文档纪律](../../../docs/design/doc-discipline.md)、[约束层总则](../../../docs/design/spec/README.md)有没有明文放行；最后看「类别」栏（写法／含义）标得对不对。默认怀疑，尽力推翻；推不翻的才维持。

涉及主评审员本人最近写过的文件（`participant.md`、`spec/participant.md`、`context.md`、`architecture.md` 的 Terminal 行）的条目，理由栏末尾多一句「作者自审」，回答他是在为自己开脱还是对自己过严。

## 逐条结论

| 编号 | 结论 | 理由与原文引用 | 修正后的表述（若修正） |
| --- | --- | --- | --- |
| A-01 | 维持 | 事实成立：`vision.md` §设计原则 第 5、6、10、11、12 条原文只有加粗标题加「——细则见[设计地图共同规则]」。写作指南 M3「每条原则是一个完整的句子，不是短语碎片；自包含」被违反。还要补一刀：链接指向的 `README.md` §共同规则里，第 6 条「Chat 不是数据库」与第 10 条「至少一次投递也必须保持正确」并没有对应细则（那一节八条讲的是稳定 ID、类型化命令、供应端事件分类、完成来源、临场边、绑定冻结、Workbench 存活、单写者），所以这五条里至少两条是空链接。建议里的「为什么」半句是新写的论证文字，第三轮落笔时要让所有者过一眼，但不改任何约束，仍算写法 | — |
| A-02 | 维持 | 事实成立：`vision.md` §产品原生核心与架构最小内核 表「持久账本与对账」原句「outbox/回读、崩溃恢复和投影重建跨组件保持一致」。路由表 ① 允许词汇是「核心产品词 + 日常语言」，`outbox` 两者皆非；词汇表也没给它「愿景层可用」的豁免 | — |
| A-03 | 修正 | 事实成立，判据不成立。原句（`vision.md` 第 122 行）「外部平台的事件先按模块约束分类：content 变化进入观测/投影，显式且可归属的 human 动作可以成为同一 HCTL 命令请求，运行时输入只推动精确执行，结果只能作为提案；只有通过同一道准入边界，才可能改变 HCTL 语义」。这句用的是日常语言，没有字段名、状态机；文档纪律明文：「愿景、论证与体验叙事不因篇幅或『与约束重复』被删除——权威去重针对约束，不针对解释」。它是对「命令准入」这项最小内核能力的解释，A2 不成立。剩下的只是 L5（一句四个分句）的写法问题 | 发现改为：一句话并列四类事件处理，读者要数着读（L5）。建议改为：拆成两句或四项清单，分类本身保留，末尾加「精确分类见系统边界」；不删。判据只留 L5，去掉 A2 |
| A-04 | 推翻 | 引文失真。`vision.md` 第 65 行原句是「越靠后**它的执行面**越是可替换的资源。上端的事实必须在工具更换后继续存在；下端的进程、会话和工作树可以丢失、重建和接管，而参与者身份不随它们丢失」——发现栏把「它的执行面」漏掉，才得出「读者会把 Participant 读成可替换的执行资源」。这句正是来时路 §34 记录的所有者裁决：「愿景里『越靠后越是可替换的执行资源』改为『越靠后，它的执行面越是可替换的资源』：参与者身份不随进程丢失」（2026-09-02）。现文与裁决一致；建议（乙）要加的那句话，第 2 点已经写了。阶段名「运行」对应的是第四个问题「哪个具体执行实例正在哪里运行，如何观察、恢复或接管」，身份是回答这个问题的手段，不需要进阶段名 | — |
| A-05 | 维持 | 事实成立：`vision.md` 第 9 行「多 Harness（编码代理工具）」、第 17 行「Coding Harness（编码代理工具，如 Codex、Claude Code、OpenCode）」、第 156 行「不重新实现 LLM、Coding Harness、……」。同一文件里两个名字、两次对照；根 README 已定「下称 Harness」。A7 成立。判据栏的 W2 不贴切（这不是同表中英夹杂），只留 A7 即可 | — |
| A-06 | 修正 | 不是缺陷，是提案。A5 问的是「条款为什么在这」，A6 问的是「模块能否覆盖方法论功能」，都套不到「愿景缺一条原则」上；真正的依据是四轴原则 §一末段所有者给的「快、好、省」三维度。而且愿景并非全无：`task.md` §无 Run 的轻量路径「走完它，必须比直接开一个终端……更轻」是「快」的可判标尺，`context.md` 有「快、省、准」。所以问题是「有没有统一立成取舍原则」，不是「没有」。「先保好，再保快，最后保省」的顺序是评审员自拟，已如实标注 | 判据改为「所有者原则（四轴 §一）」；发现改为「快、好、省散在三处，没有在设计原则里统一立成一条取舍原则」；类别仍是含义、进裁决包 |
| A-07 | 修正 | 位置不全：同一句姿态在 `vision.md` §一句话定位 第 13 行也有（「Project-scoped、Room-mediated shaping、Task-tracked、Run-executed」），愿景才是权威，README 只是复述；两处必须同改。评审员自己倾向（甲）不改，那么这条不是缺陷，是一个「已考虑、不改」的记录 | 位置加上 `vision.md` §一句话定位；结论写成「维持四短语；若所有者要加 Participant，两处同改」，从裁决包降为备注 |
| A-08 | 修正 | 数字有误：`architecture.md` 里 `Herdr` 只出现两处——§三个面 执行面行「运行时为 Herdr」、§场景与系统 Terminal 行「进程、PTY、终端会话与 TUI 采用 Herdr」；「第一阶段」出现五处。违反成立：同文件 §场景与系统 首段「角色名是产品词，具体采用哪个实现由交付文档的选型判据与限时验证决定」，`delivery.md` §选型判据「实现名只出现在本文与实现证据，设计层正文只用系统角色名」。Matrix 是稳定边界（§避免供应商锁定 表），可留。作者自审：自报「两处是我 #138 写的」，不开脱；数字错在多算了一处，不是护短 | 「三处」改「两处」；其余不变 |
| A-09 | 修正 | 方向对，数字略偏。`spec/connections.md` §连接约束总表 八行；`architecture.md` §模块交接 四行，其中「Run / Project → Participant」一行覆盖了两条边，所以是四行覆盖五条边、漏三条：合入 ChangeSet、完成 Task、Task/Run/Participant → Project 事件。漏「完成」最伤，因为 §模块交接 自称「连接约束总表的产品语言投影」。另注意 §模块交接 首句「每次交接都交付一个冻结的、带摘要的不可变对象」把事件回流那条边排除在外了（事件「可从源账本重建」，不是冻结对象）——见漏项 L-5 | 「只投了 8 条边里的 4 条」改「四行覆盖五条边，漏三条」；建议不变 |
| A-10 | 修正 | 三个名字确实并存：「工具箱」全库约 48 处（约束层为主，`participant.md` 5 处），`hctl2-tool` 标识符 12 处，「现场执行者」4 处（`architecture.md` 控制面行、`participant.md` 第 82 行、根 README 图标签、来时路 §33 标题）。但对 #33 的读法不对：§33 原文是「hctl2-tool 收束为**现场执行者**五项：worktree/ChangeSet 物化与隔离……」——那是定界（划定五项职责），不是改名；散文名一直是「工具箱」，词汇表也登记了「`hctl2-tool`｜工具箱；两者始终指同一个现场执行组件」（L3 已满足）。所以问题只是「现场执行者」在 `architecture.md` 与 `participant.md` 被当名词用了两次。评审员建议全库改「现场执行者」是 48 处 sweep 的贵方向，且要改词汇表既有映射 | 发现改为：「现场执行者」是 §33 给 hctl2-tool 的职责定界语，在 `architecture.md` 控制面行与 `participant.md` 第 82 行被当第三个名字使用。建议改为：散文名维持「工具箱」（词汇表已有映射），两处「现场执行者」改「工具箱」或写「工具箱（现场执行者）」一次；词汇表在 `hctl2-tool` 行补一句「职责定界见来时路 §33」 |
| A-11 | 修正 | 事实成立但不全。`README.md`（设计地图）模块表 Participant 行不止 content 系统列有「运行时 Herdr」，同一行「场景客户端 / 受控端口示例」列还有「Herdr API / TUI」；§场景客户端与受控端口 第 48 行「第一阶段的 Tuwunel、Vikunja、Dagu、Herdr 都经各模块自己的受控端口……接入」一句点了四个产品名。总则明文「设计层正文——含仓库 README 与设计地图——仍只用核心产品词与上述六词」。作者自审：该行是 #138 改的，评审员自报了一列、漏看了同行另一列和第 48 行，是漏看不是护短 | 位置加上同行「受控端口示例」列与 §场景客户端与受控端口 第 48 行；建议：三处一并改角色名，第 48 行改「第一阶段选定的四个供应端都经各模块自己的受控端口……接入（选型见交付文档）」 |
| A-12 | 维持 | 事实成立：模块表 Project 行「模块拥有」列写「参与者」；`architecture.md` §4×3 归属矩阵 Chat Room 行写的是「Participant 名册与角色绑定」，`participant.md` §模块拥有什么 说「角色绑定归 Project」。两处口径不一，README 的那个词会让读者以为身份归 Project。判据栏的 A7 不贴切（不是两个名字），应记 A6 或 T5（状态归属）；不影响结论 | — |
| A-13 | 维持 | 事实成立：`README.md` §共同规则 末段「CAS、outbox、单写者和适配器恢复等通用机制只在[系统边界]定义一次」。路由表 ② 只放行六个高频约束词，`CAS`、`outbox` 不在其中。反方：词汇表允许「字段名和实现机制名中保留 CAS」，这句正是在列机制名——但路由表是写作协议、词汇表是非规范对照，且中文「比较并交换」「发件箱」全库已通行（`run.md` 第 34 行就写「outbox（发件箱）」），改动零成本。维持 | — |
| A-14 | 推翻 | 随 A-04。`README.md` 第 17 行原句同样带「它的执行面」限定：「越靠前越接近人的意图，越靠后它的执行面越是可替换的资源」。与来时路 §34 裁决一致，无需改 | — |
| A-15 | 修正 | 事实成立，表述含混。全库只有一处把 `participant.md` 叫横切正文——`project.md` §为什么存在 末段「Participant 的完整设计（七件事分层、专业化与评审方法论）见[横切正文](./participant.md)」；`participant.md` 自己的文档头已写「本文是 Participant 模块的设计正文」，根 README 也只列 Context 为「一份横切正文」。另一处是 §模块拥有什么「Participant 与角色」，与 4×3 矩阵「Participant 名册与角色绑定」不一致。发现栏「两处仍把 participant.md 叫横切正文」应是「两处各一病」。作者自审：自报「#138 漏改」，诚实 | 发现改为：§为什么存在 末段仍把 `participant.md` 叫「横切正文」（一处）；§模块拥有什么 说 Project 拥有「Participant 与角色」（一处）。建议不变 |
| A-16 | 修正 | 事实成立：`project.md` §Chat Room 场景「将来若 Matrix widget/AppService 能提交显式结构化动作，也必须归一到同一 Preview/Submit 入口」。但 widget 与 AppService 是 Matrix 规范里的机制名，不是产品名；总则允许「外部标准……原名需要逐字指认时，可以保留原形」；而且同层的 `architecture.md` §避免供应商锁定 已在用同类词「homeserver/bridge 生态」。A2 不能干净成立。这句是前瞻性的一句让步，不承担架构判断，简化无害但不是必改 | 降为「可选写法」：若简化，只删 widget/AppService、保留 Matrix：「将来若 Matrix 客户端能提交显式结构化动作……」；不作为 A2 违例计入 |
| A-17 | 修正 | 事实成立：`task.md` §无 Run 的轻量路径「第一阶段一个 Run 绑定至多一个契约版本，同一 Task 最多一个活动 Run」，§关键规则「Reopen/Deleted 第一阶段只作来源事实」。但建议的去向错了：这条限制的权威在约束层——`spec/run.md` §Workflow 与 Run 授权「Project、0..1 个 Task Revision（第一阶段）」、`spec/task.md` §写入约束「每个 Task 在账本中至多有一个绑定 Run 的占用标记」；`delivery.md` §明确不做「第一阶段每个 Run 只绑定 0..1 个 Task Revision」是复述。路由表 ④ 明说交付层「非规范；不改变领域含义」，把架构层的引用指向交付层等于把权威指错。约束层自己就带「第一阶段」限定（`spec/system.md`「第一阶段是单用户模型」），说明「第一阶段」类限制是有期限的约束，不只是交付范围 | 建议改为：架构层保留结论，引用指向对应约束条款（`spec/run.md` §Workflow 与 Run 授权、`spec/task.md` §写入约束），不指向 `delivery.md`；「是否把全部『第一阶段』限制集中成一张表、放在哪层」进裁决包 B 档 |
| A-18 | 维持 | 事实成立：`run.md` §Workflow 场景「Dagu 控制台保留为供应端管理与诊断界面」；同文件 §关键规则 已用角色语「工作流引擎自带管理界面」。违反 `delivery.md` §选型判据 的仓库自定规则 | — |
| A-19 | 维持 | `run.md` §模块拥有什么 原句「施工图……作为结晶归 Chat Room 场景；它的批准与版本对象仍由 Run 模块定义——结晶归属以事实为准绳，不随对象所有权走」。一句话说清了目的，正面样本成立 | — |
| A-20 | 修正 | 名单有误。逐文件 grep 架构层七个文件：`Herdr` 19 次（architecture 2、README 3、participant 14），`Dagu` 2 次（README 1、run 1），`Vikunja` 1、`Tuwunel` 1（均在 README）；`Cinny`、`Tauri`、`Electron`、`WezTerm`、`Continuwuity` 在架构层七个文件里**零次**——它们出现在 `spec/system.md` §安全边界（A-34）与 `delivery.md`，不属于这条。另有一个漏网的：`participant.md` §Terminal 场景 表「xterm、Execution Chat/结构化检查」里的 xterm 是库名 | 发现改为：架构层产品名共 23 处——`Herdr` 19、`Dagu` 2、`Vikunja` 1、`Tuwunel` 1，另有 `xterm` 1 处待定；Cinny/Tauri/Electron 不在架构层。建议不变（一次 sweep） |
| A-21 | 修正 | 数字太薄。架构层七文件里：`outbox` 2 处（`README.md` 第 63 行；`run.md` 第 34 行且已带对照「outbox（发件箱）」）、`CAS` 1 处（`README.md` 第 63 行）、`digest` 1 处（`context.md` 投喂三档表「ref + digest」）；`readback`、`cursor`、`fence`、`lease` 零次。四处里三处与 A-13 重叠。真正在架构层大量存在、没被这条抓到的英文标签是另一类：`known`/`unknown`（`participant.md` 3 处）、`Trigger Preview`（5 处）、`Execution Chat`、`Share to Room`、五个恢复等级英文名——它们不是约束词，是无家可归的具名步骤或状态标签，见漏项 L-1、L-2 | 发现改为：约束词进架构层只有 4 处（列明）；建议：`README.md` 第 63 行随 A-13 改，`context.md` 第 43 行「ref + digest」改「精确引用 + 摘要」，`run.md` 第 34 行已带对照、可留可改 |
| A-22 | 修正 | 三处要改。（1）数字：总则核心产品词恰 23 个，本来就不含 Agent，「去掉 Agent 后」是词汇表那张表的事（词汇表「核心产品词」表有 28 行，多出 Agent、Harness、Agency、worker、Chat Room 五个总则没列的词——这个不一致本身是漏项 L-4）。（2）分档方案有误判：Kanban、Terminal 是场景名，Workbench 是用户天天打开的桌面，Memo、Artifact 在愿景 §目标体验 第 1 步就出现（「引用代码、Artifact、Commit 或 Memo」）——都是用户面对面的词，放进「治理词」不成立。（3）它推翻的是所有者 2026-09-02 刚裁的一条：路由表 ① 注「核心产品词（Project、Task、Run……）是本层合法词汇（所有者裁定，2026-09-02）」。请求复议要写明 | 发现改为：总则 23 个核心产品词一律可进愿景层（所有者 2026-09-02 裁定），但愿景第 2 条原则自己分了两档；是否把这两档写进路由表，属复议。建议改为：若分档，用户核心词至少含 Repo、Project、Room、Task、Run、Participant、Request、Workflow、Memo、Artifact 与四个场景名，治理词只留 Obligation、Seat、Attempt、Gate、Verdict、Receipt、ChangeSet、Evidence、Context、Skill；类别仍为含义、A 档，并标注「复议 2026-09-02 裁定」 |
| A-23 | 修正 | 事实成立：`participant.md` 里 `Herdr` 14 次，§Agency 与执行体 有一张三行接口表（Herdr API／终端连接／Herdr TUI）。违反同 A-08。但建议过重：那张表的「规则」列讲的是架构分工——「启动、获准输入、打断和停止都经 HCTL 验证后调用……HCTL 不重新实现终端服务」「HCTL 验证票据与当前代次；……负责观察流、终端状态和输入执行」「输入会推动精确运行时，但不是 HCTL 治理命令或 Result Proposal」——把主语换成角色（Agency 运行时 API／终端连接／Agency 原生界面），它就是架构层该有的内容；搬去 `delivery.md` 会让架构层失去「谁做什么」的分工表。与 A-33 的处置（主语改角色）一致即可。作者自审：不开脱（自报「原样搬过来的」），反而下手过重——要把角色化后仍属本层的表整张搬走 | 建议改为：接口表留在 `participant.md`，主语改角色名；「第一阶段运行时为 Herdr」「当前 Herdr 缺项与验证边界」两句改成一句「运行时选型与已知缺项见交付文档」；`Herdr` 在架构层归零。类别仍为写法 |
| A-24 | 修正 | 数字错：`participant.md` §关键规则 是 20 条，不是 19（逐条数过）。对比同层：project 11、task 6、run 9，20 确实偏多，「抓大放小」成立。但收缩的尺子不对：评审员按「约束附录里都有原文」挑要删的，可 20 条在约束附录里**全部**都有对应，这个尺子会把全部删掉；写作指南 S1 已裁「过了第二关的浓缩仍是本库的资产」，像第 10 条「模型说『我已经合并了』不可信」、第 13 条「执行体进程干净退出不等于交付了结果」正是这类人话资产。作者自审：对自己略过严——把自己写的人话浓缩当约束口径要删；条数也数少了一条 | 发现改为：20 条，约为同层其他正文的两倍。建议改为：按「读者需不需要记住它」而非「约束层有没有」挑骨架，保留已过 S1 第二关的人话一行句；收到 10 条左右，其余一句「精确规则见约束附录」。类别写法 |
| A-25 | 维持 | `participant.md` §七件事分层 原句「上三层归控制面账本，由用户建立和绑定；下四层的实物由 Agency 供给……账本只冻结对它们的精确引用」。一句话把归属说清，正面样本成立。作者自审：自评正面样本，不算护短——句子确实做到了；顺带一提，七「层」里人设、模型与特殊训练在约束层只是 Participant／Worker Profile 的字段而非对象，正文用「层」而不立名，处理得当 | — |
| A-26 | 修正 | 事实成立：`context.md` §为什么存在「检验标准就三个字：**快、省、准**」。但两把尺子量的不是一个东西：「快省准」量的是上下文组装，「快好省」是所有者这一轮量方法论投票的尺子（四轴原则 §一），还没裁成产品原则。而且「快省准」不只在这里——`context.md` 文档头引用的研究文件就叫「快省准横评」（`docs/research/context-landscape-20260824.md`），改名的脚印比一节大。A7「同一概念两个名字」不成立，因为不是同一概念。作者自审：对自己过严——A-06 还没裁，就先把自己文件里既有的、研究文献同名的尺子改去迎合 | 发现改为：Context 的「快、省、准」与所有者的「快、好、省」是两个对象上的两把尺子，只差一字，读者容易以为矛盾。建议改为：A-06 若立为原则，`context.md` 加一句「快省准是快好省在上下文这件事上的具体形式：准就是这里的好」，名字不改；A-06 不立则不动。类别含义、随 A-06 |
| A-27 | 推翻 | `context.md` §萃取与压缩 原句「顺序也随之定下：prompt 的稳定部分……在前，高频变动的萃取讨论在后。语义上该这么排，推理服务的计费与缓存也偏爱这么排，两个理由不打架」。这是给一个排序决定补第二个理由，属 M2 论证；路由表 ② 是「混合」文风，且 0.4 明说「③ 里出现『为什么这样更好』的论证，说明该论证应该在 ① 或 ②」——② 正是论证的家。「推理服务缓存偏爱稳定前缀」是外部环境的事实，不是 HCTL 的实现细节；CONSTRAINTS.md 那条「实现细节不构成对架构判断的反驳，只能作为脚注」管的是拿实现去反驳架构，这里是支持。作者自审：对自己过严 | — |
| A-28 | 修正 | 事实成立：`spec/connections.md` §连接模型 首句「连接不是一份可独立漂移的共享状态，也不需要 `Handoff` 聚合」。但它不是理由段，是排除式定义（说连接**不是**什么），紧接着就是正定义「每条连接都由『目标模块的类型化命令 + 来源模块的不可变引用』组成」；L11 的 `> 理由：` 是给「为什么这样更好」的论证用的，套不上。真问题是 `Handoff` 是个幽灵词——全库只此一处，约束层正文点名一个从未定义、也没退役记录的对象 | 发现改为：首句以一个全库仅此一见的名字 `Handoff` 做排除，读者要猜它是什么。建议改为：删掉名字，写「连接不是一份可独立漂移的共享状态，也不是独立的持久对象」；不加 `> 理由：`。类别写法 |
| A-31 | 维持 | 事实成立：总则 §词汇分类法「实现标识符另建『语义名 ↔ 标识符』对照表，以便回溯约束」；词汇表只有九个技术词的表，没有字段级对照。约束层正文里反引号小写下划线标识符去重后 30 个（`attempt_generation`、`state_version`、`completion_pending`……），「几十个」大致对。承诺未兑现，二选一进裁决包 B 档合理 | — |
| A-32 | 维持 | 总则 §六族规则「族的共同语义只在这里定义一次；成员在各模块约束中一行导出」、§三类数据「三条法贯穿全部模块约束，各处引用，不再各写一套」——原文如此，各模块约束确实只引用。正面样本成立 | — |
| A-33 | 修正 | 事实成立：`spec/system.md` 里 `Herdr` 11 处（组件表、信任边界第 1/3/4 条、事实权威地图、启动与恢复第 1/3/4 步，另有 `hctl2-control` 行「内含 Herdr 适配代码」与动作分类表「Herdr TUI」），`spec/participant.md` 10 处。路由表 ③ 验收「不需要知道选了哪个产品」被违反，主语改角色后含义确实不变（例：「Herdr 适配代码必须在每次输入前校验……」→「Agency 适配代码必须……」）。但判据栏的 T6 用错：T6 要求提到外部产品时声明它承担什么、不能承担什么——这些句子恰恰都声明了（「只提供 API 与原生 TUI」「普通交互输入也不是治理命令」），T6 是满足的。作者自审：`system.md` 不是他主写，但顺带自报了 `spec/participant.md` §运行时与观测 同病，诚实 | 判据去掉 T6，只留 A2；数字写明「system 11 处、participant 10 处」；建议不变，并注明 §运行时与观测 里「第一阶段的 Agency 是发布包自带的本地参考实现：它在 Herdr 外面只加……」这段是实现描述，角色化后保留「本地参考实现只在所选运行时外加技能目录、可用性申报与适配器」一句即可 |
| A-34 | 维持 | 事实成立：`spec/system.md` §安全边界 第 2 条写了「Tauri 2 按 window/webview 以 capability/permission/scope 显式声明……Electron 安全网形态下固定 `nodeIntegration=false`、`contextIsolation=true`、sandbox=true」。约束是「最小权限面、只暴露具名类型化命令、禁远程脚本」，配置项是选型后的实现，`delivery.md` §技术基线 已在讲桌面壳选型。注意镜像闭环（写作指南 0.8）：`contract-tests.md` `CT-PACKAGING` 第二条测的就是这些配置项，搬家后 CT 那条要改成引用交付文档指认被测项 | — |
| A-35 | 维持 | 原句在：`spec/system.md` §单写者「锁路径与推进机制是实现细节，不构成对外约束」、§控制面自己的存储「路径与布局是实现选择，三类存储的职责边界才是约束」。正面样本成立 | — |
| A-36 | 修正 | 主语改角色那一半成立（`spec/run.md` `Dagu` 10 处，`spec/task.md` `Vikunja` 1 处）；「Profile 三组规则搬去交付文档」那一半不成立。看原文 §Workflow 与 Run 授权：第一组「允许的图结构：外部执行、fork/join、switch、loop、dynamic fork、timer wait、noop 和纯数据转换」约束的是 HCTL 自己的中间表示 Workflow Revision，与引擎无关；第三组「dynamic fork 只能实例化 Manifest 中已冻结的有界 Seat 模板；loop 每次重新进入节点都创建新 Obligation」是 Seat 与 Obligation 语义，是 Run 模块最核心的约束之一（对应 CT-RUN「dynamic fork 超出冻结 Seat 模板……时拒绝」）；第二组「编译器拒绝的副作用：子 DAG、默认 command/script、HTTP/action/agent/Harness」是「引擎只拥有机械状态」的机械保证，也是引擎无关的；只有「Dagu `human.task` 仅作被动检查点」半句是引擎特有。把三组搬去非规范的交付文档，等于把 Run 约束移出约束层，那是含义变化，不是写法。判据 T6 同 A-33 用错 | 发现改为：`Dagu`、`Vikunja` 当约束主语（A2）。建议改为：主语改「workflow engine」「引擎适配器」「任务后端 webhook」；Profile 三组规则留在 `spec/run.md`，只把「Dagu `human.task`」改「引擎的被动检查点原语」、「生成并验证 Dagu YAML」改「生成并验证引擎定义」；产品名只留对齐表。类别写法 |
| A-37 | 修正 | 事实成立：两张状态表没有超时列。但超时语义并非全无——Attempt：`spec/run.md` §写入约束「超时与备用候选准入只依据账本自己的 Obligation 截止时间」，§Request、重试与 Gate「Request 冻结截止时间与 `fail｜cancel` 默认策略……过期……按冻结策略结束对应 Attempt、Seat 或 Obligation」；Run 过渡态（启动中／取消中）的等待靶是外部副作用的确认回执，`spec/system.md` §外部权威副作用「投递超时或确认回执丢失时，结果保持未知，并继续占用冲突范围」已定义。所以大部分是「定义在别处、表里没指」，是写法；真正没写的只有一句「Run 过渡态没有墙钟超时，只靠取消／失败／替代离开」 | 类别改为写法为主：两张表加「超时」一列或表下一句总纲，指向 Obligation 截止时间、Request 截止策略与外部副作用确认回执规则；「是否要给 Run 过渡态加墙钟超时」单列进裁决包 B 档 |
| A-38 | 修正 | 事实成立，但一刀切过宽。总则的规则是两句：「状态值使用中文语义名。实现标识符另建对照表」，另有「协议或 schema 字段……需要逐字指认时，可以保留原形并用代码格式标示」。所列词分三类：（1）`completion_pending`、`known/unknown`、`accepted/rejected/changes_requested`、`backend_authoritative` 等在原文已是代码体，属逐字指认放行的标识符，该做的是进 A-31 的对照表，而且 `changes_requested`、`known/unknown` 在 CT 与交付层同形使用，改中文会跨四层 sweep；（2）「quorum-unreachable」「unsupported」（`spec/run.md` §Request、重试与 Gate）是裸英文混在中文句里，既非代码体也非中文，这才是要改的；（3）Run／Attempt 的生命周期状态原文已是中文（启动中／运行中／……），规则在生命周期上是遵守的 | 发现改为：约束层里生命周期状态用中文、其余枚举值用英文标识符，两种口径并存；其中「quorum-unreachable」「unsupported」是裸英文。建议改为：裸英文两处改代码体或中文；代码体标识符不改，登记进 A-31 的对照表；是否把 Gate 票值、写入权模式也改中文语义名，随 A-31 一起裁 |
| A-39 | 维持 | `spec/task.md` §启动 Run 的前置与排序令牌 第三段原文一字不差；一段十来个英文名词。重写稿把 Start／Complete／Adopt 改成「启动、完成、采纳」与全库命令名口径（「完成 Task」「采纳契约」「启动 Run」）一致，含义不变。备注：这条判据全是 W2／L4／L7，是写作轴的发现，编号归 W 轴 | — |
| A-40 | 维持 | `spec/task.md` §契约与来源「Task Revision 契约按需创建，但只能由显式『采纳契约』命令……产生」；`task.md` §模块拥有什么「契约在首次绑定 Run、首次提交完成命令或显式升格时才创建」。两层一致，正面样本成立 | — |
| A-41 | 维持 | `spec/project.md` §Context、Memo 与 Artifact 里 `small-brain` 四处（第 112、114、118×2 行）核对无误。随 W-10。备注：判据 W3，编号归 W 轴 | — |
| A-42 | 修正 | 事实成立，数字与范围要改。`spec/project.md` §对象 表是 10 行（不是十一）；Context Manifest / Context Bundle 在总则词汇索引里列在「票据与规格」，Chat 端口绑定是 Binding 族成员，与领域对象同列无标注。同病不止此文：`spec/participant.md` §对象 的「Attach Descriptor / Terminal Input Lease」「Result Proposal / Evidence」，`spec/task.md` 的 Task Source Snapshot、Task Completion Receipt 也没标族或类别。而 `spec/run.md` §对象 已有现成做法——行内括注「（Revision 族）」「（Binding 族）」「（引用格式）」 | 发现改为：三个模块约束的对象表把票据、族成员与领域对象同列无标注，只有 `spec/run.md` 行内标了族。建议改为：其余三表照 `spec/run.md` 的写法行内括注类别或族，不另加列；含义不变 |
| A-43 | 维持 | `delivery.md` §选型判据 首句原文如此：「实现名只出现在本文与[实现证据]，设计层正文只用系统角色名」。规则在、执行不到位，A-08／11／18／20／23／33／36 都以它为据。维持 | — |
| A-44 | 修正 | 随 A-17 修正：`delivery.md` §明确不做 是「第一阶段不做」清单，非规范，不能当架构层引用的权威落点；0..1 Task Revision 的权威在 `spec/run.md`、`spec/task.md`。它可以是「第一阶段限制汇总表」的候选位置之一，但汇总表放哪层是 A-17 的裁决内容 | 发现改为：§明确不做 已是「第一阶段不做」的集中处，但它是非规范复述，架构层引用不能只指这里。建议：随 A-17 裁；若做汇总表，表内每行指回约束层条款 |
| A-45 | 修正 | 「没有针对 Request 解决路径的失败用例」字面成立，但覆盖已在：`contract-tests.md` `CT-PROJECT`「chat server 中的普通消息、反应或自动化不能成为命令」——「解决 Request」是类型化命令，被这条概括；`CT-PARTICIPANT`「Result Proposal 通道提交不了治理命令」覆盖了建议里「Result Proposal 提交『解决 Request』时拒绝」的一半；`CT-WORKBENCH-IA`「同一 Request ID 跨 Room/Task/Run 聚合且不能从聚合面直接改状态」覆盖聚合面。所以不是缺口，是「最诱人的那条路要不要单列一条」。另：这条判据是验证覆盖，属 I 轴 | 发现改为：Request 解决路径由三条通用用例概括覆盖，没有专门用例。建议改为：可选加一条专门用例（写法），不是覆盖缺口；编号归 I 轴 |
| A-46 | 维持 | `doc-discipline.md` 第四条原文列出十个有自然译名的词并规定「每个文档第一次出现时给对照」，其余「在术语对照表定义一次即可」，注明所有者裁定 2026-09-02。维持。备注：判据 W3，编号归 W 轴 | — |
| A-47 | 维持 | 路由表「参考」层允许词汇为「全部」；来时路 §35 台账 v0.11.1 行本身就是驼峰名的历史记录。维持。备注：判据 W1，编号归 W 轴 | — |

### 作者自审小结

九条涉及主评审员本人文件的发现里，没有一条在开脱：A-08、A-11、A-15、A-23 都自报了「是我写的／漏改的」。相反有四条对自己过严——A-24 用「约束层也有」当删除尺子会误删已过 S1 第二关的人话资产，A-26 要改自己文件里与研究文献同名的既有尺子，A-27 把一个合规的 M2 论证判成越层，A-23 要把角色化后仍属架构层的分工表整张搬走。A-04 与 A-14 则是在没提的情况下重开所有者 2026-09-02 刚裁的一句话（来时路 §34），这是纪律问题而非护短。

## 主评审员漏掉的 A 轴问题

查过愿景层（`vision.md`、根 README）与架构层七个文件全文，约束层六个文件全文，词汇表与总则。以下是清单里没有的：

| 编号 | 位置 | 问题 | 判据 | 建议 | 类别 |
| --- | --- | --- | --- | --- | --- |
| L-1 | `participant.md` §Terminal 场景 恢复等级表；`spec/participant.md` §终端通道、连接与租约 首句 | 五个恢复等级（exact attach、native handoff、structured inspect、semantic resume、replay）的**定义**在架构层正文的表里，约束层反过来写「定义见[设计正文](../participant.md#terminal-场景)」，`contract-tests.md` 也链到设计正文。文档纪律：「只有约束层的四个模块约束可以定义模块特有的领域名词、状态」「一个概念只在拥有它的模块完整定义一次」；这里权威方向倒了。五个名字还全是英文、无中文对照（顺带 W2）。这是评审员自己写的文件 | A2、T4 | 定义表搬到 `spec/participant.md` §终端通道、连接与租约；`participant.md` 留产品语言的一句话每级加链接。词汇表登记五级中文对照 | 写法（搬家不改义） |
| L-2 | 架构层 5 处（`project.md` 2、`participant.md` 2、`context.md` 1）、`spec/project.md` 3、`delivery.md`、`contract-tests.md`、来时路 | `Trigger Preview` 是全库用了 12 次的具名步骤，不是核心产品词，不在六个高频约束词里，词汇表**没有词条**，任何一层都没有一句定义。它其实是 `spec/system.md` §场景端口 的 `Preview(command draft)` 用在调用命令上的那一次 | A7、A3 | 词汇表登记「Trigger Preview｜调用预览；对『发起调用』命令做的 Preview，不是新命令」；架构层可改写「调用预览」 | 写法 |
| L-3 | `vision.md` 第 98、102、106 行 `control`；第 117、122 行 `actor` | 与 A-02 同类的愿景层越层词，评审只抓了 `outbox`。`control` 是组件简称（`hctl2-control`），`actor` 是命令信封字段，都不是「核心产品词 + 日常语言」；日常语言有现成的「控制面」「谁在做」 | A2 | 三处 `control` 改「控制面」；两处 `actor` 改「操作者」或「谁在做」 | 写法 |
| L-4 | `glossary.md` §核心产品词 表 与 `spec/README.md` §核心产品词 | 词汇表标题叫「核心产品词」的表有 28 行，比总则的 23 个多了 Agent、Harness、Agency、worker、Chat Room；总则明文把 Agency、worker、harness 归「六个系统角色名」、Agent 归「泛称」，Chat Room 则根本没列（四个场景名里 Kanban、Workflow、Terminal 在核心产品词内，Chat Room 不在，只有 Room）。分类法在两个文件里不一致，A-22 的清点用「去掉 Agent 后」绕过去了，没报 | A7、A2 | 词汇表把那五行拆成「系统角色名与泛称」小表；总则决定 Chat Room 是否与其他三个场景名同等进核心产品词（现状是三进一出） | 写法（分表）＋含义（Chat Room 归类，进裁决包 B 档） |
| L-5 | `architecture.md` §模块交接 首句 | 「模块之间的每次交接都交付一个冻结的、带摘要的不可变对象」——对照 `spec/connections.md` 总表，八条边里「Task/Run/Participant → Project」递的是有序事件（恢复依据「source event cursor，可从源账本重建」），「完成 Task」递的是命令加证据引用；首句以「每次」概括，被自己的总表证伪。与 A-09 同源 | A5 | 改「每次改变事实的交接都交付……；事件回流只是投影」；随 A-09 一并落 | 写法 |
| L-6 | `context.md` 4 处、`spec/project.md` §Context 1 处、`spec/task.md` §契约与来源 1 处 | 「组装器」被当作一个动作主体（「由组装器机械触发」「组装器按当前 Snapshot 的引用……冻结进 Context Manifest」），但 `spec/system.md` §组件 表里没有这个组件，词汇表也没有。按 `spec/project.md` 写入约束「Project control 按获准来源、scope、权限和预算物化」，它是 control 内部物化 Context Bundle 的那部分。一个没登记的组件名，读者会以为是第八个组件 | A1、A7、L2 | 首现处写「组装器（control 内负责物化 Context Bundle 的部分，不是独立组件）」并进词汇表；或约束层直接写 control | 写法 |
| L-7 | `spec/run.md` §外部概念对齐 表「引擎外部检查点」行 | 同文件正文七处都叫「Engine 检查点」（S1 已裁词也定「引擎侧具体对象仍叫 Engine 检查点」），对齐表这一行独自写「引擎外部检查点」。一物两名 | A7 | 对齐表行改「Engine 检查点」 | 写法 |
| L-8 | 全库 `control` 与 `hctl2-control` | 「control」作组件简称在架构层与约束层用了上百次，`spec/system.md` §组件 表登记的是 `hctl2-control`，词汇表**没有** control／hctl2-control／控制面服务三者的映射（对 `hctl2-tool`↔工具箱做了，对 control 没做）。L3 明文：「一个概念确实需要两种写法时……必须在词汇表里显式声明它们是一对一映射」 | A7、L3 | 词汇表「系统组件与常用技术词」表加一行「`hctl2-control`｜control；散文里的 control 指这个组件，『控制面』指它所在的面」 | 写法 |

没找到的方向也说一下，免得重查：愿景层 §四个阶段、§两种控制制度、§三类数据三节的概念都能在约束层找到家，没有多出的名字；架构层四份模块正文的「模块拥有什么」与 4×3 归属矩阵除 A-12／A-15 已报的两处外一致；约束层 Terminal「既是场景也是接口（participant.tui）」的双重身份是来时路 §34 所有者明裁，不算一名两物。

---

**统计：维持 18 / 修正 24 / 推翻 3**（共 45 条；A-29、A-30 不存在）。漏项 8 条（L-1 至 L-8）。
