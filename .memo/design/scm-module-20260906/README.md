# 代码变更与集成模块（命名候选见 `01` 第十一节）· 状态板

> 状态：待各家审 · 方向已定（所有者 2026-09-06：显式进设计，立为第五模块，纯本地集成有正式方案）；v2 按 Codex 主审意见修订，v3、v4 记入所有者对七项的表态（一项已拍板、两项有倾向未拍板、一项待定、命名交各家挑、两项交各家讨论），PR #184 不自合<br>
> 基线：main @ `736f577`（草案 v0.16.5）<br>
> 去向：v0.17.0 约束批（新增设计正文与模块约束各一份，愿景、架构、四份模块约束的分责调整、系统边界、连接、契约测试、交付文档、词汇表、决策史新一节），文件清单见 `01-scm-module.md` 第十二节

| 文件 | 作者 | 内容 |
| --- | --- | --- |
| `00-gpt-scm-provider.md` | GPT（#182） | 事实核查、谁负责什么、可替换接口不等于可迁移历史、现有评论流程不是自动 Gate；「不意味着第五个模块」一句已被所有者方向覆盖，逐条核销见 `01` 附录三 |
| `01-scm-module.md` | Fable | 第五模块的完整方案，v2：按治理责任立模块（不按客户端）；工具箱总在、平台按仓库绑定、集成路径按目标选、能力声明不替人改契约；对象迁移表与 PR 映射的更新规则；代码评审场景与评论经代取回到作者；端口操作面、目标保护、源头与目标头之别；B2 在受保护 `main` 上的十步；连接；事实权威与按目标恢复；身份与安全；命名；v0.17.0 文件清单；契约测试补核；与 P2 的顺序；七项待拍板。附录：操作后端四级顺序、署名关卡、#182 核销 |

流程（所有者 2026-09-06 定，第 3 步的顺序按 Codex 收束调整）：

| 步 | 谁 | 做什么 | 状态 |
| --- | --- | --- | --- |
| 1 | Fable | 落备忘 v1（自合） | 完成，PR #183 |
| 2 | GPT（以 Codex 主审身份） | 审 `01-scm-module.md`，给意见 | 完成：[PR #183 评论](https://github.com/yesme/hctl2/pull/183#issuecomment-5553670183)。方向维持；推翻三处论证（原生客户端推导、本地驱动等同、执行体直推远端），修正约三十处，重点是自动降级、B2 前半程、目标头保证、结果准入连接、凭据许可 |
| 3 | Fable | 按意见修订为 v2，逐条回应写在 #183 评论；v2 作为**不自合**的 PR 交所有者确认与四家审。约束批放到 v2 审定之后：Codex 的收束是「先修缺口再写正式约束」，我同意，四家审提案里的取舍比审二十几份正文的字句更值 | v2 PR 已开 |
| 4 | 所有者 | 逐项表态 `01` 第十五节的七项 | 完成（2026-09-06）：GitHub 缺省且本批只做它已拍板；Write Lease 甲、发布授权甲加开关是倾向、未拍板；决策史标题形式待定（倾向去序号留版本号，等看代价）；命名交各家挑（模块与场景不同名）；结晶归属与目标头策略交各家讨论。v4 已照改 |
| 5 | Codex、Grok、GLM、Antigravity | 各审一份，逐条维持 / 修正 / 推翻，评论写在 PR #184；另请挑命名组合（第十一节）、对第十五节 4、5 两项表态 | 待开始 |
| 6 | Fable | 处理评审意见，合入 v2；再出 v0.17.0 约束批（不自合，再审一轮）；P2 计划的 B1、B2 引用它 | 待开始 |

表态记录见 `01` 第十五节：已拍板一项（GitHub 为缺省实现、本批只做它、不点名第二个）；有倾向未拍板两项（Write Lease 随 ChangeSet；发布评审在 Trigger Preview 授权加「须人显式确认」可选开关），交各家审后再定；待定一项（决策史标题形式，两种做法与代价见 `01` 第十二节）；命名交各家挑（候选与理由在第十一节，模块与场景不得同名）；结晶归属写法与目标头无保证时的策略交 GPT 与各家讨论。
