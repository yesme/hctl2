# S2 三问闸

> 状态：讨论中 · S2 闸门报告（只列表，不改总图、不改 `docs/**`）<br>
> 基线：main @ 156a5ec（草案 v0.15.0）<br>
> 对象：[02-target-map.md](./02-target-map.md)<br>
> 对照：[施工图](./README.md) §2 红线、§6 S2 闸门；[01-inventory-dead-text.md](./01-inventory-dead-text.md) 最危险三条；[parking-lot.md](./parking-lot.md)<br>
> 去向：所有者一次裁决后冻结 `03-approved-plan.md`；本文件不进合同<br>
> 结论取值只能是：通过 / 回滚哪几行 / 进停车位

## ① 四模块 × 三面 × 三条法；有无第五模块、新聚合、新具名概念

总图 §1 目标树仍是 Project / Task / Run / Agent 四模块设计正文 + 四模块合同；architecture.md 仍只负责三个面；三类数据与三条法的唯一权威仍在 `spec/README.md`（§1 职责表、§3.12 三类数据 keep、§6 提纲）。participant / context 仍标横切、明确不负责规则权威。新增的 `contract-tests.md` 是从 delivery 拆出的 CT 八族（施工图拍板点 10），不是领域模块，也不定义对象。§4.4 把「Task 完成两来源」收成第四组同构，是对已有合同句的收束，不是新具名概念。glossary 的 rewrite 限定「新增词条只对应 v0.15.0 动作分类」，未开白名单外的名字。

未见第五模块、未见新聚合、未见新具名对象。

**结论：通过。**

## ② 四组同构合并与迁移表是否并丢唯一 writer / 单一权威 / §5 模块差异

逐组：

**§4.1 E2EE。** 权威钉在 `spec/project.md`「Room 与消息」。写入合同仍留 fresh 回读句；connections 失败表「已绑定房间被开启端到端加密」行保留为可观察结果唯一登记处。§5 要求「chat server 不可用」与「房间事后加密」两行都留——总图未把前一行标掉。未并丢。

**§4.2 三条底线。** 权威钉在 `spec/agent.md` 写入合同段，并写明「不可删」。`spec/system.md`「命令与跨服务正确性」保留两类 actor 来源（system 自己的权威），只删对底线的复述；§3.18 对同一节只删「第一阶段不设额外的用户在场证明」半句。与死文普查最危险第一条、第二条一致。未并丢。

**§4.3 Herdr 能力限制。** 能力条件句权威在 `spec/agent.md`「运行时与观测」；v0.8.2 缺项清单只在 delivery P0 第 2 项。设计层限制清单四条改为指向 P0 的一句，合同条件句保留。§5 与最危险第三条都禁止把合同「无则降级」当 P0 脚注删。打包策略（§3.19）只改 Tuwunel / macOS 15 两句，不删「必须原生、不进 Docker」。未并丢。

**§4.4 Task 完成两来源。** 权威钉在 `spec/task.md:68`。Run 侧 `completion_pending` 留在 spec/run；system 留两类 actor 总则，终结来源句改引用。§3.14 只把「裸终态不是命令」一类负例按 N 改正面，两个来源本身标承重保留。未并丢。

迁移表其它 merge/rewrite/fold：Run Manifest 两份清单先并集再引用（§3.17、§5）；usage 源码构建要写入例外条件、不能只删句（§3.2、§5）；decision-history 已取代节 fold 不删。§5 所列 Project / Task / Run / Agent 独有项在 §3.6–§3.16、§3.13–§3.16 均为 keep 或「承重留」，无整项 merge 进共享机制。

本问不要求回滚总图任何一行。施工时仍须按 §5 逐条核对，那是 S3 删除安全审计的范围。

**结论：通过。**

## ③ 停车位未决是否写成必做施工

[parking-lot.md](./parking-lot.md) 三项：

1. **Herdr `managed_single_writer` 暂不支持 vs 做隔离。** 总图只把 v0.8.2 点名改成能力条件句，并全部挂拍板点 11（措辞、语义不变）。未要求提供或验证隔离机制。不是把本项写成必做。
2. **`hctl2-tool` 收窄。** 总图无对应施工行。
3. **backlog 13 条中的语义改判。** §3.24 其余 11 条指向停车位第 3 项；本轮只按 N 处理措辞。Project 归档、契约与来源等节 keep。不是把语义改判写成必做。

另有一条**不在停车位、却写成了必做改写**：§3.4「数据丢了怎么办」要求补「路标停更时新义务铸造暂停」半句，并自称事实修正、不改语义。现行 `architecture.md`:89 与 `spec/system.md`:174 只写路标停更待对账、不拦判决；`spec/run.md` 铸造条件是「观察到 Engine 检查点进入等待态」。总图这半句是新的显式规则，红线 1 要求语义疑点进停车位，不得当本轮施工。

**结论：停车位 1–3 通过；§3.4「数据丢了怎么办」补「路标停更时新义务铸造暂停」半句 → 进停车位（议题：路标停更时是否暂停新义务铸造；现行合同未写这句，与「不拦判决」并读的边界未裁）。**
