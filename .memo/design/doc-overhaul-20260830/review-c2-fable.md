# C2 设计层:Fable 整体通读

> 状态:讨论中 · C2 闸门报告(只列表,不改文件)<br>
> 对象:PR #82 `codex/doc-overhaul-c2-design` @ 通读时 head(docs-check 绿,CI gate 绿)<br>
> 对照:[`03-approved-plan.md`](./03-approved-plan.md) §3 C2 行、[`02-target-map.md`](./02-target-map.md) §3.3–§3.11/§3.20/§4/§5/§6、[`04-prohibition-whitelist.md`](./04-prohibition-whitelist.md)、[`05-vision-draft.md`](./05-vision-draft.md)<br>
> 去向:所有者逐项裁决后交 GPT 一次修正;本文件不进合同<br>
> 只答施工图 §6 S4 规定的六问:主线是否清楚、文件职责是否自然、章节比例是否失衡、读者能否从愿景走到合同和交付、是否仍见演化补丁痕迹、GPT 是否在某局部堆料。不评字段级正确性(那是 Grok 抽审与合同层的事)。

## 结论

**结构级通过。**十个文件 952 行(上限合计 1085),主线「意图 → 承诺 → 治理 → 运行」从 vision 到设计地图到四模块正文一路读得通;每个文件的职责与总图 §1 职责表一致,没有一个文件在替 spec 定义对象;设计层禁令 64 → 24,已低于白名单 §3 的 ≤45 上限;四组同构在设计层都收成了「一句人话 + 指向 spec 权威」;§5 的模块差异与五个易混边界逐条在原位;WezTerm 点名、Herdr 限制清单、angle 表「不能」列、run.md 控制制度并入 vision 均按冻结方案落地;没有新概念、新禁令,没有局部堆料——GPT 这次的病没发作,反而有两处删过头(见下)。

按严重度排序的发现只有四条,前两条需要裁决:

## 发现(按严重度)

| # | 严重度 | 位置 | 问题 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | 中 | `vision.md:146–153` 设计原则 5、6、10、11、12 | 五条原则被削成只剩标题句(「5. 机械动作必须确定性。」后面什么都没有),连「细则见设计地图共同规则」的指向也删了。冻结方案说的「只留标题句」意图是标题 + 指向,不是断头;现在 15 条里 5 条是残章,读者不知道去哪找细则,原则列表的整体性被破坏 | 每条留标题 + 半句指向(「——细则见[设计地图](./README.md#共同规则)」),或在列表前加一句「第 5、6、10、11、12 条的细则只在设计地图共同规则定义」。二选一,不加回原文 |
| 2 | 低–中 | `glossary.md` Agent 行 | 中文对照由「执行治理模块」改为「执行实现模块」。术语表非规范,但改一个核心产品词的中文对照是词汇裁决,超出总图 §3.20 授权(只允许删退场词条、按 v0.15.0 动作分类增补) | 回滚该格,或所有者当场裁决采用新对照(若采用,C5 台账记一行) |
| 3 | 低 | `README.md:92`(设计地图支持文档) | 「契约测试矩阵」条目指向 `delivery.md#契约测试矩阵`——正确,但 C4 拆出 `contract-tests.md` 后要改链 | 不用现在改;记入 C4 卡的待办 |
| 4 | 低 | `architecture.md:21` | 「打开仓库」段改写后少了「必要时拉起本机控制面」这半句产品行为(原 :23) | 可补可不补;若补,一句话内 |

未发现问题的核对项(供 Grok 抽审时省力):

- **主线与阅读路径**:根 README 三条路径 → vision → 设计地图 → 四模块 → 横切 → delivery,链接均指向现存锚点;设计地图「共同规则」八条每条一句、承重的三条(完成来源、临场边、单写者)各带指向。
- **文件职责**:vision 不定义对象;architecture 只讲三面/三类/替换边界/丢失;四模块正文五节结构同构(为什么存在 / 拥有什么 / 关键规则 / 场景 / 交接)且「模块交接」节仍只列方向;participant/context 关键规则全部改引用式,「为什么」与分工表原样保留。
- **章节比例**:vision 162 / architecture 97 / 地图 96 / project 75 / task 68 / run 61 / agent 84 / participant 54 / context 113 / glossary 142;没有一个文件被削到失去解释力,也没有一个在膨胀。
- **红线 4**:五种失败模式、目标体验八步与六问、无 Run 轻量路径的产品标尺、Kanban「Board 不说谎」、Agent「为什么存在」的观察阶梯——全部在。
- **§5 差异**:Run 失败类终态不终结 Task 与 Task Reopen/Deleted 只作来源事实分开(task.md:23/:26、run.md:39);三种排他(ChangeSet lease / Task Run claim / Agency owner)在地图 :59 一句概括、精确范围指向 system;Dagu 直接 mutation 只回读为分歧(run.md:37)与 Vikunja Done 可成完成请求(task.md:23/:49)并列且原因各自写清;Harness 可读 Git 与集成凭据代用同在 agent.md:21/:27。
- **演化补丁痕迹**:未见现在时的 agentd/tmux/Zellij/Conductor;Herdr 在设计层只出现在「第一阶段默认实现」语境;v0.8.2 缺项清单已从 agent.md 移出,只留一句指向 delivery P0。
- **版本戳**:十文件保持 v0.15.0,与 GPT 的说明一致(C5 一次升 v0.15.1,提前升会被 `check_version_stamps` 拒绝);总图 §3.10/§3.11 的「改 v0.15.1」行在 C5 兑现即可,不算偏离。

## 给 Grok 抽审的提示

C2 没有触及合同,抽审重点只有两处删得多的地方:四模块正文「关键规则」里被改成正面句的否定句(是否有把 A 类合同定义句当 C 类删掉的——按我读,没有,权威都在 spec,设计层原本就是复述),以及 agent.md:23「观测用于触发关注;领域结果只经提案与上层准入形成」是否仍守住「观测无论多可信都只是观测」的原意(我判断守住了,措辞更正面)。
