# S4 收口:DoD 核验表

> 状态:待终审 · Fable 依施工图 §13 逐条附证据;所有者终审通过后本目录状态改「已收口」<br>
> 基线:main @ `5294dbf`(草案 v0.15.1,S4 修正 PR #100 合入后的终态)<br>
> 去向:所有者终审;终审后施工图与 `03-approved-plan.md` 状态行改「已收口」,停车位四项转后续立项清单<br>
> 证据来源:四份 S4 报告(`review-s4-{grok,fable,glm}.md`、PR #100 修改清单)、五簇 PR(#80/#82/#88/#91/#95)、收口 PR(#96–#101)、CI docs-check

## 逐条核验(施工图 §13,共 8 条)

### 1. 新读者少量跳转理解全貌;三条阅读路径可走通 — ✓

- ④ `review-s4-fable.md` §2 对新读者/实现者/adapter 三条路径逐站实测,全程无断站、无循环依赖;根 README 76 行,两跳内可达任一权威定义。
- ① 修正后(README 三目标发布、设计地图两处旧称、delivery 精确锚点)由 CI links/anchors 检查复核:PR #100 验证 Pass 5 / Fail 0,覆盖 80 个 Markdown 文件。

### 2. 每文件唯一职责、不互相越权;一个事实一个权威 — ✓

- ④ 六问②:各文件定位行互不越权;四组同构的权威位各只一处(加密=spec/project「Room 与消息」、三条底线=spec/agent 写入合同、能力条件句=spec/agent「运行时与观测」、完成两来源=spec/task 写入合同),其余均为引用。
- ② `review-s4-grok.md` §①②:合并未吃掉模块差异,31 条 B 类必留与 A 类 15 处点名全在权威位置。
- ① 一致性通过修掉最后 8 处交界残留(含 context.md 设计层不引合同格式名、来时路 :58/:66 改历史结论 + 链现行合同、GLM 高 2 条合同层拼接复述删并)。

### 3. 迁移表全覆盖;删除/禁令可指认判据;禁令总数净减 — ✓

- ② §①:`192e7b6..main` 文件级删除仅 2 处且均在表内(implementation-evidence delete、remote-control split);逐节核销未发现表外无记录删除;每簇 PR 描述附迁移表编号(#80/#82/#88/#91/#95)。
- 禁令密度终值(逐次计数,S4 修正后):设计层 25(上限 45)、合同层 150(上限 150)、delivery 9、usage 2、根 README 0,合计 186;contract-tests 28 按白名单 §3 单列。对基线 320 净减 **-42%(186/320)**;白名单全局目标 ≤245 达成(186+28=214)。

### 4. 退场组件不在现行规范;与代码/lock/平台一致 — ✓

- ④ §1-⑤:现行规范与门面层对 agentd/tmux/Zellij/Conductor/WezTerm 现在时扫描为零;死名扫描常驻 CI。
- 事实一致:Tuwunel 用托管制品(C4 修正)、macOS 15 依据改随包 Mach-O(delivery:134)、根 README 三目标发布与四类打包依赖(① 修正)、usage 源码构建仅限托管 Tuwunel 制品更新(② §④ 复核)。

### 5. 机械检查全绿常驻 CI;行数无超限或已论证 — ✓

- `src/build/docs/` 五项检查(链接锚点/版本戳/死名/禁令密度报告/memo-review 基线)为 required `docs-check`,C1 起挡每个 PR;终态验证 Pass 5 / Fail 0。
- 行数对 §10 上限:全部达标(spec/system 恰 200);唯一超限 delivery+contract-tests 合计 294(预算 220),已在 PR #91 论证「保真优先于预算」并经所有者接受。

### 6. Fable 复审与 Grok 审计逐项裁决并应用;四份种子台账核销或显式留痕 — ✓

- S4 四清单经所有者一次裁决(09-s4-cards.md「所有者裁决(2026-08-31)」),GPT 于 #100 全部应用;C2/C3/C4 各簇裁决记录在对应 review-* 文件并已应用。
- 种子台账:`doc-cleanup-backlog` 13 条经 S1 禁令盘点逐条裁决、C5 标注核销;`glm-20260830a` §三 A 与 `grok-20260830a` 的死文种子经 S1 普查→S2 迁移表消化;grok 报告中「设计收笔、转 P2/B0」的工程排期建议**不属大修范围,显式留在 `.memo/README` 待拍板表**;`v07-v012 retrospective` 状态已落地,其缺口审计作 S2 输入。

### 7. 台账一行、版本戳 v0.15.1、待拍板表刷新、全簇 PR 经 required CI 合入 — ✓

- decision-history §32 大修台账行(v0.15.1);全库版本戳 v0.15.1 一致(CI stamps 检查);`.memo/README` 待拍板表已刷新(大修行现为「施工收口中」,终审后改「已落地」并删行);C1–C5 与收口 #96–#101 共 11 个 PR 全部经 required CI gate 合入 main。

### 8. 最终标尺:更短、更易进入、更难被局部修订破坏 — 达成,但「更短」有限,如实报告

- **行数**:规范与解释层 2936 → 2773(净 **-163,-5.6%**);`docs/ + README` 全量 diff 为 +637/-886(净 -249)。收缩集中在门户与总则:根 README 181→76(-58%)、spec/README 142→84(-41%)、设计层正文 837→810;delivery 拆分后 294(+4,保真接受)、来时路 +48(五张表迁入,历史归位不是新增)。**④ 报告中「全库净减约 700 行」为高估,以本表 git diff 数字为准。**
- **更易进入**:三条路径逐站实测走通(④ §2);任一事实两跳可达权威。
- **更难被局部破坏**:权威唯一化 + 引用结构使单点修订波及面收窄(② §② 必留终盘背书);五项机械检查常驻 CI 兜底链接/版本/死名。
- **不是搬家加索引**:唯一「搬家」是五张核销表迁入来时路,属拍板的历史归位;其余为删(-886)、合并、改写与能力条件句化。是否满足「明显更短」由所有者终审裁定;若裁定不满足,残余候选是停车位 #2/#3 涉及的语义级收缩(另立项)。

## 停车位转后续立项(交所有者排期)

1. Herdr `managed_single_writer`(语义:单写 gate 缺口标注 vs 提供隔离机制)
2. `hctl2-tool` 范围收窄(语义)
3. backlog 13 条中的语义改判项(逐条立项、各自 bump)
4. 路标停更时新义务铸造是否暂停(S2 三问③移入)
