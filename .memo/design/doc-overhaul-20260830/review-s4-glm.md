# S4 中文二读报告（GLM）

> 状态：待裁决 · 四份 S4 清单之一，交所有者一次裁决，被接受项由 GPT 合成修正 PR<br>
> 基线：main @ 95cadc4（草案 v0.15.1）· 只读，未改任何文件<br>
> 范围：docs/design/**（含 references）、根 README.md、docs/usage.md 全文通读<br>
> 口径：只挑中文可读性——重复、病句、指代不清、英文夹杂失控、首现专名缺中文对照、大修拼接处的断裂句；不评含义、不提结构。两条疑似事实问题已显式标注「抄送①」。

共 17 条：高 2、中 7、低 8。

## 高（合同层同文件重复，疑似 C3 拼接缝）

| # | 位置 | 原句 | 问题 | 建议改法 |
| --- | --- | --- | --- | --- |
| 1 | spec/agent.md:68 与 :74 | :68「Agency 若声明栅栏回显能力，必须原样携带并回显请求所附的代次与租约引用，并拒绝不匹配项；未声明栅栏回显的 Agency，第一阶段只在 HCTL 入口校验。」:74「Agency 声明栅栏回显时，原样携带并回显代次与租约引用，拒绝不匹配项；未声明时只在 HCTL 入口校验，物理 fence 记为未生效。」 | 同一规则在 6 行内完整复述两遍——:74 是能力条件句化的新段落，:68 的旧句未随之删除，典型的合并残留 | 删 :68 该句（保留本段「Agency 合同永不包含治理权威」主句与后续「原生输入…」句）；栅栏回显规则只在 :74 的能力条件句段落定义一次 |
| 2 | spec/system.md:169 | 段中「不接收也不回显 HCTL generation 的 Agency，其 terminal 动作按未取得物理 fence 的低信任观测记录」；段末「Agency 接收并回显 generation 时可把规则落实到物理执行点；不接收也不回显 generation 时，第一阶段只在 HCTL 入口强制，绕过入口的动作按未被物理 fence 的低信任观测记录。」 | 同一段内「不回显 generation → 入口强制 + 低信任」说两遍；且「每个 Agency binding scope 在账本中同时只有一个 owner lease…」句被夹在两次复述之间，行文顺序被打乱 | 段中句删去或并入段末句；scope 句紧随「每个 Repo Instance…」句之后 |

## 中

| # | 位置 | 原句 | 问题 | 建议改法 |
| --- | --- | --- | --- | --- |
| 3 | contract-tests.md:4 | 「本文列出八族可观察行为的失败用例」 | 实际 10 族（CT-PROJECT…CT-PRODUCT；`###` 计数=10）；拆分时计数未更新。**抄送①（疑似事实）** | 改「十族」 |
| 4 | contract-tests.md:72、75、76 | :72「…是有效运行时输入,该输入不能直接产生领域结果;Agency 未声明…」;:75「…能力,或不能证明…时,不得声称 exact attach;缺失…」;:76「…时拒绝； Agency 恢复报告…」 | 半角逗号/分号（,;）与全库全角标点不一致；:76 全角分号后多一个空格 | 统一全角标点并删 :76 空格 |
| 5 | participant.md、context.md 全文 | 如 participant.md:5「回答"谁在参与、按什么方法工作、结果归于谁"」、context.md:9「不是"把最近若干条聊天塞进 prompt"」 | 两份横切正文通篇用 ASCII 直引号 "…"，其余全库用弯引号 “…” | 全文替换为弯引号（含单引号若有） |
| 6 | references/decision-history.md:101、130、149、214、245 | 「核销记录(自 spec/README 迁入)」 | 五张迁入表引言行用半角圆括号，与全文 72 处全角括号不一致 | 改「（自 spec/README 迁入）」 |
| 7 | task.md:23 | 「能证明 owner human 明确 Done 的 binding 可生成同一个「完成 Task」请求并进入 Task 验收」 | 一句内三个无中文对照的英文术语（owner human、binding、Done），设计正文关键规则里英文密度失控；两个文件均未给 owner human/binding 首现对照 | 改「能证明是 owner human（所有者本人）明确完成 Done 的 binding（任务绑定）…」或在本文首现处统一给一次对照 |
| 8 | spec/README.md:59 | 「## 词汇索引（v0.9.1 归并后）」 | 节标题仍挂 v0.9.1 版本号；历史表已迁走来时路，标签失去时效意义，v0.15.1 读者会误读索引新旧 | 改「## 词汇索引」 |
| 9 | delivery.md:118 ↔ :122 | :118（选型判据 4）「…只验 HCTL 实际依赖的 API 与行为（第 2、3 条是选型时的资料判断，在首次消费时产品化，不进 P0）」；:122（P0 引言）「关键假设只指 HCTL 实际调用的 API 与行为。第三方自身的功能…不在 P0：要么是选型时的资料判断，要么在首次消费时产品化。」 | 同一文件两处完整陈述同一条排除规则，近乎逐句重复 | 保留 :122 的完整版（它在 P0 节内），:118 括注缩为「（边界见「开工前限时验证」）」 |

## 低

| # | 位置 | 原句 | 问题 | 建议改法 |
| --- | --- | --- | --- | --- |
| 10 | task.md:26 | 「重开开始新的生命周期版本并保留旧凭证」 | 「重开开始」双动词连用拗口 | 「重开开启新一轮生命周期并保留旧凭证」 |
| 11 | task.md:21 | 「阻塞与需要关注是正交健康标注」 | 缺「的」，「正交健康标注」读作生造复合名词 | 「是正交的健康标注」 |
| 12 | task.md:22 | 「历史版本、Run 和凭证保持只追加」 | 「保持只追加」动宾搭配生硬 | 「只追加、不改写」 |
| 13 | vision.md:13 | 「正常执行默认无界面（headless）在后台推进」 | 「默认无界面」与「在后台推进」两个状语直接堆叠 | 「正常执行默认以无界面（headless）方式在后台推进」 |
| 14 | README.md:19 | 「却没有共同连接从目标塑形、承诺追踪、施工治理到运行观察的完整过程」 | 「连接…过程」搭配欠妥（连接的是环节，不是过程） | 「却没有共同覆盖从目标塑形到运行观察的完整过程」 |
| 15 | participant.md:35 | 「评审独立性按[Run 合同]记录可认证的 known 或 unknown」 | 「记录可认证的 known 或 unknown」易误读为「可认证的 known」，且拗口 | 「记录可认证性：能机械认证的标 known，不能的标 unknown」 |
| 16 | spec/agent.md:34 | 「安全输入只有在所选接入方式声明并实现…的保证时才能启用；未声明该保证的接入方式不启用安全输入」 | 后半句是前半句的否命题复述（同一句内 A→B、¬A→¬B） | 删后半句，或缩为「未声明即不启用」 |
| 17 | 设计层多处（首现缺中文对照，一组） | provider（README.md:66、design/README.md:48、vision.md:134、project.md:56）；adapter（README.md:72、task.md:49）；ground truth（project.md:62、task.md:57）；worker（project.md:56）；shim（architecture.md:38）；local-first（vision.md:9）；outbox（run.md:31）；current pointer（architecture.md:62） | 文档纪律要求「每个文档首次出现时给中文对照」；spec/README.md:24 有定义，但设计层各文件首现处均未携对照，英文夹杂在阅读入口（README/vision）最密 | 各文件首现处补括号对照：provider（供应端）、adapter（适配器）、ground truth（事实源头）、worker（执行体）、shim（垫片服务）、local-first（本地优先）、outbox（发件箱）、current pointer（当前指针） |

## 附注

- 三组同构合并、E2EE 收束、Herdr 能力条件句化、Tuwunel/macOS 15 事实修正、usage.md 死名清理的可读性效果良好，无拼接断裂句残留于设计正文；本次发现的缝合问题集中在合同层两处（#1、#2）与拆分产物的标点/计数（#3–#6）。
- #3 为计数事实，按任务卡口径标注抄送①线（GPT 一致性通过一并处理）。
