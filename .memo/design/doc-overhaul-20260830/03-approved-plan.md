# 冻结方案:S2 拍板记录与施工入口

> 状态:已拍板 · 2026-08-31 所有者裁决「8 项都按建议来」;总图自此冻结,S3 可开工<br>
> 基线:main @ `6dd7665`(草案 v0.15.0)<br>
> 冻结对象:[`02-target-map.md`](./02-target-map.md) @ `6dd7665`;此后总图只随本文件的「拍板后修订」节改动,施工 harness 发现结构问题一律进 [`parking-lot.md`](./parking-lot.md)<br>
> S1 基建就位:K3 的五项文档检查已做成 Buck2 `sh_test`(`root//build/docs/...`),CI `docs-check` job 挂进 CI gate(PR #75);禁令盘点 `01-inventory-prohibitions.md` 已到,拍板点 7 见 §1 末<br>
> 闸门记录:GPT 保真通过(PR #74:10 处事实/出处/断链修正,「未决写成必做」0 处);Grok 三问([`review-s2-grok.md`](./review-s2-grok.md):① 通过 ② 通过 ③ 停车位 1–3 通过、§3.4 半句进停车位 #4)

## 1. 拍板单(所有者逐项填「接受 / 改为…」)

| # | 议题 | 总图默认 | 我的建议 | 裁决 |
| --- | --- | --- | --- | --- |
| 3 | 来时路已取代章节:折叠不删,§18 只折「B4 阻断」段、选型理由保留(Grok A15),§19 折叠时保留「因 tmux 二进制升 macOS 15、后被 §29 取代」的时间关系(GPT) | 折叠不删 | 接受 | **接受** |
| 8 | 目标文件树与迁移表整体(§1、§3,171 条处置;净变化 +1 −1;`docs/design/` 顶层不新增子目录) | 如总图 | 接受 | **接受** |
| 9 | `spec/README.md` 五张历史归并/清扫表去向 | 并入来时路对应章节尾(§11/§12/§14/§20/§22)的折叠段 | 接受;总则只留当前词汇法、六族、三类数据、对齐原则 | **接受** |
| 10 | `delivery.md` 拆分:CT 八族拆出为 `docs/design/contract-tests.md`;delivery 保留范围/阶段/切片/自举/选型/P0/打包/基线/未决 | 拆 | 接受(CT 是验收不是合同,独立成文后 delivery 回到 220 行内) | **接受** |
| 11 | Herdr v0.8.2 细节:合同层改四项能力条件句(栅栏回显 / 逐次输入记录 / 事件游标 / 退出停止回读),缺项清单只在 delivery P0 第 2 项与 binding 声明;13 处按 §4.3 逐处收束 | 本轮做 | 接受(措辞改写、语义不变;Grok ② 已确认不并丢) | **接受** |
| W | WezTerm 去留:agent.md:53 与 CLI 并列为一等客户端、delivery:16「可选」、根 README 未列——三处口径不一 | 需拍板 | **删点名**:agent.md 角色表改为「CLI / 其他终端客户端(凭票据 attach)」,delivery:16 保留「Herdr 官方 TUI 是原生 Terminal 客户端」并删「WezTerm 可选」;research L1 表里的 WezTerm 证据不动 | **接受(删点名)** |
| P4 | 停车位 #4(路标停更时新义务铸造是否暂停):本轮不在 architecture 补半句 | 进停车位 | 接受 Grok 判定 | **接受** |
| 12 | 版本:S5 结束一次 v0.15.1 + 来时路台账一行 | v0.15.1 | 接受 | **接受** |

其余拍板点(1、2、4、5)已随「可以开始了」按默认生效;6(死文/不一致两张清单)已由本总图消费。**拍板点 7(禁令保留白名单)**:K3 盘点已到,提案见 `04-prohibition-whitelist.md`(待拍),C2/C3 开工前拍;C1 不依赖它。

## 2. 拍板后修订(只记总图相对 PR #74 版本的改动)

| 位置 | 改动 | 依据 |
| --- | --- | --- |
| §3.4 architecture「数据丢了怎么办」 | 删去「补『路标停更时新义务铸造暂停』半句」的要求;该行只剩「留产品叙述、细则改引用」 | Grok 三问 ③ → parking-lot #4 |

## 3. 施工入口(GPT 按此顺序开簇,每簇引用总图章节)

| 簇 | 总图依据 | 前置 | 闸门 |
| --- | --- | --- | --- |
| C1 根层门面 | §3.1 根 README、§3.2 usage、§3.22 stub 删除、§3.23 research 断链行(C6–C9) | 本文件状态「已拍板」;K3 机械检查合入 CI(合入前必需) | K3 机械 + 所有者目检 |
| C2 设计层 | §3.3–§3.11、§3.20 glossary、§4.1/§4.2/§4.4 的设计层落点、§5、§6 提纲;vision.md 以 Fable 初稿为底 | 拍板点 7 禁令白名单(角色表「不能做什么」列要用) | Fable 通读 + Grok 抽审 |
| C3 合同层 | §3.12–§3.18、§4 四组全部合同层落点、§5 差异清单、Manifest 清单并集核对(§3.17) | 拍板点 7、11 | Grok 删除安全审计 + K3 机械 |
| C4 交付与研究索引 | §3.19 delivery(含 contract-tests 拆出、CT-PACKAGING Tauri 用例、Tuwunel/macOS 15 事实修正)、§3.23 research 索引与搬移 | 拍板点 10 | Grok 删除安全审计 + K3 机械 |
| C5 历史与档案 | §3.21 decision-history 折叠、§3.24 `.memo` 核销、全库版本戳 v0.15.1 | C1–C4 合入 | 所有者 |

vision.md 初稿由 Fable 在 C2 开工前交付(唯一例外,GPT 就地改事实后接管):已交付为 [`05-vision-draft.md`](./05-vision-draft.md)(162 行;相对现行删三处——三类数据缩为三句 + 指向总则、原则 6/10 缩为标题句、「从这里读下去」节删;补一段承接 run.md「两种控制制度」并入;4 处 `<!-- GPT 核 -->` 由 GPT 在 C2 判定并去标)。GPT 在 C2 以此为底写入 `docs/design/vision.md`,之后 Fable 不再改此稿。
