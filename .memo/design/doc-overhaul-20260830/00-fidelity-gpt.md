# 施工图保真通过（GPT）

> 核验基线：`origin/main @ 061bd7e`；施工图声明的正文基线为 `main @ 192e7b6`，两者之间没有 `README.md` 或 `docs/**` 变更。
> 核验范围：施工图中的文件与章节出处、显式行号、种子与台账对应关系、文件数、行数和计数。下表只列需要所有者裁决的偏差；未直接修改施工图。

| 位置 | 原文 | 问题 | 建议改法 |
| --- | --- | --- | --- |
| 文件头，第 6 行 | `notes/doc-cleanup-backlog-20260825.md`、`notes/design-doc-method-20260819.md`、`review/20260830-v0.15.0/*` | 以仓库根为基准，这三个路径都不存在；实际文件位于 `.memo/notes/` 与 `.memo/review/`。 | 分别补成 `.memo/notes/doc-cleanup-backlog-20260825.md`、`.memo/notes/design-doc-method-20260819.md`、`.memo/review/20260830-v0.15.0/*`。 |
| §1 第 11 行 | “六个 harness 各司一段” | 角色表只有 GPT、Fable、Grok、K3、GLM 五个 harness；“六份”成立的是输入建议，不是施工 harness 数。 | 改为“五个 harness 各司一段”，或明确写“六份建议由五个 harness 角色执行”。 |
| §1 第 11 行、§5 第 62 行、§14 第 298 行 | “四个拍板点” | §12 实际有 12 个编号项；施工图想表达的是 S0、S1 末、S2 末、S4 末四次裁决。当前“点”同时指 4 和 12。 | 统一改成“四次拍板（共 12 项）”；§12 继续保留 12 个编号项。 |
| §2 第 21 行 | `docs/design/**`“设计 10 + 合同 6 + references 3，约 2400 行” | 当前有设计层 10 文件、合同层总则 1 文件、模块/连接/系统合同 6 文件、references 3 文件，共 20 文件、2520 行；原文漏计 `spec/README.md`。 | 改为“设计 10 + 合同总则 1 + 合同 6 + references 3，共 2520 行”。 |
| §2 第 22 行、§8.1 第 167 行、C1 第 113 行 | “usage.md 仍写 agentd”“仍以现在时写 agentd” | `docs/usage.md` 只有第 212 行出现 `hctl2-agentd`，语义是“Herdr 不安装成 HCTL2 自建的 hctl2-agentd”，不是把 agentd 当现役组件。 | 把种子事实改成“第 212 行保留了一处退场名称的否定说明，是否保留由普查裁决”；不要写成现役死名。 |
| §6 S1 标题第 79 行 | “一天，四线并行” | 紧随其后的表有五行：机械检查、死文、禁令、不一致、权威表与架构摘要。 | 改为“五线并行”；若把同一施工员的两行合并，则表也应同步成四行。 |
| §6 S1 闸门第 89 行 | “拍板四张清单的 删/转/留” | S1 明列的产物包含机械报告、三份 inventory、权威表和架构摘要；“四张清单”没有逐名绑定，而且“删/转/留”只直接适用于死文与禁令处置。 | 写出四张清单的文件名，并分别注明需要的裁决类型。 |
| §6 S2 第 94 行、§13 第 259 行、§14 第 281 行 | “四份种子台账” | 施工图没有定义这四份台账。上游来源 `.memo/notes/doc-overhaul-20260830/glm-20260830a.md` 第 65、117 行实际指 backlog、GLM v0.15.0 评审 §三 A、Grok v0.15.0 评审、v0.7–v0.12 复盘四项。 | 在首次出现处写出四个精确路径，后文再简称“四份种子台账”。 |
| C3 第 115 行 | “合同层：spec/ 六件”，同一行又处理 `spec/README` | `docs/design/spec/` 有 7 个 Markdown 文件：总则 1 个加 6 份合同；C3 的实际范围包含总则。 | 改为“`spec/README.md` + 六份合同”。 |
| §8.1 第 166 行 | `implementation-evidence.md`“零引用” | 对这个精确文件路径确实没有入链；但 research 内仍有 3 处裸写 `implementation-evidence.md` 的旧称，其中若干已是断链。直接写“零引用”会漏掉这些残留。 | 改为“零个指向该精确文件的入链；research 内另有 3 处旧称待死名/断链扫描处理”。 |
| §8.2 第 183 行 | E2EE 规则“七处” | 仅按该行列出的章节计数就是 9 处：`project.md` 2、`spec/project.md` 4、`connections.md` 1、`architecture.md` 1、`delivery.md` CT 1；完整现行范围还出现于 `spec/system.md`、delivery P0/未决等位置。 | 删除“七处”或改为可复现的逐处清单；若保留数字，先声明是否排除 glossary、清扫表、历史和交付重复。 |
| §8.2 第 185 行 | Herdr v0.8.2 限制“七处” | 该行自身列出的二级章节计数是 8：`agent.md` 1、`spec/agent.md` 2、`system.md` 2、`connections.md` 1、delivery P0 与 CT-AGENT 2。 | 改为“八处”，或删除数字并保留逐处清单。 |
| §8.3 第 189 行 | `delivery.md`“一文十一职” | 当前文件有 14 个二级章节；原文把三个切片合为一职、把“明确不做”并入范围，才得到 11，未说明统计口径。 | 改为“14 个二级章节，按职责合并计为 11 组”，并写清两处合并口径。 |
| §8.4 第 197 行 | “全库约 270”条禁令 | 使用施工图指定的六个词逐次计数，`README.md + docs/usage.md + docs/design/**` 为 320 次，单 `docs/design/**` 为 315 次；合同层 185 次及各高密度文件的 41/39/32/29/26/26/24/12 均可复现。约 270 的排除范围没有定义。 | 明确排除范围与命令，或把全范围基线改为 320；保留已经核准的分文件与合同层计数。 |
