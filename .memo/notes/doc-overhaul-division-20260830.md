# 全库文档大修：多 Agent 分工方法论

> 说明：所有者提出全库文档大修任务（去死文 / 收束集群 / 删繁为简 / 严丝合缝），并给出 gpt-5.6-sol 与 fable-5 的历史行为画像，问如何安排。本文记录分工结构与判据；大修本身未开工，阶段 0 章程待立。<br>
> 基线：main @ 13504eb（草案 v0.15.0）<br>
> 去向：待大修章程立项时消化（`.memo/design/`）；方法论本身不进 `docs/design/`

## 一、输入：所有者原述

任务四要点：

1. **去死文**：演化遗留的过时内容，该删的删、该归档的归档；
2. **收束集群**：回到 big picture，内容结构与文件结构上找可整合、可拆进目录、跨域同构可合并的；
3. **删繁为简**："正确的路只有一条，错误的 case 无穷无尽"——『一定不要』列表要极谨慎，只有特别容易犯的、特别 tricky 的才落下来；可 mechanically cover 的也不必靠文档；
4. **严丝合缝**：全面大修，前后一致、整体性高。

Agent 画像与历史失败形态：

- **gpt-5.6-sol (xhigh)**：逻辑缜密、一致性强；但抓细节没有大局观，会钻牛角尖，在一两个细节问题上叠床架屋产生无效内容；
- **fable-5 (xhigh)**：愿景协作强、有架构意识；但细节老写不对。历史形态：fable 写 → gpt 挑错 → fable 改又没改对/改全 → 往复 5-6 轮 → 最后让挑错的 gpt 自己改完。
- 另可用：grok-4.6 (xhigh)、K3 (high)、GLM-5.3 (max)。

## 二、核心判断

**两个病各有触发条件，拆开阶段就能各自避开：**

- gpt 的病（钻细节、叠床架屋）只在**它拥有结构决定权**时发作；
- fable 的病（细节写不对）只在**它拥有改写权**时发作；
- 5-6 轮往复的根源是**作者 ≠ 修复者**：gpt 挑出的错由 fable 改，两个模型对文档的心智模型已经分叉，修补永远差半步。修法不是换人重试，是**评审者即修复者**——gpt 挑出的细节问题由 gpt 自己改。

因此：**fable 只做结构总图、不碰正文；gpt 只做执行改写、不发明结构；所有者只在两处便宜的控制点拍板（章程、总图）。**

## 三、分工表

| 阶段 | 谁做什么 | 产出与闸门 |
| --- | --- | --- |
| 0 · 立章程 | 所有者拍板（GLM 可代拟底稿）：冻结删除判据与负例判据 | 章程落 `.memo/design/`。删除判据原文引用现有文档纪律："精简只针对重复权威、无第一阶段用途的对象和补丁衍生对象；能够回答独立实现选择、交接、故障或权限边界的设计不得因篇幅被删除"（v0.8 删过头的教训，防过度精简的护栏） |
| 1 · 结构总图 | **fable**：只做 big picture——目标文件结构、合并/拆分/归档/删除台账，每条一行理由；**禁止改写任何散文** | 重排提案，状态"待拍板" |
| 2 · 拍板总图 | 所有者增删 amend；比较两份地图远比比较两版重写便宜 | 冻结的结构 |
| 3 · 分片执行 | **gpt**：按冻结图分 5-6 切片执行（根层 README → 设计层 → 合同层 → research → .memo 归档 → src 内文档），一切片一 PR，走 PR contract；每片自查清单，细节问题 gpt 自己改（评审者即修复者） | 一串 PR |
| 4 · 验收 | **机械：K3 + 脚本**（死链、词形一致、版本号一致、死名残留 grep：agentd/tmux/Zellij/Conductor/Electron）；**fable**：只读一遍，只问"作为整体是不是一个系统、有没有大东西丢了"（结构级，不挑细节）；**grok**：只抽审最危险的两片——合同层与负例清扫 | 核销记录 |

辅助角色：**GLM** 持"不一致账本"（刚完成全库交叉核对，手上有最新跨层差异图：根 README 陈旧、cleanup-backlog 过期、F1/F2/F11 无裁决等），并可代拟章程底稿。

## 四、负例三留原则（对应任务第 3 点）

仓库已裁决过一半（`notes/doc-cleanup-backlog-20260825.md` §1："把每条负例问一遍'正确道路写清了它还进得来吗'——进不来就删，进得来才说明正确道路没写清，改正面句而不是加禁令"）。完整原则：

只有三类『不要』留在文档里——

1. **特别容易犯的错**；
2. **特别 tricky 的错**；
3. **可机械化的**——转成 CT-* 契约测试用例。负例进了测试就不再占文档篇幅，还顺带获得可执行对偶，缓解"合同密度无可执行对偶"的旧病（kimi 0.10.2、gemini 同轮）。

其余一律改写成正面陈述或删除。

## 五、防叠床架屋的机械闸

- **每个切片限两轮评审**，沿用既有审计纪律："第二轮若主要发现第一轮新增内容的问题，则回滚该片而不是继续打补丁"；
- 总图冻结后 gpt 无结构决定权，想叠也没有落点；
- fable 不进入"照 gpt 意见改细节"的循环——那个循环就是历史失败的形态。

## 六、执行前提

1. **先机械后人**：链接/词形/版本/死名检查先做成 CI 脚本再动第一篇文档；LLM 评审面收窄到只有 LLM 能判的东西。负例如此，一致性校验更是如此（kimi 历史上纯机械检查抓到过真问题，不烧 xhigh）。
2. **素材已存在，不重新发现**：大修输入不从零调研——`doc-cleanup-backlog`（13 条过强断言 + 禁令清扫原则）、`review/20260830-v0.15.0/glm-20260830a.md` §三A（陈旧清单）、grok 同基线判断（PR #48）、v07-v012 复盘债务表，四份现成台账合并即为死文与收敛清单的种子。fable 的总图消费它们，不重新发明。

## 七、详细开工建议

### 7.0 一条红线（先于一切）

**大修不改任何合同语义。** 只动结构、归档、措辞与负例形态；执行中发现语义疑点，记入台账（issue 或 memo），不在本修内改。防止"文档大修"漂变成又一轮设计迭代——那是 v0.7–v0.12 已经走完的路，重走等于回滚六轮评审的收敛成果。

### 7.1 章程内容清单（阶段 0 底稿目录）

1. **删除判据**：原文引用文档纪律的精简三对象 + 反向护栏（能回答实现选择、交接、故障或权限边界的设计不得因篇幅删除）。
2. **归档语义分层**：
   - `.memo/review/` 本就是按基线作废的档案层——只补状态标注（已废弃/已归档/已核销），不搬不重写；
   - `.memo/notes/`、`.memo/design/` 刷新"待拍板"表，已消化项标去向；
   - `docs/research/` 带日期文件是证据（被 delivery 引用），原则上不删；被取代的调研（如 `tmux-runtime.md` 被 Herdr 取代）标"已被 XX 取代"留档，不删——它们是 decision-history 链条的一环；
   - `docs/design/` 正文删除/合并必须走小修订台账一行；`references/decision-history.md` 本身只增不改写；
   - **链接完整性约束**：任何被 decision-history / implementation-evidence / delivery 链接引用的目标文件，删除时必须留档或同步改链（机械检查兜底）。
3. **负例处置去向表**：每条『不要』→ 三选一（保留 / 转 CT-* 用例 / 改正面句），第四态"删除"留给进得来正确道路的。
4. **机械检查清单**（见 7.2，先进 CI 再动文档）。
5. **切片边界与顺序**（见 7.3）、每片验收口径（见 7.5）、回滚规则（第五节）。

### 7.2 机械检查清单（S1 基建，半天）

| 检查 | 内容 | 排除域 |
| --- | --- | --- |
| 死链 | markdown 相对链接目标存在性 | — |
| 版本一致 | 根 README 与 `docs/design/README.md` 等头部基线号一致 | — |
| 死名残留 | `agentd`、`tmux`、`Zellij`、`Conductor`、`Electron`（主选已是 Tauri 2）、旧选型名 | `references/decision-history.md`、`.memo/`（来时路与评审史里的历史名合法）、`docs/research/` 证据文件 |
| 词形一致 | 核心产品词 + 六个高频合同词拼写统一（含中英对照首次出现规则） | — |
| 状态标注 | `.memo/design/` 待拍板表与文件头状态一致 | — |

### 7.3 切片划分（每片一 PR，串行）

| 片 | 范围 | 主要动作 | 审者 |
| --- | --- | --- | --- |
| S1 | 机械检查脚本 + CI wiring | 上表五项检查落地为 PR/push 均跑的 job | — |
| S2 | 根层门面：根 README、AGENTS.md、CONSTRAINTS.md、docs/usage.md | 升 v0.15.0、架构图换 Herdr/Tauri、措辞与设计层对齐 | K3 机械 + 所有者目检 |
| S3 | 设计层正文七篇 + glossary | **收束集群主战场**：按 fable 冻结总图执行合并/拆目录/删重复解释 | fable 通读 + grok 抽审 |
| S4 | 合同层 spec/ 六篇 | **最危险片**：负例三留清扫、跨篇引用统一 | grok 抽审（重点：清扫是否误删护栏）+ K3 机械 |
| S5 | docs/research | 被取代调研标注、README 索引补全、子目录归置 | K3 机械 |
| S6 | `.memo/` 状态核销 + `src/` 内 README/packaging 文档 | 待拍板表刷新、backlog 13 条逐条裁决标注 | 所有者 |

串行理由：后片依赖前片稳定后的引用基线；S3/S4 在总图冻结后理论上可并行，初版建议不并行——本次大修的优先级是一致性，不是速度。

### 7.4 节奏

S1 与章程可并行（各半天）；fable 总图一天内出、所有者当天拍板；每片"执行+验收"一个工作日以内；**全程一周内收口**，超期即触发缩小范围（止损线，见 7.6）。

### 7.5 全局完成定义（DoD）

- 机械检查全绿且常驻 CI；
- fable 整体通读报告无结构级缺失（"是一个系统"）；
- 四份种子台账全部核销或显式拒绝并留痕（backlog 13 条、PR#49 §三A、grok PR#48、v07-v012 复盘未闭合项）；
- decision-history 记一条大修条目；触及合同层措辞（S4 负例形态变化）按纪律推进基线补丁号；
- 每个 PR 描述附该片新增/删除/归档文件清单。

### 7.6 风险与止损

| 风险 | 信号 | 止损 |
| --- | --- | --- |
| gpt 执行期顺手改语义 | S3/S4 diff 出现语义级措辞变化 | 红线拦截：打回该片，语义疑点记台账 |
| fable 总图过度合并（重演 v0.8 删过头） | 总图条目无法回答"删的是重复权威还是解释" | 阶段 2 拍板时逐条问；护栏判据已在章程 |
| 大修滚成设计迭代 | PR 里出现新概念、新机制 | 不是本修对象，转独立提案 |
| 进度失控 | 超一周 | 收缩到 S1+S2+S4 已完成部分收口，剩余转常态清理 |
