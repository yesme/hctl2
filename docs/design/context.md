# Context 与可解释上下文

> 状态：规范性（横切设计正文）· 草案 v0.12.4<br>
> 日期：2026-08-25<br>
> 定位：本文回答"每次执行看到了什么、为什么是这些、漏了什么"。它是横切设计正文，不是第五个模块：上下文清单与内容包的合同在 [Project 模块合同](./spec/project.md)，随执行规格冻结与交付核验在[连接合同](./spec/connections.md)与 [Agent 模块合同](./spec/agent.md)。合同级细节的完整底稿与逐条裁决来源见 `.memo/context-design-20260819.md`；生态四族地图与快省准横评见 `.memo/context-landscape-20260824.md`。

## 为什么存在

Context 不是"把最近若干条聊天塞进 prompt"。那只能恢复文字，回答不了真正要紧的问题：这次执行继承了哪些目标、决定、约束和未决问题？每条信息来自哪里、是否还新鲜？检索覆盖了什么、漏了什么？故障切换之后，能不能重现同一份交付义务？

先划清管辖：**我们管理的是 prompt——调用开工时交给执行体的那个包；不代管执行体在会话内自行组装的工作上下文。** 仓库代码、Memo、Skill 这些材料，执行体自己会挖、自己管理——读文件、载技能本来就是它的本职，它比我们更清楚自己需要什么。唯独聊天史是它拿不到也不该自己去翻的来源：凭据不在执行沙箱里，翻全史也烧 token。所以本文的主题就是那个开工包怎么来：从聊天史萃取的相关讨论 + 契约与范围 + 指向其余材料的精确指针。

遍览同类工具后的共同结论是：上下文是必需品，而且直接决定四件事——冷启动成本、重复核验次数、模型的有效工作时间、结果的可复核性。目标不是"给得越多越好"，而是：

> 在获准范围内，用尽可能少且足够的信息恢复工作的因果链，并让每个结论都能回到来源。

检验标准就三个字：**快、省、准**。快——识别相关上下文毫秒级、全本地；省——组装本身不花模型 token，压缩可选不强制；准——因果链完整、结论可回源，由冻结与可解释兜底。三个字同时成立才算合格，牺牲任何一个都要在清单里显式可见。

## 与相邻概念的分工

| 东西 | 是什么 | 不是什么 |
| --- | --- | --- |
| 聊天历史 | 完整、按序的协作记录（content，住在 chat server） | 默认 prompt，也不是长期知识库 |
| 工作投影 | 由引用、置顶/排除与当前对象重建的讨论现场 | 独立对象；可随时重建 |
| 上下文清单 | 一次授权用什么来源、怎么选的、基于哪个时间点的不可变说明 | 内容本身 |
| 内容包 | 按某个执行者的权限和预算实际物化、交付的内容 | 可以事后改写的东西 |
| Memo | 显式提炼、预览、去敏后发布的长期知识 | 聊天或执行日志的自动沉淀 |
| 会话内工作上下文 | 执行体在会话中用自己的工具挖出来、自己管理的动态上下文（读文件、载 Skill、查 Memo） | 我们的管理对象——只观测，不代管 |

搜索索引、检索服务都是可重建的内部投影，不是权威；任何索引都必须能从来源和不可变引用重建。

## 萃取与压缩：中心设计

进展由聊天室驱动，所以每次调用的第一件事，是从聊天史里**萃取**出与这位执行者相关的部分。这一步必须快、必须全本地、不花模型 token，走三级阶梯：

1. **结构化引用**：显式引用、讨论窗口、对象引用链——组装顺序的既有第一级，零成本且全对；
2. **本地全文检索**：从消息流增量维护、可随时重建的本地索引，配机械融合排序——毫秒级、零费用，兜住显式引用漏掉的隐式关联；
3. **可选的小模型相关性门**：只判"该不该拿"，判定输入用账本里的事实（谁被提及、谁认领了什么），不用消息措辞；每次判定入账可审计。

开工包里物化原文的只有两类：萃取出的相关讨论，和用户显式引用的内容。其余材料——仓库文件、Artifact、Memo、Skill——只进指针（精确引用 + 指纹），由执行体在获准范围内自己取用：懒加载永远比预先塞进 prompt 省。顺序也由此了结：prompt 的稳定部分（契约、范围、指针清单）在前，高频变动的萃取讨论在后——语义优先级和提供商的前缀缓存给出同一个答案，不冲突。

萃取出的上下文**缺省不压缩**，原文直给、预算内按选择优先级裁剪。只有当用户配置了专用小模型（small-brain）时才启用**压缩**——摘要式或逐词裁剪式都可以，但压缩永远是清单里显式记录的一步：用了哪个模型、压了多少、原文指纹是什么，全部冻结；可解释性不因压缩打折。证据类内容——指纹、凭证、验收标准原文——永不压缩。small-brain 只是执行者配置里的一个模型引用，不是新对象。

长期记忆不在这里发明：结晶进 Git、场景内容留在各自的系统，是三类数据已经回答的问题。别家在自建第五套记忆库，HCTL2 的 Context 只做一件事——把工作记忆组装对。

## 前情提要：房间的滚动上下文

从父概念转入子概念（Repo Room 提升出 Project Room、Project 派生 Scoped Room 或启动施工）以及在长房间里反复调用时，都有一份"前情提要"要交代。它其实是三种生命周期不同的东西，"每次传不传"分别回答：

| 成分 | 是什么 | 每次传吗 |
| --- | --- | --- |
| 权威骨架 | Project 的目标、范围、验收要点、当前契约、指针清单——带版本号的权威文本，不是摘要 | 每次传：小、稳定、免费，坐 prompt 的稳定头 |
| 出生来源链 | 提升时冻结的显式来源 | 不每次传：指针化，萃取命中才物化 |
| 滚动讨论纪要 | 讨论过、口头定了、但尚未结晶成 Memo 或契约的共识 | 分级，见下 |

滚动纪要按三条规则管住：

1. **没配 small-brain 时它不存在**，用零模型成本的"近详远略"代替——近期消息全文、更早的降为标题加事件指针。这个缺省的缺陷是故意保留的：重要的口头共识就该结晶成 Memo，缺省态的不完美恰好推动显式沉淀，而不是纵容"反正纪要会记住"。
2. **配了 small-brain 时它存在且每次传**：一份挂在（房间，游标）上的增量折叠缓存——新消息折叠进旧纪要，不从头重推；天生紧凑，折叠比原始聊天变化慢，不常击穿缓存前缀。
3. **它永远是派生缓存，不是权威**：每条带消息事件回源指针；治理引用不得指向纪要，只能指向精确事件；丢了就重建，不进账本，清单只记引用与指纹。它也不由房间里的模型 Participant 书写——"agent 自己决定记什么"是自述，纪要是组装器机械触发、small-brain 计算的系统派生产品。

父转子不需要新机制：父房间的纪要（若有）作为提升/创建预览的预填材料，人删减、补充、去敏并确认后冻结为子房间的出生来源链；出生之后各长各的，父房间后续讨论不会活体渗入既有子概念。

## 关键规则

- **可解释**：每次调用冻结一份上下文清单——来源、筛选依据、新鲜度、覆盖面和已知缺口都写在里面；事后永远能回答"它当时看到了什么、漏了什么"。模型的自由总结不能替代来源和版本。
- **冻结**：执行规格同时冻结清单与该执行者的内容包，交付前核对实际字节；此后的聊天、索引变化或另一个执行者的包都改不了已冻结记录。
- **传承有链**：Repo Room 的研究 → Project → Task/Run → 每个执行者的内容包，只能通过显式的来源引用传承；"最近消息"、当前指针或搜索排名都不构成传承。
- **运行中追加召回不越权**：执行中可以在获准范围内补充检索，但不能绕过冻结的范围、权限与审计。
- **晋升有门槛**：原始消息和执行日志不会自动变成长期知识；进 Memo 必须显式提炼、预览、去敏、发布，带来源、适用范围和有效期。
- **成本纪律：能用规则就不用模型。** 采集、规范化、检索、融合、排序全部机械化；摘要压缩是唯一显式的模型步骤——有预算、结果持久化、增量折叠而不是每次从原文重推。每次调用的上下文构成应可审计（哪些来源、各占多少），固定前缀保持稳定以利缓存。
- **降级显式**：来源不可用时按级降——先本地检索并标注来源，再提示缺口；不用陈旧缓存冒充新鲜读数。

## 场景

Trigger Preview 在提交前展示本次调用的上下文构成与 token 估算；升格 Project 时的提升预览允许删减、补充和去敏，只带显式选中的来源链。这两处是用户直接触摸 Context 的地方——其余时间它应该安静地正确。

## 模块交接与合同落点

- 清单与内容包的对象、组装顺序、保留与丢弃合同：[Project 模块合同](./spec/project.md)；
- 施工清单冻结根清单、执行规格冻结执行者内容包、交付核验：[连接合同](./spec/connections.md)与 [Agent 模块合同](./spec/agent.md)；
- 契约版本冻结来源引用：[Task 模块合同](./spec/task.md)。

萃取阶梯、压缩记录、前情提要与省的计量已随 Context 合同裁决轮进入 [Project 模块合同](./spec/project.md)；尚未进入合同的部分（派生索引的具体形态、检索融合策略、跨 Repo 传承）留在专题 memo，按"逐条裁决、不整篇升格"进入规范。
