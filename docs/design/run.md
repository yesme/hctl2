# Run 与 Workflow

> 本文是 Run 模块的设计正文：它为什么存在、拥有什么、按什么规则运转。精确对象、状态机与写入约束见[约束附录](./spec/run.md)；模块交接见[连接约束](./spec/connections.md)，共享机制见[系统边界](./spec/system.md)。

## 为什么存在

行业并不缺工作流引擎：DAG、代理委派、重试、定时器和历史恢复都已被反复实现。真正缺失的，是一套面向项目语义、绑定精确版本与证据的治理层——它回答“这个节点对应哪份交付义务、谁有资格尝试、结果是否有效、凭什么可以算完成”。这件事不能委托给聊天室、看板、Harness、终端或任何通用引擎；它是 Run 模块的原生价值。

两种权威一句话可以分清：

> 引擎说：“这个外部节点 READY / IN_PROGRESS / COMPLETED 了。”<br>
> HCTL2 说：“它对应哪份交付义务（Obligation），谁可以尝试（Seat 与候选），结果是否有效（Verdict），是否可以完成（准入校验）。”

引擎拥有机械位置，HCTL 拥有语义治理；两边不能互相冒充。

## 模块拥有什么

Run 模块保存“哪份自动施工已获授权、机械进度如何映射为语义结果”。Workflow 是它的操作场景，不是第五个领域模块。它拥有：

- **施工图及其批准**：与引擎无关的不可变控制图（Workflow Revision），先批准、后开工；
- **一次授权执行**：Run 是对冻结施工图、任务契约、代码基线、候选、权限和预算的整体授权；
- **交付义务**：Obligation（交付义务）是一个外部节点必须产出的逻辑结果。引擎重试只是路标再次进入等待态，由 HCTL 铸造新义务，旧义务连同它的票一起作废——重试不灌票；
- **席位与尝试**：Seat（执行席位）是义务里稳定的逻辑裁判位，换候选人不改变席位——换人不换裁判、不多一票；Attempt（执行尝试）是某个候选的一次具体执行，物理现场崩溃或重连不改变尝试身份——掉线不丢身份；
- **评审关卡**：Gate（评审关卡）按冻结规则决定结果凭什么通过；
- **裁决与凭证**：Verdict（裁决）是对精确版本的语义结论，Receipt（凭证）是校验后的正式证明。

在三类数据里，Workflow 场景的结晶是凭证链——“干成了的证明”。施工图（“干什么的计划”）从 Project Room 的塑形讨论中长出，作为结晶归 Chat Room 场景；它的批准与版本对象仍由 Run 模块定义——结晶归属以事实为准绳，不随对象所有权走。判决的权威在控制面账本，结晶副本进 Git（私有仓库默认全文，公开仓库可降为仅摘要）。

## 关键规则

Run 采用愿景中的[两种控制制度](./vision.md#两种控制制度)：批准与开工分开，开工后 control 才在冻结边界内自动推进。用户命令和 Agent 结果先经校验并持久化，涉及运行时的动作再按撤权与 outbox（发件箱）顺序送往引擎；谁点按钮不是关键，保持这个顺序才是关键。

- 开工时冻结一份施工清单（Manifest）：项目与任务契约、施工图、代码基线、席位与候选、能力权限、评审与预算规则一次冻结。
- 运行中只有清单声明的放置参数可变；范围、验收、候选或权限变化通过结束或替代旧 Run 生效。
- “重试”有五种身份不同的路径——传输重投、候选切换、引擎重试（路标再次进入等待态，由 HCTL 铸新义务）、语义返工、替代执行；混用它们会复制票数、绕过验收或复活旧执行。完整分类见[约束附录](./spec/run.md#request重试与-gate)。
- 评审必须独立：被评对象的作者不占必需评审席位；备用尝试不增加票数；被评对象换版本后旧票作废、完整重评。
- Dagu 界面的直接 mutation 会先改变机械执行，因此只回读为分歧并安全暂停；精确顺序与准入见[Run 约束](./spec/run.md#写入约束)。
- 尝试的逻辑身份由 Run 拥有，物理进程由 Agent 模块观测；两边不维护两套状态。
- Run 正常完成且绑定契约时，由归约器提交同一个 Task 完成命令并交由 Task 独立验收；权威来源定义见[Task 约束](./spec/task.md#写入约束)。
- 裁决与凭证绑定精确的评审对象版本。
- 同一 Run 里节点之间靠结晶接力：评审席位拿被评版本，返工席位拿裁决正文，备用尝试拿同一份上下文加旧变更集版本的指针；引擎路标、step 日志和终端 trace 都不是来源，没提交的对继任者不存在。完整规则见[横切正文](./context.md)。

## Workflow 场景

Workflow 场景提供施工清单预览、只读图、节点/席位/尝试的渐进展开、Request、证据、暂停/取消和恢复状态。

| 角色 | 可以做什么 |
| --- | --- |
| 场景客户端：Workbench Run 图 | 查询、预览并通过公共 command service 提交 Run 命令 |
| 场景客户端：CLI | show/preview/start/pause/cancel；写操作先预览确认 |
| 受控端口：workflow engine | 保存令牌、机械任务、定时器、重试和历史 |

Workbench 关闭不停止 Run。P2 用 `hctl2` CLI，Workbench 就位后仍调用同一服务。Dagu 控制台保留为 provider 管理与诊断界面；其直接动作按[Run 约束](./spec/run.md#写入约束)回读为机械事实与分歧。

## 模块交接

以下只列所有权方向；字段、事务与故障语义由[连接约束](./spec/connections.md)统一定义。

- Project 通过启动命令交付冻结的 Project 与施工图引用；可选的契约版本由 Task 提供，Run 独占施工清单与执行生命周期。
- Run 向 [Agent](./agent.md) 模块交付派发时冻结的执行规格（Execution Spec）；Agent 模块只返回结果提议、版本、证据和物理观测。
- Run 向 Task 返回精确的 Run、裁决与凭证引用；正常完成且绑定契约的 Run 可由归约器提交同一个 Task 完成命令。Run 向 Project 提交 Request 并投影低噪声里程碑。
