# 当前生效约束集

> 所有者维护，harness 只读。每条约束持续生效，直到在本文件里被修改或删除——聊天里的新指令是对本清单的**叠加**，不是替换；推倒重来只在所有者显式说"重来"时发生。设计决策不在这里（见 [decision-history](./docs/design/references/decision-history.md)），这里只放"怎么工作"的横切约束。

## 词汇与文档

- 禁用"张力"，一律写"冲突"。
- 执行身份不可证的状态统一叫"丢失"，不用"中断"。
- 禁用"账本"，按上下文写精确的标的物：控制面存储、治理记录、Git 正文或平台记录；决策史与 `.memo` 里的历史用词不改；机械检查随全库改口批一起落。（所有者裁定，2026-09-07）
- 逐条裁决，不整篇升格；决策史只记转折，小修订进台账；约束变更 bump patch 版本；新约束要配 CT 失败用例。

## 代码与构建

- 构建动作一律走 Buck2 原生目标与 actions；自建 shell 脚本旁路需要在 PR 里单独论证。
- 三平台构建使用同一 rustc 版本（当前 1.98.0，权威在 `src/rust-toolchain.toml`）。
- 新组件、新依赖先落 `docs/research/` 对应调研文件，再写代码。

## 协作

- 接口设计 for agent，不必逐项找 human 确认；方向、边界、取舍必须找 human 确认。
- 结果如实报告：测试红就说红，跳过的步骤就说跳过，不粉饰。
- 设计讨论默认在架构层进行；实现细节不构成对架构判断的反驳，只能作为标注了层级的脚注。（所有者裁定，2026-08-29）
- 讨论阶段不提交；落盘须所有者显式指令，一轮讨论收敛后打包成一个 PR。（所有者裁定，2026-08-29）
