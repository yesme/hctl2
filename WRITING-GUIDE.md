# HCTL2 文档写作指南

> 本指南适用于 `docs/` 下的设计文档、合同、交付文档与仓库门面。它管**风格与结构**（怎么写），不管**内容与权威**（写什么、谁定义什么）——后者在[设计地图的「文档纪律」](./docs/design/README.md#文档纪律)与 [CONSTRAINTS.md](./CONSTRAINTS.md)。两者冲突时，内容正确性优先，并就近注明偏离理由。
>
> **两种文风：** 宣言文风说服读者「设计方向是对的」（Part I）；技术文风让实现者「能正确实现」（Part II）。混合文档在**章节级**切换，不在段落级混用；归属判定见 0.3。
>
> 来源：改编自 `yesme/abacistopia` 仓库的 `SPEC-WRITING-GUIDE.md`。本文是 HCTL2 自己的副本，按本库需要增删，不追求与上游逐字一致；HCTL2 未采纳的条目与理由见文末。

---

## Part 0：写作协议

本节回答：拿到写作任务后按什么顺序决策、用什么模板生成、生成后怎么自检。Part I / II / 共用规则是它背后的理由库；本节是执行路径。

时间有限时的最小路径：宣言读 M1 / M2 / M6，技术读 T1 / T2 / T3，共用读 L1 / L5 / L7 加增补三条 S1–S3，写完过对应自检清单。

### 0.1 写作前三问

1. 目标读者是谁？新读者 / 实现者 / provider adapter 开发者——决定信息密度与术语深度。
2. 本节用哪种文风？宣言还是技术——判定见 0.3。
3. 核心约束是什么？说不出一句「什么永远成立」，说明还没想清楚。

### 0.2 规则强度分层

| 层级 | 标记 | 含义 |
|---|---|---|
| Hard rule | 必须 / 禁止 | 不遵守则文档不符合本库风格 |
| Strong default | 应当 / 不应 | 默认遵守；偏离需就近说明理由 |
| Recommendation | 建议 / 可以 | 可选，视上下文调整 |

### 0.3 分类决策树

生成或编辑任何段落前先分类：

```pseudo
1. 哪一层？        见 0.4 路由表 —— 决定文风、允许词汇与规范性
2. 文风？          删掉后实现者会做错 → 技术(Part II)；否则 → 宣言(Part I)
3. 产生合同义务？   是 → 规范性，用 T1 关键词写成可测试断言
                  否 → 非规范，按 L11 标注
4. 定义还是引用？   本层拥有该概念 → 在此定义一次；否则 → 交叉引用(L9)，不复制
5. 内容类别？      规范 / 理由 / 示例 / 未决——按 L11 标注；未决用 L12
```

### 0.4 四层路由表

HCTL2 的文档按颗粒度分四层。层与层之间**引用而不复述**；读者读完任一层应能自洽地停下来，不必为读懂本层而下潜到下一层。

| 层 | 回答什么 | 文风 | 允许词汇 | 规范性 | 文件 |
|---|---|---|---|---|---|
| ① 愿景 | 为什么存在、想给什么体验、按什么原则取舍 | 宣言（M1 / M2 / M6） | 核心产品词 + 日常语言 | 规范性（原则层）；与合同冲突以合同为准 | `docs/design/vision.md`、根 `README.md` |
| ② 架构 | 由哪些模块与面组成、怎么组装、失败时怎么办 | 混合，抓大放小 | 核心产品词 + 六个高频合同词（携中文对照） | 规范性（架构层） | `architecture.md`、`design/README.md`、`project/task/run/agent.md`、`participant.md`、`context.md` |
| ③ 机制 | 精确对象、状态机、写入者、不变量、互操作边界 | 技术（T1–T5） | 合同层全部词汇 | 规范性合同；冲突时最终权威 | `docs/design/spec/` 七个文件 |
| ④ 交付与验证 | 第一阶段做什么、怎么选型、怎么验收 | 技术（非规范） | 可引用合同词以指认被测合同，禁止重定义 | 非规范；不改变领域含义 | `delivery.md`、`contract-tests.md`、`docs/usage.md`、`docs/research/README.md` |
| — 参考 | 对照与史料 | 表格 / 叙事 | 全部 | 非规范 | `references/glossary.md`、`references/decision-history.md` |

**每层读完应该能回答什么**（本表的验收标准）：

- ① 读完能回答「为什么需要它、它不做什么」，**不需要知道任何对象名**。
- ② 读完能回答「有哪些模块和面、怎么组装、失败会怎样」，**不需要知道状态机与字段**。
- ③ 读完能实现内核或写一个 provider adapter，**不需要知道选了哪个产品**。
- ④ 读完能开工、能验收。

**失败判据同样明确**：读者为了读懂 ① 必须翻 ③，是 ① 写漏了；③ 里出现「为什么这样更好」的论证，说明该论证应该在 ① 或 ②。

### 0.5 各层写作模板

**① 愿景：** 一句话定位 → 问题与失败模式（M1 第 1–2 步）→ 心智模型 → 目标体验 → 设计原则（每条独立可引用，M3）→ 代价与不做什么（M1 第 4 步）。

**② 架构：** 本文回答什么 → 组成与职责表 → 组装关系（图为直觉，表为规范）→ 归属矩阵 → 交接 → 失败与恢复。

**③ 机制：** 本文拥有什么 / 不拥有什么 → 对象表 → 写入合同表（聚合 / 版本与 lifecycle / 合法命令与唯一写入者 / 终态）→ 状态机（T3 五件套）→ 归约与流程 → 失败与恢复 → 外部概念对齐表。

**④ 交付：** 范围声明（非规范）→ 阶段与切片 → 选型判据与证据链接 → 验收用例 → 未决问题（L12）。

### 0.6 段落级重写

生成中命中「反模式清单」任一行的段落，按该行的修正方向改写。

### 0.7 生成后自检

逐项过「自检清单」对应文风部分加共用部分。

### 0.8 提交前收口协议

0.7 管单文件质量，本节管跨文件契约——多轮评审的实证是：最贵的返工来自跨文件不闭合，而不是单文件语病。

**镜像闭环（改一处，同批改齐镜像面）：**

- 合同层改对象或状态 → 同批更新该模块设计正文的产品语言投影、`glossary.md` 词条、`contract-tests.md` 对应用例；
- 新增跨模块交接 → 接收端同批定义承接（谁准入、凭什么、失败怎样）——写下「交由 X 处理」的同时切到 X 落承接，不开空头支票；
- 既有字段加新取值 → 检索所有触及旧取值的「必须」句，同批分轨改完；
- 改标题 → 先算新锚点，再全库改指向它的链接（`check_links.pl` 会拦，但要在提交前自己先跑）。

**system pass（文本一致性之外，在脑子里跑一遍系统）：**

- 每种对象从生到灭无死胡同；每条出向交接有入向承接；
- 每个改变事实的命令过作恶四问：会死锁卡住吗；哪个角色能绕过；并发竞态会不一致吗；时间异常（迟到、过期、重放）会怎么破坏——结论写进失败合同或前置条件；
- 凭证即授权的路径三层齐：谁写（写入者与命令）、凭什么信（证据与摘要在不在合同里）、重复提交怎么收敛（幂等键是什么）。

**收口纪律：**

- 「已解决 / 已覆盖 / 通过」三件套核验后才可标：条款存在 + 引用链闭合 + 反面用例走得通；
- 发现一个缺陷先命名结构模式，再全库扫同构位置——最有效的修复是把同一模式全库闭合，不是单点打补丁；
- 合同变更同批推进基线版本与日期，并配契约测试的失败用例（见 CONSTRAINTS.md）。

### 0.9 术语改名 sweep 协议

改名或统一术语按此序执行，防漏也防误伤：

1. **全谱枚举**：全称、缩写、复合词、大小写变体、文件名与锚点——先列出全部命中形态再动手，缩写与旧简称最易漏。
2. **定义与锚点先行**：先改词汇表、定义位与标题，让后续 sweep 有判据；改标题先算新锚点、同批改全库入链。
3. **字面值与散文双轨**：配置键、路径、协议常量与散文分开处理——字面值改动是语义变更，逐个确认，不随散文批量替换。
4. **批量替换前排除例外**：定义性出现（「X（原名 Y）」）、反引号内命中、引用外部产品的原文，逐个人工确认。
5. **清零加链检收口**：旧形态全部归零；跑 `docs-check`；新旧术语不得在正文交替存活（L3）。

---

## Part I：宣言文风

**适用场景**：愿景层全部；架构层的「为什么存在」「为什么不用现成方案」小节。

### M1 论证结构

宣言的核心是论证，不是声明。读者读完应能回答：为什么是这个设计，而不是别的？

**叙事弧四步：**

1. **问题定义。** 用一段话精确描述要解决的问题。不使用「革命性」「前所未有」这类措辞——如果问题真实存在，平实描述就足够有力。
2. **现有方案审视。** 逐个列出已有方案。对每个方案，先以其最强形态描述，再指出为何仍然不够。反面做法是稻草人论证——歪曲或简化对手方案再驳斥，在技术读者面前会立刻丧失可信度。
3. **设计选择。** 提出方案，论证它如何避免前述不足。核心技艺是从约束推导（M2），不是从功能枚举。
4. **后果与代价。** 诚实说明这个选择牺牲了什么、不解决什么问题。回避代价的宣言不可信——读者会默认作者在藏缺陷。

**结论先行。** 每个章节第一段就给出结论，后续段落是论证。读者没有义务陪作者制造悬念。

### M2 论证方法：从约束推导

**反面（功能枚举式）：** 「本系统提供可审计、可恢复、可替换的治理能力。」这是声明，不是论证——读者无法判断这些性质是否必要、是否可实现。

**正面（约束推导式）：** 先立约束，再证明架构性质是这些约束的必然结果。读者接受约束后，架构选择就成了逻辑必然。

**四种论证模式：**

| 模式 | 适用场景 |
|---|---|
| **负证明**（为何不能更简单） | 引入新机制、新组件时 |
| **对比论证**（为何 A 优于 B） | 在两种设计间选择时 |
| **失败推演**（为何这样不会坏） | 一致性、恢复、权限机制 |
| **可检测性论证**（为何出错能被发现） | 无法预防的情形——当预防不可能时，论证「越界必留痕、必可对账」比硬写预防机制更有力 |

**负证明模板：**

> 机制 X 必须存在，因为：
> (a) 无法由 {既有组件} 组合得到，理由是……；
> (b) 若放在更高层，会破坏约束……；
> (c) 若放在更低层，会迫使该层处理它结构上不应关心的信息……。

### M3 论证风格

| 风格 | 适用场景 | 特征 |
|---|---|---|
| 叙事弧 | 设计总览 | 问题 → 审视 → 选择 → 代价 |
| 原则驱动 | 设计原则章节 | 每条原则独立、自洽、可引用 |
| 对比论证 | 设计抉择 | 两种方案并排对照 + 具体失败模式 |
| 分类法 | 大量现有方案的综述 | 找到正确的分类轴，展示本方案填补的空白 |

**原则驱动的写作纪律：** 每条原则是一个完整的句子，不是短语碎片；自包含，不依赖前一条才能理解；可独立引用。原则之间可以有层次，但每层内部是平的。

### M4 类比与直觉

类比帮读者建立直觉，但不能替代论证。

- 每个类比标注适用范围与失效边界（「与 X 类似——但在 Y 方面不同，因为……」）。
- 类比出现一次后，后续用名称引用，不重复展开。
- 核心设计决策不能只靠类比支撑，类比之后必须跟约束推导（M2）。

### M5 失败模式分析

论证一个治理或一致性机制时，配一节失败分析。形态：

> **谁会越界 / 什么会坏** → **怎么发生** → **本系统的反制（引用具体条款）** → **残余风险（诚实写出）**

纪律：每种失败独立成条，不合并叙述；反制必须引用具体条款或约束，禁止「系统会防止这种情况」式的无引用断言；残余风险如实陈述。

### M6 密度与诚实

**密度优先于覆盖。** 宣言的价值在论证密度，不在覆盖面。每段问一遍：删掉这段，论证链是否断裂？不断裂就删。

**零修辞即是修辞。** 禁令见 L1。这种克制让读者意识到作者没有在推销，于是认真对待论证本身。

**诚实承认代价。** 区分「已证明的」和「推测的」，区分「合同保证的」和「实现应做的」，区分「第一阶段覆盖的」和「未来工作的」。每处模糊的承诺都是未来争议的种子。

> **注意：** M6 是压缩的**上限**，S1 是压缩的**下限**，两条必须一起读。只按 M6 删会把论证删成结论电报。

---

## Part II：技术文风

**适用场景**：机制层全部；架构层的表与矩阵；交付层的验收用例。

### T1 规范性关键词纪律

规范性文本只用三档，**每档只用一种写法**：

| 档 | 用词 | 含义 |
|---|---|---|
| MUST | **必须** / **不得** | 不满足即违反合同 |
| SHOULD | **应当** / **不应** | 默认如此；偏离需就近说明理由 |
| MAY | **可以** | 可选 |

**只能** 保留为 MUST 的排他形态（「只有 X 才能……」「结果只能来自……」），语义等同必须，不另立一档。

**「需要」不是规范性词。** 它同时承担「描述性必要条件」和「规范性应当」两种意思，读者无法判断这句话是不是义务。写规范句时改用上表三档；描述客观必要性时改写句子（如「这一步依赖 fresh readback」）。

**判定标准**：每个用关键词的句子必须**可测试**——能写出一条契约测试判定某实现是否合规。写不出测试的句子，要么改写成可测试形式，要么移入非规范文本。

精确性不需要形式化记号来保证；散文体同样可以做到零歧义，前提是每句话只表达一个可判定的约束。

非规范文本（设计理由、历史背景、实现建议）避免使用上述关键词的加粗形态。

### T2 约束优先于流程

写「什么永远成立」，而不是「先做 A 再做 B」。流程描述会把实现自由度坍缩成单一路径；约束描述保留实现自由，同时让违例可判定。

**反面（流程式）：**
> 控制面先取锁，然后推进代次，再把动作发给适配器。

**正面（约束式）：**
> 约束：存在活动写入者时，其代次**必须**是账本中的最大值。观察到活动写入者持有非最大代次，即构成违例。

流程图与顺序表可以作为非规范辅助保留，但不能替代约束。**例外**：当顺序本身就是合同的一部分（如启动恢复顺序、撤权与投递的先后），流程可以是规范性的，但要在节首用一句约束式总纲说明这个顺序保证了什么。

### T3 状态机的标准写法

有生命周期的对象用统一格式定义：

1. **状态枚举**：状态名 + 一句话语义。
2. **转移表**：`(当前状态, 触发事件) → 新状态`。触发事件限定为：收到某命令 / 超时 / 内部条件成立。
3. **各状态的允许输入集**：收到允许集之外的输入时的行为**必须**显式定义（忽略 / 类型化拒绝 / 转入需要关注）。
4. **超时语义**：每个非终结状态**必须**定义超时行为。没有超时定义的等待状态是合同缺陷。
5. **失败模型**：每种失败的信令方式与引发的状态转移**必须**定义。
6. **图与表双呈现**：图提供直觉（非规范），表提供规范。两者冲突时以表为准。

### T4 单源与统一结构

**单源：** 每个对象、状态机与写入合同只在拥有它的文件里定义一次。其他位置一律引用，不复制定义。

**统一结构：** 同一层的多个文件用同一结构展开（③ 层见 0.5），读者不必为每个文件重新适应阅读预期。

### T5 状态归属

每份状态**必须**有单一权威所有者；其他方持有的是缓存或投影，不是权威副本。规范须回答三问：

- 谁是这份状态的唯一权威？
- 其他方读到的是什么——缓存、投影还是只读视图？
- 部分失败或失联时，谁的状态算数？

「多个所有者可写同一份状态」是不一致的根源。

### T6 对外部产品的显式表态

禁止隐式引用。每个外部产品或协议必须声明它承担什么、本系统保留什么边界、失败时怎样、替换成本多大，以及**它不能承担什么**。最后一项防止「支持 X」被写成「保证 X」。介绍外部机制的表格**应当**设「不能据此推断」一列。

---

## 共用规则

### L1 零修饰语言

禁止修饰词，中英同等生效：革命性 / 颠覆 / 赋能 / 范式转移 / 前所未有 / 强大的 / 极致的 / 无缝；revolutionary / game-changing / powerful / robust / seamless / flexible（作空泛修辞时）/ scalable（无量化时）/ next-generation / state-of-the-art。

判定方法：删掉修饰词，句意不变即删。尤其警惕伪装成专业术语的修饰词——「强大的验证机制」「灵活的授权模型」，无量化定义即空话。

### L2 主语精确

规范性句子的主语必须是具体的角色或组件。禁止「系统会……」「平台可以……」这类无主语句。

**反面：** 「备份完成前验证快照边界与 schema 可读性。」——谁验证？
**正面：** 「备份流程必须在完成前验证快照边界与 schema 可读性。」

### L3 术语单一来源

同一概念全文使用同一个术语，包括大小写。禁止同一概念出现两个名称交替使用。

一个概念确实需要两种写法时（产品散文名与组件标识符，如「工具箱」与 `hctl2-tool`），必须在词汇表里显式声明它们是一对一映射，否则读者会当成两个东西。

### L4 中英文混排与术语处理

HCTL2 的词汇分层由[合同层总则](./docs/design/spec/README.md)定义（核心产品词 / 合同词 / 系统角色名），本条只管写法：

| 术语类型 | 处理方式 |
|---|---|
| 具名对象与角色名 | 按合同层总则的规定形态，全库统一 |
| 已有通用中文译名的技术词 | 用中文（签名、摘要、队列、租约） |
| 无通用译名且中文表达不精确的 | 保留英文，首次出现给中文释义 |
| 介于两者之间的 | 选一个，全文统一 |

**禁止在同一术语内混用中英文**——「直接 mutation」「binding 分歧」既不是中文也不是英文。要么整体译出（「直接改写」），要么首次出现写成「直接改写（mutation）」之后统一用中文。

**首次出现格式：** 新术语在词汇表或首次出现处给一次释义，之后全文统一使用选定形式，不再重复释义。

**微观排版：** 中文与英文、数字之间加一个半角空格；句内标点用全角，英文行内代码、URL、整句英文除外；行内代码与相邻中文之间加半角空格；禁止全角空格作分隔。

**用中文讲透：** 能用中文讲清楚的概念就用中文，不插入英文词当拐杖。发现自己在中文句子里嵌了英文词，问一遍：这个词有中文表达吗？有且不歧义，换掉。

### L5 句式纪律

- 规范性句子一句表达一条约束。复合约束拆成编号列表。
- 每段只推进一个论证步骤。一段里出现两个「因此」时拆段。
- 先给结论，再给论证；先给规则，再给示例。

**短句优先：** 英文技术文本常有长复合句，中文不宜照搬。

- 一个中文句子表达一层意思，下一层另起一句。
- 不用「……的……的……」链式定语，超过两层就拆句。
- 不用从句嵌套。「当 X 发生时，如果 Y 满足，则 Z 必须执行」拆为「X 发生后，Y 必须满足。Y 满足时，Z 必须执行。」

**删除连接词冗余：**

| 冗余表达 | 处理 |
|---|---|
| 所谓的 | 删 |
| 也就是说 / 换句话说 | 说明前一句没写清楚；重写前一句，不要追加解释 |
| 需要注意的是 | 直接陈述需要注意的内容 |
| 正如前面提到的 | 用交叉引用，或判断是否值得重提 |
| 在……的情况下 | 改为「当……时」或直接陈述条件 |
| 对于……而言 | 通常可删，直接说主语 |

### L6 可理解性是结构责任

每节开头一句话说明本节回答什么问题。

表格用于映射类信息（对应关系、转移表、属性矩阵），论述用于论证。已经是表格结构的内容不要写成段落。

章节尽量可独立阅读：依赖其他章节结论时给出显式引用路径，不默认读者按顺序阅读。

### L7 避免翻译腔

中文技术写作最常见的质量问题，是把英文句法照搬到中文——语法正确但读起来不自然。以下模式出现时重写：

| 翻译腔模式 | 示例 | 自然中文 |
|---|---|---|
| 被动语态滥用 | 该结果**被**校验后**被**采纳 | 控制面校验结果后采纳 |
| 形式主语 | **需要注意的是**，摘要**是**必需的 | 摘要是必需的 |
| 英文从句直译 | 当一个事务**已经被**提交**的情况下** | 事务提交后 |
| 抽象名词链 | 实现了对资源的访问的控制机制 | 实现了资源访问控制 |
| of 结构直译 | 对 X 的校验的失败 | X 校验失败 |
| 冗余指代 | 合同**它**保证了…… | 合同保证了…… |
| 生硬抽象 | 确立了基于 X 的治理逻辑根基 | 让治理靠 X 来定夺（说人话） |

**总原则：** 写完一段出声念一遍。读起来不像中文、像翻译过来的英文，就重写。好的中文技术文本精确但不生硬，专业但不冷漠——读者能感觉到作者是用中文思考的，而不是用英文思考再翻译。

### L8 Markdown 与图表格式

- **标题层级：** `#` 文档标题（每文件一个）→ `##` 大章节 → `###` 节 → `####` 小节。避免五级标题，需更细分时改用列表或表格。
- **列表：** 同级列表项语法平行（全为名词短语，或全为完整句）。
- **表格：** 复杂表格前加一句话说明用途；表头列名与术语全库一致。
- **代码块：** 伪代码用 `pseudo`，Shell 用 `bash`，目录树与命令输出用 `text`；块前注明性质。
- **图：** mermaid 图为非规范（提供直觉），图与表冲突时以表为准。图内标签换行用 `<br/>`，`\n` 在 GitHub 上会原样显示。

### L9 交叉引用格式

引用本库其他文件时用相对路径加锚点，链接文本含上下文：

```markdown
详见[系统边界的单写者](./docs/design/spec/system.md#单写者)
```

- 必须给出被引用文件路径 + 锚点。
- 锚点用 GitHub 风格（标题转小写、空格转连字符，中文原样保留）。
- 禁止「上文 / 下文」「详见这里」这类无定位引用。
- 改动某节标题时，必须同步更新指向它的反向引用。

### L10 文档头

`docs/design/` 下每个文件的头部必须标注状态、日期与基线版本，格式与全库一致：

```markdown
> 状态：规范性合同 · 草案 vX.Y.Z<br>
> 日期：YYYY-MM-DD<br>
> 定位：本文回答……
```

头部版本与根 `README.md` 声明的基线不一致是缺陷（`check_version_stamps` 会拦）。变更历史不进正文——转折进[来时路](./docs/design/references/decision-history.md)，小修订进它的台账。

### L11 规范性等级标注

HCTL2 用**文件级**标注：每个文件的状态行声明它整体是规范性合同、规范性（架构层 / 原则层）还是非规范对照。这是本库对上游逐段四类标注的收窄——按现有篇幅，逐段标注的成本大于收益。

在此之上只要求一件事：**规范性文件里的理由段落就近标注**，用 `> 理由：` 引用块隔开，避免读者把论证误当义务。示例一律非规范；示例与合同冲突时以合同为准。

### L12 未决问题措辞

未决工作必须显式标注，不得藏进规范性正文。每个未决项写明：

- **问题**：直接陈述未决的设计问题。
- **影响面**：影响哪个文件、哪个机制。
- **阻塞级别**：阻塞合同 / 实现 / 发布，还是仅影响未来扩展。
- **当前决策**：第一阶段是否实现；推迟的触发条件（何时必须重评）。

禁止把未来工作写成「系统将支持……」式的隐性承诺。

### L13 最小实体原则

落一个新的具名概念前，先证明已有概念的组合表达不了它。

**判定方法：** 移除这个新概念，用已有对象加交叉引用能否表达同一约束？能，就删掉它。

最常见的冗余信号：新对象的每条性质，都已经是某个已有对象的性质。这种对象只是把已有性质重新拥有一遍，删。

引入门槛的**内容侧**判据（独立生命周期、恢复边界、权限边界）在[合同层总则](./docs/design/spec/README.md#词汇分类法)，本条只管写作纪律：新增一个具名概念时，就近给出一句「已有组合表达不了，因为……」。给不出，就别加。

L13 与 T4 是两件事：T4 防「同一实体定义两遍」（去重），L13 防「造一个本不必存在的实体」（不增）。

---

## HCTL2 增补

> 以下三条是本库相对上游指南新增的，针对本库实测出的三种病。

### S1 压缩下限

M6 只给了压缩的上限——删掉不推进论证的段落。它没有下限，而本库上一轮大修恰好把这股力用过了头，出现了「结论留下、判据被删」的电报体。

**规则.** 一个断言若在本库其他位置没有展开，就必须在同一段带上它的判据：为什么成立、凭什么、否则会怎样——三者至少居其一。

**判定.** 把这句话交给一个没参与讨论的读者，他能否复述出「为什么」。不能，就是压过头了。

**反例：**

> SQLite 锁不是外部副作用隔离。幂等键、代次、租约、outbox 和 readback 必须共同工作。

两句都是结论，没有一个字说明 SQLite 锁为什么不够。读者无法据此判断自己的实现是否违规。

**正例：**

> 引擎重试只是路标再次进入等待态，由 HCTL 铸造新义务，旧义务连同它的票一起作废——重试不灌票。

判据在前、口号在后，同一句自足。

**S1 不反对口号，反对没有判据的口号。** 「重试不灌票」「换人不换裁判」「掉线不丢身份」这类浓缩表述是本库的资产，条件是它们就近有判据支撑，或在本层的职责范围内已经完整（比如架构层给出「有五种路径、混用会出什么事、细则在合同层」，就不必逐个解释）。

### S2 自造词密度上限

本库有约四十个具名概念，句子容易变成概念串烧。

**规则.** 一个句子最多引入一个需要跳转才能理解的具名概念。第二个起，要么就地给一句话释义，要么拆句。

**反例：**

> `attempt_generation` 是语义 owner 代次；它既不是后续激活时分配的 `runtime_generation`，也不是 control/site/backend 的基础设施 fence 代次，所有层都必须分别携带并逐项校验。

一句话里五个代次概念，且「基础设施 fence 代次」是中英名词堆（L7）。

### S3 每层说每层的话

每层用该层的语言写，跨层引用而不复述。落点是 0.4 路由表：写之前先定位这句话属于哪一层，再用该层的允许词汇与文风。

审计时分两个方向查，两个方向都要查：

- **下沉**：高层文件里出现了低层细节（合同词、状态机口径、字段名、锁路径）。
- **上浮**：低层文件里出现了本该在高层的论证、口号或愿景表述。

---

## 反模式清单

命中以下任一模式时，相关段落必须重写。

### 宣言文风反模式

| 反模式 | 症状 | 修正方向 |
|---|---|---|
| 修饰词代替论证 | 用「强大的」「灵活的」替代具体性质描述 | 删修饰词，补可验证的性质陈述（M6 / L1） |
| 声明代替论证 | 「本系统保证了一致性」——没有论证过程 | 按 M1 叙事弧补论证 |
| 稻草人论证 | 审视现有方案时简化或歪曲其设计 | 以最强形态描述再驳斥（M1 第 2 步） |
| 功能枚举式架构 | 「本层提供 A、B、C 能力」 | 从约束推导（M2） |
| 类比代替逻辑 | 仅用「就像 X 一样」论证设计成立 | 类比降级为直觉辅助，补约束推导（M4） |
| 原则不自洽 | 设计原则只有短语碎片，或依赖前文才能理解 | 每条写成完整、独立、可引用的句子（M3） |
| 回避代价 | 只讲好处，不讲牺牲了什么 | 按 M1 第 4 步补「后果与代价」 |
| 模糊承诺 | 不区分已证明 / 推测 / 未来 | 明确区分（M6）；未来工作改写为未决项（L12） |

### 技术文风反模式

| 反模式 | 症状 | 修正方向 |
|---|---|---|
| 不可测试的必须 | 「实现必须高效」「应尽快响应」 | 量化或删除（T1） |
| 强度不明 | 用「需要」表达义务 | 归入三档之一或改写为描述句（T1） |
| 流程当规范 | 「先做……然后……」出现在规范文本，且顺序不是合同本身 | 改写为约束加状态机（T2 / T3） |
| 无主语规范 | 「系统将保证……」 | 指明具体角色或组件（L2） |
| 等待无超时 | 状态机有等待态但无超时转移 | 补超时语义（T3.4） |
| 失败未定义 | 定义了正常流程但未定义失败行为 | 补失败模型（T3.5） |
| 定义重复 | 同一对象在多处定义 | 单源定义，其余引用（T4） |
| 冗余实体 | 新概念的每条性质都已是某已有对象的性质 | 删除，用已有对象加引用表达（L13） |
| 隐式外部依赖 | 提到外部产品但不声明它承担什么、不能承担什么 | 补表态（T6） |
| 状态无主 | 多方可写同一份状态 | 指定唯一权威，其余降为投影（T5） |

### 中文表达反模式

| 反模式 | 症状 | 修正方向 |
|---|---|---|
| 结论电报 | 结论留下、判据被删，读者复述不出「为什么」 | 补判据（S1） |
| 概念串烧 | 一句话里三个以上需要跳转的具名概念 | 拆句或就地释义（S2） |
| 术语内混排 | 「直接 mutation」「binding 分歧」 | 整体译出，或首现给对照后统一（L4） |
| 名词链 | 「同一份上下文加旧变更集版本的指针」 | 拆成动宾结构（L7） |
| 长句套嵌 | 单段数百字，多个分号条件并列 | 拆成编号列表（L5） |
| 越层 | 愿景层出现字段名；合同层出现「为什么更好」 | 按 0.4 路由表搬家或改引用（S3） |

---

## 自检清单

### 宣言文风

- [ ] 问题定义用一段话精确描述，不使用修饰词？
- [ ] 现有方案以最强形态描述后再指出不足？
- [ ] 设计选择从约束推导，不是功能枚举？
- [ ] 后果与代价诚实写出？
- [ ] 每条原则是完整、独立、可引用的句子？
- [ ] 类比标注了适用范围与失效边界？
- [ ] 每段问一遍：删掉后论证链是否断裂？
- [ ] 结论在每节第一段给出？
- [ ] 区分了「已证明的」和「推测的」？

### 技术文风

- [ ] 每个必须 / 应当 / 可以都能写出一条契约测试？
- [ ] 全文只用三档关键词，没有用「需要」表达义务？
- [ ] 约束优先于流程；保留的流程都是合同本身，且节首有约束式总纲？
- [ ] 状态机五件套齐：枚举、转移表、允许输入集、超时、失败模型？
- [ ] 每份状态有单一权威所有者，其余是投影？
- [ ] 对象与写入合同单源，其余位置引用？
- [ ] 同层文件结构统一？
- [ ] 外部产品都声明了承担什么、不能承担什么？
- [ ] 规范性句子的主语是具体角色或组件？

### 共用

- [ ] 每节开头一句话说明本节回答什么问题？
- [ ] 删一遍修饰词，句意不变的都删了？
- [ ] 同一概念全文同一术语，包括大小写？
- [ ] 没有同一术语内的中英混排？新术语首现给了一次释义？
- [ ] 中英之间半角空格、句内标点全角？
- [ ] 交叉引用用相对路径加锚点，改标题已同步反向引用？
- [ ] 文档头状态、日期、基线版本齐且与根 README 一致？
- [ ] 理由段落就近标注，示例标为非规范？
- [ ] 未决项写明影响面与阻塞级别，未写成隐性承诺？
- [ ] 每个新增具名概念都过了「已有组合表达不了」测试？
- [ ] **每个断言的判据在本段或本库某处可查（S1）？**
- [ ] **一句话不超过一个需要跳转的具名概念（S2）？**
- [ ] **这段话属于哪一层，用的是不是该层的语言（S3）？**

---

## HCTL2 未采纳的上游条目

记录在此是为了避免反复讨论，也为了将来需要时知道去哪里取。

| 上游条目 | 不采纳的理由 |
|---|---|
| 原语合约模板、conformance 判定 | 本库合同层是**领域合同**，不是 API 规范；套用原语模板会把设计文档变成接口手册 |
| 确定性编码规则 | 已在[系统边界](./docs/design/spec/system.md)定为 RFC 8785 JCS 加 SHA-256；这是内容不是文风 |
| 演进纪律（SemVer、字段生命周期、废弃流程） | 本库已有等价制度：合同变更推进补丁号、配契约测试失败用例、转折进来时路 |
| 故障与对手模型前置 | 第一阶段是单用户模型，安全边界已独立成节；引入对手模型模板属于内容扩张 |
| 未知与畸形输入处理 | 本库没有 wire format；等价关注（provider 事件缺字段只成 Snapshot）已在系统边界 |
| 规范性测试向量 | [契约测试矩阵](./docs/design/contract-tests.md)是等价制度 |
| identifier 后缀分型 | 依赖上游的字段命名规范单源；本库合同层用语义名而非代码词形，暂不适用 |

---

## 参考谱系

范本用于校准手感，不构成规则。规则正文已经把要学的动作提炼出来了。

| 范本 | 学什么 |
|---|---|
| BTC 白皮书 (Nakamoto, 2008) §1–4 | 论证密度：九页建立完整论证链，每段推进一步 |
| No Silver Bullet (Brooks, 1986) | 系统性审视候选方案为何失败；区分本质复杂性与偶然复杂性 |
| Fielding REST 论文 (2000) Ch.1–3 | 从约束推导架构，而非从功能枚举出发 |
| The Rise of "Worse is Better" (Gabriel, 1991) | 用具体对比论证设计哲学 |
| The Bitter Lesson (Sutton, 2019) | 极端论证密度的样子（但注意 S1 的下限） |
| Raft 论文 (2014) | 把「可理解性」当设计目标，每节开头说明本节回答什么 |
| RFC 2119 + 8174 | 规范性关键词的精确语义 |
| TLS 1.3 (RFC 8446) 附录 A | 状态机的标准写法 |
| DNS (RFC 1034 + 1035) | 概念与规范分篇：概念读者不必翻字段定义 |
| Semantic Versioning | 一页纸定义整个生态的兼容性沟通规则 |
| K&R《C 程序设计语言》附录 A | 散文体规范的范本：零修饰、零冗余、每句可测试 |
| Bach《UNIX 操作系统设计》 | 复杂系统的统一结构展开：数据结构 → 操作 → 约束 → 接口 |

**中文译本的笔法**（本库最该学的一栏）：

| 译本 | 写作动作 |
|---|---|
| 《程序设计实践》（裘宗燕译） | 精确但不生硬：用自然中文讲清接口、失败与边界 |
| 《C 程序设计语言》（徐宝文等译） | 短句、低修饰密度：每句只保留一个技术断言 |
| 《重构》（熊节译） | 术语分层：模式名保留英文，通用概念用中文，首现定义 |
| 《深入理解计算机系统》（龚奕利、雷迎春译） | 不用英文词遮蔽解释义务 |
| 《黑客与画家》（阮一峰译） | 技术散文的节奏：短句加具体例子，降低说教感 |
