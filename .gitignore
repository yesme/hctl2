# Local scratch; evidence audits are curated under docs/research/, design under docs/design/.
.tmp/
buck-out/
# Local Buck2 config (remote cache endpoints); the project root is the repository root.
.buckconfig.local
