# 对应源码

本安装包对应的锁定上游源码单独发布为：

```text
@SOURCE_PACKAGE_ID@.tar.gz
```

它应与本安装包出现在同一个 HCTL2 Release 下载页，并提供相邻的 `.sha256` 校验文件。安装和运行 HCTL2 不需要下载源码包。

Dagu、Vikunja 与 Cinny 的源码用于履行 GPL/AGPL 二进制分发义务；其余上游源码用于复现构建与审计。HCTL2 不依赖上游仓库继续保存这些版本，发行方应持续保留对应的源码归档。
