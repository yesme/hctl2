# path<TAB>term<TAB>required substring on the pre-existing line
# Baseline exceptions are tied to the current first-use line. Moving or editing
# that line makes the checker require an explicit Chinese counterpart.
#
# Empty on purpose. Grok's R3 audit (任务书三 §1, 2026-09-04) found all 46
# baseline entries were rule exemptions rather than places the body should
# skip 对照; the counterparts were written back into the design layer instead.
# check_docs_toolchain_fixtures.sh keeps this file comment-only: a term that
# lacks its counterpart is fixed in the text, not exempted here.
